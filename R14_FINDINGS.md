# R14_FINDINGS.md — review iteration 14, findings of record

**Run 2026-08-19** by the G1–G9 executor session via the workflow
snapshot `tools/review/flip-review-iter14.js` (fix-in-iteration per
plan §4; quiet is judged AFTER fixing). 7 finder lanes, 2-vote refute,
33 agents (33 completed, 0 errors), HEAD e86013f (code HEAD a5fc1a0 =
G1–G9 + floor 718; suite 722/722 both hosts). NOTE: the first launch
died with a session restart; the run of record is the full fresh
relaunch (`wf_0461b927-169`, ~43 min, ~3.79M subagent tokens).

**Pre-fix verdict: NOT QUIET — 13 raised → 13 confirmed (5 major /
8 minor, 0 blockers), 0 killed.** Per Adnaan's 2026-08-19 instruction,
the fixes are specified in `R8_IMPLEMENTATION_PLAN.md` §3 (H1–H9,
vetted from these findings) and EXECUTED BY THE NEXT SESSION; the
iteration-14 quiet judgment (R5_TRIAGE §7: zero confirmed blockers AND
every confirmed finding fixed with both gates green) is recorded there
once the H set lands.

**Driver-core lane ruling resolved:** Adnaan's conditional (drop
driver-core from iteration 15 if 14 confirms zero driver-core
findings) is NOT triggered — driver-core confirmed #4 and #5. All 7
lanes stay for iteration 15.

**Known duplicates/clusters:** #1 ≡ #6 (G2's session branch anchors
the built-in-keybind fallback on player-typed metadata instead of the
ledger slug — regressions-r13 + fix-validate, independently). #2 ≡ #3
(G1's counted_at anchor is captured AFTER the sheet's row read —
regressions-r13 + payment-split, independently). #11/#12/#13 are
tests-coverage pins for G5/G4/G7 halves. Three findings (#1/#6, #2/#3,
and the #11–#13 pin set) are gaps in or regressions from the G fix
set; the rest are pre-existing. #1 survives at 1/2 — its dissenting
refuter argued intake translation would pre-reject such bundles, which
holds only for RAW payloads; v2 payloads (the production norm) carry
player-produced frames.csv and first meet our resolution chain at the
retranslate, so the harm is reachable there (and the twin #6 survived
0/2).

Machine-readable results incl. both refuter verdicts: session
scratchpad `r14-results.json`.

## Confirmed findings (fix specs of record: plan §3 H1–H9)

### #1 [MAJOR] pipeline/fix.py:928 (regressions-r13, refuters 1/2)

**G2 made the retranslate's session branch live with its built-in fallback anchored on player-typed metadata instead of the ledger slug — a degraded metadata game name now empties every input column and terminally rejects a good session**

Commit 2d1b071 (G2) changed _dispatch (pipeline/fix.py:494-496) to pass game_override only on reroute plans, making retranslate_from_sidecars' previously-dead else branch (fix.py:894-896, 928-930) the production path for every non-reroute FIX_RETRANSLATE. That branch anchors the built-in-keybind fallback on the player-typed chain `game_info.get("name") or meta.get("game_name") or s.get("game_title")` plus exe_name — NOT on the ledger slug that _dispatch holds in `game`. Its siblings follow the F4 doctrine correctly: fix_key_hygiene (fix.py:1035) and fix_actions_context (fix.py:1141) both call resolve_keybind(game_name=game) with the LEDGER slug (accepted entry 37: "game_name=the ledger slug"). When the metadata chain is unusable and raw/keybind.json is absent (keybind.json is optional per the capture contract), game_key_from_name returns None and resolve_keybind returns {} — proven by execution: resolve_keybind(keybind_path=<missing>, game_name=None, exe_name=None) -> {}. Driven end-to-end through the PRODUCTION chain in a scratch copy outside the repo (plan_fixes -> apply_fixes -> _dispatch; plan printed ('FIX_RETRANSLATE', {'rerouted': False})): a bundle with metadata game {"name": 12345} (the exact non-str provenance class r-loop 9 proved real for exe_name in this same metadata block — see the guard at fix.py:887-889 — and r-loop 3 proved for game_title, per keybinds._collapse's docstring), no exe_name, no keybind.json, ledger game="kamla": apply_fixes returned error=None and frames.csv came back with `DEGRADED keys: [] actions: []`. Control with metadata name "Kamla": keys ['A','E','S','W'], actions ['interact','move_down','move_left','move_up']. Pre-G2 behavior on the SAME degraded bundle (retranslate_from_sidecars(work, game_override="kamla")): identical full keys/actions. The truthy-garbage name also short-circuits the `or` chain, so even a pristine …

**Scenario:** A Kamla session with raw sidecars but no keybind.json and a degraded metadata game block (name numeric/absent/unmatchable, exe_name numeric/absent — both classes documented in the wild by r-loop 9 / r-loop 3) hits any retranslate-planned FAIL (SYN_TS_NOT_PTS, STR family with sidecars — 'the universal strong fix'). Post-G2 the retranslate runs under an empty keybind: bound_literals({}) strips 100% of key presses and build_resolver({}) resolves zero actions, silently (apply_fixes reports no error). Revalidation then finds key_frames==0 while the video shows live motion -> INP_KEYS_MISSING (validate.py:669, blocking, UNFIXABLE) and distinct_actions 0 < 3 -> CNT_ACTIONS_FEW (validate.py:640, blocking, UNFIXABLE) -> bin 3 -> terminal REJECT with 're-record (never fabricate)' coaching — for keys the fix itself deleted, on a session the pre-G2 code translated perfectly (the exact disaster shape resolve_keybind's own r-loop-7 comment documents: 'REJECTED with zero fix attempts and the player is coached to play more actively for keys our own parser deleted'). Secondary variant: a metadata …

**Finder's proposed fix (vetted into plan §3 — deviations stated there):** Thread the ledger slug into the session branch and anchor the fallback on it, exactly as F4 does: give retranslate_from_sidecars a ledger_game param that _dispatch always passes (game when game in C.GAMES); in the else branch use it for the resolve_keybind fallback (resolve_keybind(keybind_path=raw/'keybind.json', game_name=ledger_game, exe_name=exe_name) — the session's own keybind.json still wins when usable, preserving r13 #4's intent) and for the KEYBIND_PATCHES slug at fix.py:931 (currently also metadata-derived 'unknown_game' in this class, silently skipping the patches).

### #2 [MINOR] pipeline/reports.py:358 (regressions-r13, refuters 0/2)

**G1's arm-2 count anchor is captured AFTER the sheet's row read, so a ZIP_ADJ_CHANGED adjudication landing during the sheet build is missed by ts >= counted_at and the stamp silently strands the new bytes' hours — the r13 #2 harm surviving in the window the fix's own rationale claims cannot exist**

abf052b (G1) keys the recorded-'' skip on `ts >= counted_at` (reports.py:356-360) and justifies it with 'a marker BEFORE the sheet's row read would have left a REAL md5 in the snapshot and this arm is never reached' (reports.py:352-355 comment; same sentence in the commit message and accepted-entry 55). That claim is false for the window between the row read and the counted_at capture: build_sheet_rows takes its md5 snapshot from ONE SELECT at its top (reports.py:711-716; md5_out filled from that result set at 858-862/928-942), while counted_at is captured only AFTER write_payment_sheet returns (run.py:1294 vs run.py:1309). The daily stamps run in hl-H while hl-S concurrently heals/supersedes/downloads by design (ledger.update_where_md5's own comment, ledger.py:203-211). A deferral adjudication (ingest.py:875-901: clear, then the ZIP_ADJ_CHANGED marker, then the md5 backfill) landing after the SELECT but in an earlier second than counted_at is therefore neither 'before the row read' (snapshot already holds '') nor '>= counted_at'. Proven by execution against the real Ledger + reports._stamp in a scratch copy: row md5 '' at snapshot time, marker at ts 2026-08-19T10:00:00+00:00, counted_at 2026-08-19T10:00:01+00:00 -> _stamp returned True and uploaded_reported_at landed (probe output: 'mid-build marker (1s before counted_at): stamped = True | uploaded_reported_at = 2026-08-19T10:05:00+00:00'); the same call with counted_at equal to the marker's second correctly skipped ('same-second marker: stamped = False' plus the loud SKIPPED stderr line). The resume path inherits the identical blind window (rec['at'] is the same post-build capture, run.py:1306-1316; the resume pre-filter _bytes_changed is real-vs-real only and passes ''-recorded sids through to _stamp).

**Scenario:** A zip-class root recorded with the '' sentinel is counted on today's sheet. During the remainder of the sheet build / CSV write (concurrent hl-S), the re-upload's download completes and the deferral adjudicates the bytes CHANGED: it clears the stamps and duration_raw_s, writes the durable marker, and backfills the real md5 — with the marker's second earlier than the counted_at captured at run.py:1309. Minutes later the stamps run: arm 2 queries markers ts >= counted_at, finds none, and stamps uploaded_reported_at + accepted_reported_at onto the row now holding the NEW bytes — with no output at all. No self-heal remains (the download already ran, md5 is now real, so no future deferral fires), the F6 probe refill restores duration_raw_s, and the stamped root is excluded from every future sheet by the late-arrival guard (build_sheet_rows requires not uploaded_reported_at): the corrected re-upload's hours silently never reach any sheet — the identical harm r13 #2 was confirmed for, in the row-read-to-counted_at window that G1's '>=' covers only when the marker falls in counted_at's …

**Finder's proposed fix (vetted into plan §3 — deviations stated there):** Capture counted_at BEFORE the sheet's row read — move the `counted_at = datetime.now(timezone.utc).isoformat(timespec="seconds")` capture above the reports.write_payment_sheet call at run.py:1294 (still writing it into the durable record as "at"). Every marker written after the snapshot moment then satisfies ts >= counted_at, so the existing '>=' logic covers the whole window; a marker in the capture's own second but before the row read is harmless (the read then sees either '' with the marker >= counted_at → correct skip, or the backfilled real md5 → routes to the CAS arm).

### #3 [MINOR] pipeline/run.py:1309 (payment-split, refuters 0/2)

**counted_at is captured after the sheet's row read, so a ZIP_ADJ_CHANGED adjudication landing in that gap defeats _stamp arm 2: the stamp lands silently and the re-upload's uploaded hours reach no sheet**

G1's recorded-'' skip (reports.py:356-368) fires only when a ZIP_ADJ_CHANGED event has ts >= counted_at, justified by the comment at reports.py:352-355 that 'a marker BEFORE the sheet's row read would have left a REAL md5 in the snapshot and this arm is never reached'. But run.py captures counted_at at line 1309 AFTER write_payment_sheet (line 1294) returns, while the md5 snapshot is filled from build_sheet_rows' single row read at reports.py:710 — leaving the interval (row-read, counted_at) uncovered: it spans the whole CSV+MD build (rollup, incomplete_list query, reject-detail query, two file writes) plus the durable-record write. The download-time deferral (ingest.py:879-900) runs concurrently in the D thread; when it adjudicates CHANGED inside that interval, the marker's ts is < counted_at, so _stamp's arm-2 query (reports.py:357-360) finds nothing and falls through to the unconditional ledger.update at reports.py:367 — no skip, no loud line. Proven by running the real code against a scratch ledger (/tmp/payprobe/s10_race.py): a heal-class '' root (md5='', duration preserved, breadcrumb event present) is counted by build_sheet_rows (counted=['Z'], md5 snapshot {'Z': ''}, 1.0h uploaded); the deferral block (ingest.py:886-900 executed verbatim) then clears the marks, writes the marker and backfills md5='newmd5'; counted_at is captured 1.1s later; mark_uploads_reported then returned 1 with empty stderr — output: "stamped u (expect 0 if arm-2 worked; BUG if 1): 1 | stderr: (silent)" and "row: md5 newmd5 up_rep 2026-08-19T07:12:42+00:00 dur None". The falsely-landed stamp cannot self-heal: the deferral already ran (its clear preceded the stamp), and no second adjudication ever fires for this generation. The same record 'at' is replayed by _resume_daily_send (run.py:1138/1143), so the crash-resume path inherits the identical hole.

**Scenario:** A player's zip-origin session was quarantined and the operator fixes the folder path; the quarantine heal re-registers it with md5_video='' and stamps+duration preserved (ingest.py:302-330), and the D thread starts re-downloading the corrected re-upload. The 14:00 IST daily sheet counts the root's old 1.0h uploaded (snapshot ''). During the ~1-2s between build_sheet_rows' row read and run.py:1309's counted_at capture (CSV+MD write, record write), the download completes and the deferral proves the bytes CHANGED: it clears the (already-NULL) marks, writes the ZIP_ADJ_CHANGED marker, and backfills the real md5. The stamps then run: arm 2 sees no marker >= counted_at and silently plants uploaded_reported_at on the adjudicated slot. The re-upload later probes at 4200s and delivers — S10 shows the consequence across every later sheet: W2 row = uploaded 0.0h / accepted 1.14h (accepted flows via accepted_due because the stamp is set), W3 empty; the 1.17h of re-uploaded footage never appears in any *_hrs_uploaded column, with zero loud lines — the exact 'stamp written for something the sheet …

**Finder's proposed fix (vetted into plan §3 — deviations stated there):** Capture the anchor BEFORE the sheet's row read and thread that one string everywhere: in send_daily_report_if_due, move `counted_at = datetime.now(timezone.utc).isoformat(timespec="seconds")` from run.py:1309 to just above the write_payment_sheet call at run.py:1294, keeping it as the record's "at" and both stamp calls' counted_at (identical-string property preserved). This is conservative-correct by G1's own argument: a marker at/after the pre-read instant means the adjudication is not provably pre-count, so skipping is the money-safe direction (the '>=' rationale at reports.py:352-355 already accepts skipping a same-second post-count marker), while a marker strictly before the read genuinely leaves a real md5 in the snapshot and never reaches arm 2. Add a test that writes the marker between the build and the stamp with counted_at captured pre-build, pinning stamped==0 plus the loud …

### #4 [MAJOR] pipeline/continuous.py:193 (driver-core, refuters 0/2)

**AlertBook dedup keys on exact alert text, but every rclone-backed failure alert embeds per-attempt-timestamped stderr — the 60-min TTL never fires and a Drive/network incident becomes a per-retry Telegram alert storm on the flip's only ops surface**

AlertBook.alert dedups on the literal message string: continuous.py:192-194 `last = self._sent.get(text); if last is not None and now - last < self._ttl: return`. Three high-volume alert sites build that text from rclone stderr: (1) continuous.py:539-540 `alert(f"download failed for {sid} (will retry): {e}")` where e is ingest.py:811-812 `DownloadError(f"rclone copy failed x3: {p.stderr.strip()[:300]}")`; (2) continuous.py:1105 `alert(f"upload failed for {sid}: {out.detail}")` where detail is deliver.py:128 `DeliverError(f"upload failed: {p.stderr.strip()[:300]}")`; (3) the scan lane's guard alert continuous.py:577-579 wrapping ingest.py:112 `RuntimeError(f"rclone lsjson failed: {p.stderr.strip()[:300]}")`. rclone stderr lines begin with a wall-clock timestamp — verified with the real binary on this machine (rclone v1.75.0): `rclone copy /tmp/definitely-missing-src-dir-xyz /tmp/rclone-probe-dst` printed `2026/08/19 12:47:07 ERROR : ... directory not found` / `2026/08/19 12:47:07 ERROR : Attempt 3/3 failed...` (exit=3). Retries are >=5 min apart (CONT_DOWNLOAD_RETRY_MIN=5, CONT_UPLOAD_RETRY_MIN=10, scan idle 300s), so the embedded timestamp differs on every attempt and the dedup key never repeats. Executed proof with the REAL AlertBook and an injected clock (/tmp/probe_alertbook.py, stubbed telegram.send_message): one session retried every 5 min for 1h with production-shaped timestamped text -> `timestamped text: 12 sends in 1h (designed contract: 1 per 60-min TTL)`; identical cadence with stable text -> `stable text: 1 sends in 1h`. The class docstring (continuous.py:174-178) and accepted item 11 ("cooldown + hourly alert" is the contract) state the intended once-per-TTL cadence; only the stable-text case (e.g. run_rclone's synthetic `timed out after Ns`, ingest.py:67) actually gets it.

**Scenario:** A Drive I/II or network incident at the flip — exactly when CONT_DAILY_REPORTS is False and the Telegram digest is the operator's only view. Every eligible DISCOVERED/DOWNLOADING row fails its download in seconds (rclone exits non-zero fast), alerts once, cools 5 min, and re-alerts with a fresh timestamp; the serial D lane cycles the whole backlog (~12 alerts/hour PER row instead of 1/hour total per condition — with the flip-era backlog of dozens-to-hundreds of DISCOVERED rows that is hundreds to >1000 alerts/hour). The scan lane adds one varying-text alert per 5-min poll and the U lane one per READY sid per 10 min. The flood buries the 3-hourly digest and the actionable one-shot alerts (F7 disk-low, quarantines) in noise, and — per R8's own analysis of Telegram's ~20 msg/min per-chat limit — drives sendMessage 429s, so the digest send itself starts failing (TelegramError -> anchor unwritten -> retry in 600s) during the precise incident the operator must see to decide a rollback.

**Finder's proposed fix (vetted into plan §3 — deviations stated there):** Make the dedup key stable: normalize rclone stderr at the single choke point run_rclone (ingest.py:55-67) by stripping the leading `YYYY/MM/DD HH:MM:SS ` prefix from each stderr line before returning (all three sites inherit the fix), or give AlertBook an explicit stable dedup key parameter (e.g. alert(text, key=f"download-failed:{sid}")) and pass one at the rclone-backed call sites, keeping full volatile text in the message body.

### #5 [MINOR] pipeline/continuous.py:536 (driver-core, refuters 0/2)

**A DISCOVERED row whose Drive folder was deleted retries forever — no terminal state, no prune arm, a permanently inflated 'undownloaded' backlog counter and perpetual alert/API churn**

rclone copy from a deleted source directory fails with 'directory not found' (verified live: exit=3, `ERROR : ... error reading source root directory: directory not found`). ingest.download raises the kind-less transient DownloadError after 3 attempts (ingest.py:804-812), and _download_one routes it back to DISCOVERED with a 5-min cooldown and an alert, forever (continuous.py:536-540). Nothing ever terminates the loop: the scan's two vanished-folder prune arms cover only `incomplete` rows (ingest.py:638-649) and INT_PATH QUARANTINED rows (ingest.py:658-675) — there is no arm for DISCOVERED rows; the move-heal (ingest.py:346-386) requires the same session id to REAPPEAR at a new path, which never happens for a deletion (or for the documented 'normal operator fix' of renaming a bad session folder, which mints a NEW sid — review-r5 #1 comment, ingest.py:651-653); the 12h media reclaim needs a dir that holds bytes, but ingest.download's `dst.mkdir` (ingest.py:802) leaves only an EMPTY dir, which _held_discovered scores as no media (continuous.py:428-439); the quarantine kind is unreachable pre-payload. The row therefore cycles pick->fail->DISCOVERED every ~5 min indefinitely and is counted in the digest's `backlog_undownloaded=counts.get("DISCOVERED", 0)` (continuous.py:1452) forever.

**Scenario:** An operator deletes a mistaken/junk upload folder from Drive I after the scanner has already registered it (routine drive hygiene — the codebase added two sibling prune arms precisely because registered folders vanish, and renaming a mis-named session folder orphans the old sid the same way). From then on, permanently: ~12 pick cycles/hour x 3 rclone invocations of failing Drive API calls per row; one Telegram alert per cycle today (see the dedup finding; still one per hour per row after that fix); the digest's 'undownloaded' backlog never drains to its true value, so at the flip the operator reads phantom intake that no amount of pipeline health will ever clear, and an --until-idle canary run can never reach idle while such a row's cooldown is expired.

**Finder's proposed fix (vetted into plan §3 — deviations stated there):** Add a third vanished-folder arm to ingest.scan under the same healthy-listing guard as the existing two (games_present + `drive_path not in listed_dirs`): for a DISCOVERED row whose registered path is gone from a successful listing of a non-empty game tree, transition it to a terminal state (e.g. QUARANTINED with detail 'folder gone from Drive I — dropped from intake', no INT_PATH reason so it stays off the chase list) with the same loud per-row log line the other arms print; alternatively count consecutive not-found download failures in _download_one and terminalize after N.

### #6 [MINOR] pipeline/fix.py:928 (fix-validate, refuters 0/2)

**G2 non-reroute retranslate resolves its built-in fallback keybind from metadata-derived names, discarding the ledger slug in hand — empty resolution strips 100% of key presses into an unfixable reject**

G2 (commit 2d1b071) made _dispatch pass game_override=None on every non-reroute FIX_RETRANSLATE, so retranslate_from_sidecars now resolves the keybind at fix.py:928-930 via resolve_keybind(keybind_path=raw/'keybind.json', game_name=game_name, exe_name=exe_name), where game_name is built at fix.py:894-895 from game_info.get('name') or meta.get('game_name') or s.get('game_title') — the raw metadata and the claim, never the ledger slug that _dispatch holds in its `game` parameter. This diverges from the F4 shape both sibling fixes use: fix_key_hygiene (fix.py:1035) and fix_actions_context (fix.py:1141) pass game_name=<ledger slug> for exactly this fallback. When raw/keybind.json is absent (the common case — CLAUDE.md documents keybind.json as optional for Kamla/OW) and none of the three name sources resolves, I proved by execution: resolve_keybind(missing file, game_name='') returns {} and bound_literals({}) is frozenset() (probe output: "resolve_keybind(missing file, game_name='') -> {}", "bound_literals -> frozenset()", "game_key_from_name('') -> None"); translator/v2._v2_rows then keeps a key only `if k in bound`, so an empty bound strips every key press (source read, and translator/translate.py:100-110's own comment documents the identical failure for a parsed-but-unusable file — a case it guards, unlike this one). KEYBIND_PATCHES and the ctxmod context-games test at fix.py:931/999 also key on the metadata-derived slug ('unknown_game' here), and fix_sessionjson_recompute is called with that slug (fix.py:1010). Pre-G2 the unconditional game_override=game (always in C.GAMES) made this corner impossible — this is a strict regression of the prime-suspect commit. The r13 test pins (test_r_loop13.py:255/278) cover only bundles whose names resolve; the fallback corner is unpinned.

**Scenario:** A v2 upload with raw sidecars but no raw/keybind.json, whose metadata game object carries no usable name/exe and whose session.json game_title is an empty string (checker-passing: game_title present and a string, and _map_game_identity skips a falsy claim), raises any retranslate-group FAIL (e.g. 'raw recomputation: dx/dy differs' -> SYN_TS_NOT_PTS). plan_fixes emits FIX_RETRANSLATE {'rerouted': False}; _dispatch passes game_override=None; the keybind resolves to {}; bin_session+_v2_rows write a frames.csv whose input_keys column is empty on every row while motion survives. Revalidation then raises INP_KEYS_MISSING (blocking, unfixable, video_active) plus CNT_ACTIONS_FEW -> bin 3 -> REJECTED with 're-record (never fabricate)' coaching — a wrongful terminal reject and an unpaid player for a session whose raw data was complete and whose correct keybind (the ledger slug's built-in, KEYBINDS['kamla'/'outer_wilds']) was available in _dispatch's own `game` argument. A second flavor: metadata naming the OTHER in-scope game re-bins under the wrong game's keybind and rewrites game_title to …

**Finder's proposed fix (vetted into plan §3 — deviations stated there):** Thread the ledger slug into the non-override path: give retranslate_from_sidecars a fallback_game parameter (passed as `game` from _dispatch), use it as resolve_keybind's game_name when the metadata chain yields nothing, and after resolution fall back to dict(KEYBINDS[fallback_game]) when the resolved keybind is empty — mirroring resolve_keybind's own parsed-but-unusable fallback and the F4 shape fix_key_hygiene/fix_actions_context already use. Use the same fallback slug for KEYBIND_PATCHES, the ctxmod context test, and fix_sessionjson_recompute when the metadata-derived slug is 'unknown_game'.

### #7 [MINOR] pipeline/fix.py:244 (fix-validate, refuters 0/2)

**Joint head+tail edge cuts are never checked against MIN_CLIP_S at plan time — a 70-78s clip with confirmed flags on both edges burns a fix attempt reaching an inevitable reject under a misdirecting reason**

The r12 #7 / G3 CNT_SHORT arms (validate.py:488-498 _edge_short, used at 512-525 and 537-544) judge each edge INDIVIDUALLY: head remain = dur_true-(t+1.0), tail remain = t-1.0. When a session carries one confirmed head flag and one confirmed tail flag that each pass their own check, plan_fixes (fix.py:173-178) accumulates head_cut=max(t+1.0) and tail_cut=min(t-1.0) and the cut-assembly at fix.py:244-249 emits both cuts with no check of the joint keep (tail_cut-head_cut) against C.MIN_CLIP_S. Proved by execution: plan_fixes([CNT_NOTIF_EDGE t=2.0 head, CNT_NOTIF_EDGE t=73.0 tail]) returns steps=[('FIX_CUT_SEGMENTS', {'cut': [(0.0, 3.0), (72.0, 1000000000.0)]})] (probe output above) — on a 75s clip the only keep window is [3.0, 72.0] = 69.0s, and cutter.py:133-137 drops any segment under C.MIN_CLIP_S ('< 70s minimum'), so cut_segments returns segments=[]. _fix_one (continuous.py:989-995) / _fix_phase (run.py:664-671) then REJECT with 'split produced no >=70s segment (dropped 1)'. The joint outcome is fully decided at map time — both reasons, both t's and dur_true are in hand — which is precisely the criterion r12 #7 used to move the single-edge version of this inevitability to a map-time CNT_SHORT (entry 50); the composition case was left open.

**Scenario:** A 75s clip with a confirmed Steam-toast notification at t=2.5s and a confirmed chat overlay at t=73s: both edge checks pass individually (head remain 71.5s, tail remain 72s), the verdict is bin 2 with two fixable reasons, one of the two fix attempts is charged (led.update fix_attempts+1 at the FIXING transition), the video is losslessly cut, every segment is dropped, and the session is terminally REJECTED the same tick under 'split produced no >=70s segment' — with finalize_reject wiping the media. The reject itself is substantively correct (nothing >=70s survives removing both flags), but the stored reasons are all fixable so the reject-reason report degrades to the fix-failed/split marker instead of CNT_SHORT's post_cut_s story, and the player coaching misses the actionable message ('record longer; both clip edges carried overlays') — the exact misdirection class r12 #7 was accepted to fix for single edges, plus one pointless ffmpeg cut per occurrence. Bounded harm: no wrongful reject, no extra paid sweep.

**Finder's proposed fix (vetted into plan §3 — deviations stated there):** In _map_flags (or at the top of plan_fixes' cut assembly), when both a head edge and a tail edge are being planned, also test the joint remainder: min(tail_cut) - max(head_cut) < C.MIN_CLIP_S => emit the map-time CNT_SHORT (blocking, unfixable, post_cut_s=joint remainder) exactly as the single-edge arms do, so the session rejects at once with the truthful reason and no burned attempt. The same composition guard can cover CNT_EDGE_NONGAMEPLAY pairs without touching entry 26's settled claim-based geometry (the check only combines cut points already computed).

### #8 [MINOR] translator/translate.py:68 (translator, refuters 0/2)

**NUL-byte session_id passes safe_session_id and crashes the translate join untyped**

safe_session_id (translator/translate.py:61-72) accepts any str that is not ''/'.', has no '/', '\\', os.sep or '..' — an embedded NUL passes, and " " is legal JSON, so metadata.json {"session_id": "abc def"} reaches the path join. The very next line at every consumer calls out_dir.resolve() in the containment assert (translator/v2.py:319, translator/translate.py:265, tools/fix_actions_from_v2.py:59, tools/fix_sync_from_v1.py:262), which raises ValueError on a NUL component. Proved by execution (/tmp/probe3.py replicating translator/v2.py:316-320 exactly): safe_session_id('abc\x00def', ...) -> 'abc\x00def', then "CRASH: ValueError - lstat: embedded null character in path". mkdir raises the same ValueError, so removing the assert would not save it. This is the residual gap in the r9 (non-str) / F9-r11 (traversal) sanitizer class: garbage ids are DESIGNED to degrade to the bundle-folder-name fallback, not to raise.

**Scenario:** A player-tampered raw bundle (metadata.json with session_id "abc def") is ingested and routed ARR_RAW_ONLY -> fix_translate_raw (pipeline/fix.py:837) -> translate_bundle_v2 raises ValueError; apply_fixes' classifier (pipeline/fix.py:425-427) marks kind="session", burning the attempt; attempt 2 fails identically -> terminal REJECT "fix retries exhausted" carrying "FIX_TRANSLATE_RAW: ValueError: lstat: embedded null character in path" — a parser fault reported to the player as a fault in their footage (the exact r-loop-7-blocker message class), for a session whose good video+inputs would translate fine under the designed folder-name fallback. The same id in a delivered session.json also crashes both G7 operator tools (fix_actions_from_v2, fix_sync_from_v1) mid-batch.

**Finder's proposed fix (vetted into plan §3 — deviations stated there):** In safe_session_id, additionally reject ids containing control characters — e.g. add `and not any(ord(c) < 32 for c in session_id)` (at minimum '\x00' not in session_id) to the accept condition so such ids take the existing bundle-folder-name fallback; covers all five join sites at their shared single decision point.

### #9 [MINOR] tools/analyze_sample.py:1006 (translator, refuters 0/2)

**build_verdict judges clip-short and tail-edge-ness on the CLAIMED duration — the D2/G3 class left unswept in the operator tool**

a.duration_s is the player-supplied session.json claim (line 1345: _num(s.get("duration_seconds"), float, 0.0)). Line 1006 issues the re-record verdict ("clip Xs under the hard 70s delivery minimum — unfixable in post; re-record") from that claim, and lines 827/837 set dur = a.duration_s and at_tail = w["t1"] >= dur - 1.0 while every VLM window's t1 is capped at sweep_dur = min(claim, probed) (lines 1455-1458), so a corrupt-LARGE claim makes every genuine tail window read as MID-CLIP (lines 894-905: "no mid-clip cut tool exists ... or re-record" instead of the tail-edge retrim advice). The probed truth is computed right there (line 1422 probe_streams -> a.video_probe["duration_s"], already the r12 #9 sweep-clamp truth source) but the verdict never uses it. The pipeline was fixed for exactly this class — D2 (entry 26: CNT_SHORT judges the probed duration) and G3 (commit b383f05: notif/chat edge-vs-mid on dur_true) — and the G3 commit's sibling sweep explicitly covered validate.py only, never this file's build_verdict.

**Scenario:** A delivered session whose duration_seconds claim is corrupted small (e.g. 59.9 claimed for a 600s video — the inverse of the observed r12 #9 ms-in-seconds corruption class) is run through the standalone playbook tool: reasons_re gets "clip 59.9s under the hard 70s delivery minimum — unfixable in post; re-record", which dominates the roll-up (lines 1017-1019) -> verdict RE-RECORD, exit code 2, for a session that needs only a session.json rewrite (the qa-v2 duration-mismatch FAIL on the same report proves the file, not the footage, is wrong). The operator, following the recommend-only report, tells a player to re-record good footage — lost hours. Pipeline verdicts are unaffected (validate.map_reasons uses aux["probed_duration_s"]); the harm surface is the standalone report.md/exit code the operator acts on.

**Finder's proposed fix (vetted into plan §3 — deviations stated there):** In analyze(), derive dur_true the way validate does — probed = float(a.video_probe.get("duration_s") or 0); dur_true = probed or a.duration_s — and use dur_true for the line-1006 clip-short gate and for build_verdict's dur/at_head/at_tail tests (pass it in or set a.duration_s to dur_true after the probe), keeping the claim only as the fallback when the probe yields nothing.

### #10 [MAJOR] tools/recal_rebuild_reset.py:243 (ops-tools, refuters 0/2)

**Rebuild reset leaves split manifests + rowless segment dirs; crash triage then adopts the pre-recalibration (VOID) cut, and the dirs leak unreclaimably**

recal_rebuild_reset's teardown (tools/recal_rebuild_reset.py:239-246) wipes only work/<sid> for ROWED sids (line 243: shutil.rmtree(cfg.work/sid)); it never unlinks work/<sid>.split-manifest.json and never removes rowless work/<sid>-p<N> segment dirs — unlike its refix sibling, whose discard_split_artifacts (tools/recal_refix_reset.py:40-62, called at line 331) exists for exactly this class (r-loop 3) and whose docstring states both harms: 'a stale manifest could be adopted as a COMPLETED cut over half-written segments' and 'Segment videos are hundreds of MB and would sit in work/ permanently'. The adoption mechanism is live at HEAD: cutter.py:110-115 writes the manifest BEFORE the caller inserts child rows (run.py:675), so a kill in that seconds-wide window (which spans the tail of apply_fixes — gate-record propagation, fixlog persistence) leaves manifest + all segment dirs + zero child rows; the reset then resets the parent to DISCOVERED, deletes all children, but carries the manifest and dirs through. On the re-run, any FIXING pickup (continuous.py:670-673 -> _fixing_triage:858-891 -> run._recover_split:385-459) finds manifest_ids all present as dirs -> complete=True -> inserts the stale gen-1 segments as INGESTED children and commits the parent SPLIT ('completed after mid-split crash'), skipping the new-rules re-derive entirely. Nothing else reclaims the leftovers: _sweep_terminal_work unlinks a manifest only when the parent is SPLIT/REJECTED/DELIVERED (run.py:1955-1960) — never during the re-run's DISCOVERED..FIXING states — and wipes a work dir only when its sid HAS a ledger row (run.py:1962-2050), which a deleted child's -pN dir never does again. PROVEN BY EXECUTION (scratch HL_PIPELINE_HOME, repo read-only): running the actual HEAD tool with --yes printed 'root state after reset: DISCOVERED / work/<root> wiped: True / MANIFEST STILL PRESENT: True / SEGMENT …

**Scenario:** The driver is SIGKILLed/OOM-killed (the design explicitly embraces kill-at-any-instant) between the cutter's manifest write and the first child insert — parent FIXING, manifest + all gen-1 segment dirs on disk, zero child rows. The operator runs recal_rebuild_reset --yes (a standing tool per G5's own commit message, and a rebuild rerun is an anticipated operation — rebuild-sessions-2026-08-18.csv was captured 'for possible rerun'). The root resets to DISCOVERED but the gen-1 manifest and segment dirs survive. The root re-runs under the NEW recalibrated rules; when its fix attempt is in FIXING at any routine driver restart (every rsync deploy restarts hl-continuous; Restart=always) or on any runner exception, the next pickup adopts the stale gen-1 cut: the parent commits SPLIT with the pre-recalibration cut geometry — the exact VOID black-frozen-era judgment (122/122 false positives) the rebuild was ordered to erase — so footage the old cut dropped (inter-window gaps, sub-70s tails) is silently never delivered or paid, the new-rules whole-clip judgment (R1-R3 keep-instead-of-split) …

**Finder's proposed fix (vetted into plan §3 — deviations stated there):** Mirror the refix sibling: in the teardown loop, for every sid in all_sids call the discard logic recal_refix_reset already carries — unlink work/<sid>.split-manifest.json and rmtree every rowless work/<sid>-p<digits> dir (the -p\d+ fullmatch shape from refix, so unrelated prefixes are not eaten) — and, while there, also wipe work/<sid>-analysis as refix does (line 330). Simplest form: lift discard_split_artifacts into a shared location (or duplicate the 6 lines) and call it beside the existing shutil.rmtree(wd) at tools/recal_rebuild_reset.py:243. Pin with a test in the G5 group: reset over a manifest+dirs/no-rows state must leave neither the manifest nor the segment dirs, and a subsequent _recover_split on the re-run root must return complete=False.

### #11 [MAJOR] tools/recal_rebuild_reset.py:173 (tests-coverage, refuters 0/2)

**G5's tree-root paid-piece keying (the parent-chain walk) survives deletion with the 722-test arming gate green; the undetected regression is a silent double-pay**

The G5 payment-surface fix records each accepted-stamped DELIVERED node 'keyed to its TREE root (parent-chain walk — -p1-p1 nesting makes the immediate parent the wrong key)' (commit dc97f9d's own words; tools/recal_rebuild_reset.py:160-173). No test exercises depth ≥ 2: the only cohort (test_r_loop13.py _stamped_cohort, line 543) is root → kid, where immediate parent == tree root. Mutation proven in a scratch copy at /tmp/mut14 (never touching the repo): replacing `_tree_root(n["session_id"])` at line 173 with `parent_of.get(n["session_id"]) or n["session_id"]` ran the FULL suite green — `722 passed in 133.92s` (baseline also 722). A depth-2 probe (root SPLIT → root-p1 SPLIT → root-p1-p1 DELIVERED with accepted_reported_at; real tool run with --yes --allow-reported) prints on HEAD: `paid_pieces keyed under ROOT: {'…gg5-p1-p1': 900.0}` and after a deterministic re-cut `g5 sheet rows: []` (correct skip). On the suite-green mutant: `paid_pieces keyed under ROOT: {}`, `paid_pieces keyed under P1: {'…gg5-p1-p1': 900.0}` (keyed to a node the same run then DELETEs), and the rebuilt sheet emits `{"player_email": "g5@x.com", "kamla_accepted_hrs": 0.25, …}` — RESULT: DOUBLE-PAY, with no ORPHANED/AMBIGUOUS line, because reports.build_sheet_rows loads paid_pieces keyed by root_id (reports.py:729) and a row keyed to a non-root id is invisible to both _mem_reconcile_failures and _tree_has_uncounted_accepted forever.

**Scenario:** Split cascades produce -p1-p1 DELIVERED nodes in the real ledger (entry 35's premise; test_split_cascade_r1r3 pins the cascade). An executor 'simplification' or refactor of the walk to the immediate parent — exactly the shape the commit warns against — passes the flip's arming gate (tools/run_suite.sh, floor 718) on both hosts. The next recal_rebuild_reset --allow-reported over a tree holding an accepted-stamped grandchild then records the payment memory under a deleted child id; after the rebuild, the re-cut re-creates the same node ids and the next daily sheet pays the same 900s of footage a second time, silently — the ruled unrecoverable direction — while the memory row sits orphaned under a key no sheet ever consults.

**Finder's proposed fix (vetted into plan §3 — deviations stated there):** Add a depth-2 variant of test_rebuild_reset_allow_reported_preserves_and_records to pipeline/tests/test_r_loop13.py: cohort root(SPLIT) → root-p1(SPLIT, parent=root) → root-p1-p1(DELIVERED, delivered=900.0, accepted_reported_at set); after reset.main()==0 assert led.paid_pieces_for(root) == {root+'-p1-p1': 900.0} AND led.paid_pieces_for(root+'-p1') == {}; then re-put the same two child rows, set root SPLIT, build_sheet_rows over the window and assert no g5 row (my probe /tmp/probe_g5_depth2.py is effectively this test body — it passes on HEAD and fails on the mutant).

### #12 [MAJOR] translator/tests/test_r_loop13_translator.py:62 (tests-coverage, refuters 0/2)

**G4's _verify_against_raw OverflowError guard (site 4, both halves) is deletable with the suite green — its dedicated test never invokes _verify_against_raw at all (raw_bundle is never passed)**

test_bigint_duration_with_raw_sidecars_skips_the_raw_recheck (line 62) claims to pin _verify_against_raw's degrade-to-warn-skip, but check_session_v2 only calls _verify_against_raw when the caller passes raw_bundle (translator/v2.py:648 signature, :932 `if raw_bundle is not None`) — there is NO auto-detection of session_dir/raw — and the test calls `check_session_v2(d)` bare. Spy probe (wrapping v2._verify_against_raw, exact test recipe): `A: test recipe -> status FAIL | _verify_against_raw calls: 0` — the FAIL comes from sites 1/3 (_check_session_json + the video-duration compare), which the parametrized test already covers. Mutation proven in the scratch copy: reverting BOTH site-4 arms (OverflowError removed from the except tuple at v2.py:997, and the per-event `int(e["t"] - head_us)` try/except at v2.py:1037 unwrapped) ran the FULL suite green — `722 passed in 133.73s`. On that suite-green mutant the PRODUCTION call shape crashes: `check_session_v2(d, raw_bundle=d/"raw")` with duration_seconds=10**400 → `CHECKER CRASHED OverflowError: int too large to convert to float`; on HEAD the same call degrades: `WARN: raw verification skipped: raw metadata/session timestamps unreadable (OverflowError)`. Production reaches this path: pipeline/validate.py:862-877 sets raw_by_sid[work_dir.name]=work/raw whenever sidecars exist, and tools/analyze_sample.py:1350 passes `raw_bundle=raw` into check_session_v2 — line 1350 is unguarded, so the OverflowError escapes analyze() → validate_session.

**Scenario:** Any raw-capture session (sidecars present — the normal case, and the precondition for the FIX_RETRANSLATE repair route) whose player-typed session.json carries a bigint/1e999-class duration_seconds. If a later change regresses the site-4 guard — including re-narrowing that except tuple during any refactor — the arming gate stays green on both hosts, and in production the checker raises OverflowError out of eng.analyze(); validate_session's host split doesn't apply (not OSError), so the session goes terminally QUARANTINED 'validation crashed' instead of taking the typed fixable route: the exact wrongful-terminal-reject harm r13 #6 was rated MAJOR for, now invisible to the suite. The test meanwhile keeps passing, asserting a claim it never tests.

**Finder's proposed fix (vetted into plan §3 — deviations stated there):** De-vacuous the test: call `check_session_v2(d, raw_bundle=d / "raw")` and assert the specific degrade line ('raw verification skipped' with 'OverflowError') is present in r.issues alongside status FAIL. Add a sibling for the per-event arm: duration_seconds=float('inf') (json 1e999 class) + one bigint-t mouse_raw event in raw/inputs.jsonl, assert check_session_v2(d, raw_bundle=d/'raw') returns a verdict without raising (my probes /tmp/probe_g4_final.py and /tmp/probe_g4_event.py demonstrate both directions: HEAD degrades, guard-reverted tree crashes).

### #13 [MINOR] tools/fix_sync_from_v1.py:177 (tests-coverage, refuters 0/2)

**G7's second half — fix_sync_from_v1's session_id sanitize + containment assert — has no test anywhere; the full revert survives the suite green**

Commit 623ab8a fixed the identical unsanitized path join in TWO tools but only tested one: test_fix_actions_tool_contains_a_traversal_session_id covers tools/fix_actions_from_v2.py, while tools/fix_sync_from_v1.py:177 (`session_id = safe_session_id(s.get("session_id"), v2_dir)`) and its :262 containment assert have zero coverage. Proof: `grep -rn "fix_sync" pipeline/tests translator/tests` → no matches (exit 1); the tool-loader helper `_load` is invoked for exactly four tool names (fix_actions_from_v2, recal_rebuild_reset, recal_refix_reset, recal_regen_sheets) — fix_sync_from_v1 is imported by nothing in the suite. Mutation proven in the scratch copy: reverting the hunk to the pre-G7 `session_id = s["session_id"]` and deleting the assert ran the full suite green — `722 passed in 134.27s` (run also carried the independent v2.py site-4 mutation; no masking is possible since no test imports this module, so any failure in that run would have implicated the other mutation — and the module-level grep independently proves invisibility). This is the exact class r13 #10 flagged last iteration (a two-site fix with only one site pinned), recurring in the very fix set that closed it.

**Scenario:** A regression of this hunk — e.g. reverting to the raw `s["session_id"]` read during a refactor of the tool's fix_session — passes the arming gate. An operator later runs fix_sync_from_v1 over a delivered session whose player-typed session.json carries session_id '../../../../X' (the F9 traversal class r13 #7 confirmed as reachable), and the tool writes the full delivery file set outside out_root — clobbering sibling paths and producing a delivery that never lands in the expected VENDOR/date/slug tree, with the arming gate unable to catch the regression.

**Finder's proposed fix (vetted into plan §3 — deviations stated there):** Add a fix_sync_from_v1 twin of test_fix_actions_tool_contains_a_traversal_session_id in pipeline/tests/test_r_loop13.py: _load('fix_sync_from_v1'), build a minimal v1+v2 session pair whose v2 session.json carries session_id '../../../../ESCAPED', run fix_session(v1_dir, v2_dir, out_root, date), and assert the resolved out_dir is_relative_to(out_root), res falls back to the dir-name id, and no ESCAPED dir exists outside out_root (mirroring lines 323-349 of the existing twin test).
