# R17_FINDINGS.md — review iteration 17, findings of record

**Run 2026-08-19** by the I/J executor session via the workflow snapshot
`tools/review/flip-review-iter17.js` (FIX-IN-ITERATION pass per
R8_IMPLEMENTATION_PLAN §4, RULED 2026-08-19; iteration 18 — the last
before the checkpoint — reviews these fixes; the stream stops at the
first quiet iteration). 7 finder lanes, 2-vote refute, 19 agents (19
completed, 0 errors, ~2.58M subagent tokens, run `wf_7f33bc0c-52c`).
HEAD `7ad7b71` (code HEAD `ddc6da8` = J1–J6 + floor 778 `cba8fd2`;
suite 782/782 both hosts).

**Verdict: NOT QUIET pre-fix — 6 raised → 6 confirmed
(4 major / 2 minor, 0 blockers), 0 killed.** All 0/2
refuted.

**Clusters/classes:** #1 is a regression-interaction of the H5 arm with
the pre-existing guard-less quarantined-path heal (cross-player
takeover; the r5 #41 refusal bypassed in two scans). #2 is the F4
doctrine's fourth instance (fix_v1_to_v2 the last unswept sibling). #3
re-opens the r12 #6 cure-later-in-the-same-plan shape at an unguarded
`_active`. #4/#5/#6 are tests-coverage pins for J2/J3/I7 halves that
survive mutation with the arming gate green (the r15 #6 cohort class,
recurring again).

Machine-readable results incl. both refuter verdicts: session
scratchpad `r17-results.json`.

## Confirmed findings (severity-ordered)

### #1 [MAJOR] pipeline/ingest.py:274 (driver-core, refuters 0/2)

**H5 vanished arm launders identity collisions through the guard-less quarantined-path heal — cross-player session takeover in two scans**

The QUARANTINED-path heal (ingest.py:274-366) re-registers a known sid at a new path with NO identity guard: it unconditionally rewrites player_email/operator_email/md5_video to the new folder's values (ingest.py:331-340). Its designed input population was INT_PATH rows (malformed paths, inserted with player_email="" at ingest.py:241-244), where no guard is possible. The H5 vanished arm (ingest.py:717-728, commit c731e32; coaching text I7 f3f131e) feeds it a NEW population: healthy DISCOVERED rows whose folder left the listing. The pre-existing identity-collision refusal for DISCOVERED rows (ingest.py:408-414, guarded by review-r5 #41's move-heal test at ingest.py:367-372: md5 match when both known, else player unchanged) is thereby bypassed — the very move that creates the collision (folder moved into another player's tree, original path gone) also triggers the vanished arm, which converts the refused DISCOVERED row into a QUARANTINED row the heal accepts one scan later. Proven by execution against HEAD (scratchpad/probe_heal_launder.py): scan 1 registers zip session S under playerA; scan 2 with S at playerB's path prints the collision flag ('ignored + flagged') AND quarantines the row via the vanished arm ([vanished-discovered] line); scan 3 prints 'quarantined path healed — re-registered' and led.get(S) shows state=DISCOVERED, drive_path=playerB's path, player_email=playerB@example.com. Control run of the identical probe against the pre-H5 tree (git archive c731e32^): the row stays DISCOVERED under playerA with the collision flag re-printed every scan, forever — the refusal held. No test pins cross-player quarantine-healing as intended; the pinned tests pin the REFUSAL (test_scan_copy_at_both_paths_stays_collision, test_scan_move_heal_refuses_md5_mismatch). The entry-70 ruling blesses different-path re-registration only for the same uploader's rename re-upload and the same-path dead end; it does not overturn the r5 #41 cross-player guard, so this is outside the ruled dead end.

**Scenario:** A zip-payload session (no Drive-side video md5 — the class review-r5 #41 explicitly refused to re-attribute without byte identity) is uploaded by player A. The folder is then moved into player B's tree — by a colluding pair splitting identities for payment, or by an operator mis-filing a session during routine free-text folder cleanup. Scan N flags the collision and quarantines the dead-path row; scan N+1 (~5 min later) heals it under player B; the session downloads, validates, delivers, and its duration_delivered_s lands under player B's player_email in player_rollup — player B is paid for player A's footage (or for unverifiable bytes nobody can tie to the sid's original registration). The same two-scan path works for a files payload carrying a DIFFERENT md5 — arbitrary new bytes capture a registered sid (and the heal then clears the old registration's payment stamps as 'genuinely new hours'). The takeover is invisible to the operator at the flip: the collision flag and the heal line are stderr prints only (continuous.py:349), scan integrity flags reach no Telegram surface (the digest has no integrity line; the daily message carries only INT_DUP_CROSS reject counts, run.py:896-898, and CONT_DAILY_REPORTS ships False at the flip), and after the heal the collision flag stops printing entirely — the only lasting trace is the events audit.

**Finder's proposed fix (vetting in-iteration per the ruling):** Gate the QUARANTINED-path heal with the same identity test the move-heal uses, for rows that have a real prior registration. Discriminator already in the schema: INT_PATH chase rows are inserted with player_email="" (ingest.py:242), while vanished-arm and post-download quarantines keep the genuine registration. In the heal branch (ingest.py:274), when existing["player_email"] is non-empty, require ((vmd5 == existing["md5_video"]) if (vmd5 and existing["md5_video"]) else existing["player_email"] == ds.player_email); on failure, keep the row QUARANTINED and append the existing session-id-collision integrity flag instead of healing. This preserves every designed flow — malformed-path heals (empty player_email) stay unguarded, same-player re-uploads at a new path still heal, byte-identical cross-player moves still heal exactly like the move-heal — and restores the r5 #41 refusal, with its coached remedy (re-upload under a NEW folder name, which mints a new sid) unchanged and consistent with the entry-70 ruling. Add a test twin of test_scan_copy_at_both_paths_stays_collision that routes the move through the vanished-arm quarantine and asserts the sid never flips player.

### #2 [MAJOR] pipeline/fix.py:1524 (translator, refuters 0/2)

**FIX_V1_TO_V2 silently deletes custom-bound key presses — bound is judged against the built-in keybind only, shipping orphan actions checker-green**

fix_v1_to_v2 builds its keybind from built-ins alone (fix.py:1507-1510: kb = dict(KEYBINDS.get(slug, {})) + KEYBIND_PATCHES) and keeps a key token only if key_canonical(t) is in that bound set (fix.py:1524-1525), while carrying the v1-resolved input_actions verbatim (fix.py:1548). It never consults the session's OWN keybind.json — which is sitting at the work root at that moment (the function itself moves it into raw/ only later, at lines 1573-1577) — nor the delivered key_binding.json (whose keys ARE the bound literals, and whose inverted form resolve_keybind already sniffs). Ran it (probe in scratchpad, real ffmpeg video, production call shape fix_v1_to_v2(work, "kamla")): a v1 session whose player rebound interact to ';' (session keybind {"interact": ";", "move_up": "w"}) came out with 0 rows carrying ';' (all 31 presses deleted, counted nowhere), 31 rows carrying action 'interact' with EMPTY input_keys, 'w' kept (built-in-bound), and check_session_v2 returned WARN-only — no FAIL fires because the checker tests keys-without-actions (keys_no_action), never actions-without-keys. This is the exact defect class r-loop 11 F4 confirmed and fixed in fix_key_hygiene ('judging bound against the built-ins alone made the r10 unbound strip delete every custom-bound key press — silent delivered-data corruption that then passed the checker cleanly'), whose sweep (r12 #5/#8 fix_actions_context + operator twin, r14 H2 retranslate) covered every fix-family sibling EXCEPT this one. Accepted entries 44/72 rule only the button canonicalize-or-drop contract and the keep-v1-actions-verbatim choice — neither rules the built-ins-only bound test for keys.

**Scenario:** A rebind player — the population r15 #4 (';') and r16 #5 (',') established as real — uploads a v1-format delivery (key_binding.json present => payload 'v1' => typed ARR_V1_FORMAT, bin 2 => plan [FIX_V1_TO_V2]). _dispatch passes the ledger game (always in C.GAMES), so built-ins are always non-empty and the strip is always armed. Every press of the custom-bound key is deleted from the delivered frames.csv while its resolved action ships on the same rows with an empty input_keys cell; revalidation passes (WARN-only, proven), the row goes READY and the corrupted CSV is DELIVERED to Odyssey — keystroke evidence destroyed, phantom actions with no key, invisible to qa-v2, to the raw dx/dy recheck (mouse-only) and to every report. No retranslate is ever planned even though inputs.jsonl sits in raw/ — the silent pass is what prevents the recovery the pipeline could do. (If a later plan ever runs FIX_KEY_HYGIENE, its session-keybind re-resolve erases the orphan actions too — the data is destroyed either way.)

**Finder's proposed fix (vetting in-iteration per the ruling):** Mirror the F4 doctrine in fix_v1_to_v2: resolve the session's own keybind before computing bound — kbp = work/'keybind.json' (it is still at the root at that point; also accept work/'raw'/'keybind.json' and, failing those, the delivered key_binding.json via resolve_keybind's inverted-direction handling), kb = resolve_keybind(keybind_path=kbp, game_name=<ledger slug>, exe_name=None) when the file exists, else the built-ins; then kb.update(KEYBIND_PATCHES.get(slug, {})) and judge the line-1524 strip against bound_literals(kb). resolve_keybind's parsed-but-unusable fallback already lands on the right built-in. Pin with a twin of the F4 test: custom ';'/',' v1 bind survives conversion with its action, built-in-only session unchanged.

### #3 [MAJOR] translator/v2.py:151 (translator, refuters 0/2)

**apply_context_to_rows' _active crashes ValueError on a malformed dx/dy cell — FIX_ACTIONS_CONTEXT burns both attempts while FIX_SENTINELS, the cure, sits later in the same plan**

v2.py:149-151 _active does a bare float(v) on delivered CSV cells ('Mouse delta counts as motion: not blank and not (float) zero') and is called for EVERY row. Ran it: apply_context_to_rows on one row whose dx cell is '1,5' (the r-loop-3 STR_SENTINELS population: locale decimals, 'abc', stringified None) raises 'ValueError: could not convert string to float: 1,5'. Both fix-lane callers hand it raw delivered cells: pipeline/fix.py fix_actions_context (line 1182) and tools/fix_actions_from_v2.py (line 102). pipeline/fix.py's OWN hygiene has the guarded sibling _moving (fix.py:1114-1120, review-2 #7: try/except ValueError -> False) for these exact cells, and fix_sentinels (fix.py:1359-1366) degrades them — but plan_fixes structurally orders FIX_ACTIONS_CONTEXT (appended at fix.py:328-330) BEFORE the rank-2 csv loop that emits FIX_SENTINELS (fix.py:333-339). Ran plan_fixes: [INP_FANOUT + STR_SENTINELS, game=outer_wilds, has_raw=False] -> ['FIX_ACTIONS_CONTEXT', 'FIX_SENTINELS', 'FIX_SESSIONJSON_RECOMPUTE']; the hygiene-triggered variant [INP_TOKEN_CASE + STR_SENTINELS] -> ['FIX_KEY_HYGIENE', 'FIX_ACTIONS_CONTEXT', 'FIX_SENTINELS', ...] (hygiene never touches dx/dy, so the poison cell survives into the context step). apply_fixes classifies ValueError as session-kind (fix.py:425-427 — not in the host tuple, so no refund) and breaks before FIX_SENTINELS ever runs; the crash happens before fix_actions_context's _write_csv, so revalidation reproduces the identical reasons (in the hygiene variant, hygiene's context-free re-resolve re-fans-out OW keys, so INP_FANOUT re-fires into the same [CONTEXT, SENTINELS] order) and attempt 2 crashes identically. This is the r12 #6 shape — both attempts burned on a FixFailed-class error 'whose cure was one step later in the same plan' — re-opened at a call site that pass missed.

**Scenario:** A no-sidecar Outer Wilds v2 delivery from foreign/old tooling — exactly the population that both fans out multi-bound actions (no context gating) and writes non-conformant dx cells (the checker FAILs it as 'not float-formatted' -> STR_SENTINELS, blocking+fixable; with no raw/ dir the retranslate supersede never triggers). map_reasons emits INP_FANOUT (or any hygiene trigger: INP_TOKEN_CASE/INP_OSKEYS/INP_BLEED — on OW hygiene always appends FIX_ACTIONS_CONTEXT after itself, fix.py:328). Attempt 1: paid VLM sweep + classify_video scan, then ValueError inside apply_context_to_rows -> session-kind fix failure. Attempt 2: same plan, same crash. Session REJECTED 'fix retries exhausted' — a wrongful terminal reject and an unpaid player, plus two paid Gemini sweeps, for a session whose complete repair (FIX_SENTINELS then context) was already in the plan. The same ValueError kills the operator tool tools/fix_actions_from_v2.py mid-batch.

**Finder's proposed fix (vetting in-iteration per the ruling):** Guard _active in translator/v2.py exactly like fix.py's _moving (review-2 #7's sibling): try: return v not in ('', None) and float(v) != 0.0; except (TypeError, ValueError): return False — a junk cell is not motion, matching _num_cell/fix_sentinels semantics. Belt-and-braces (the r12 #6 pattern): also emit FIX_SENTINELS with the pre-structural fixes so cell repair precedes every reader of dx/dy (note sync.input_track_from_rows' bare float() at sync.py:156-158 is today shielded only by reasons ordering — STR_SENTINELS maps before the sync FAIL in issue order — not by design). Pin with a plan-order test plus a context-fix run over a '1,5' cell.

### #4 [MAJOR] pipeline/tests/test_r_loop16.py:33 (tests-coverage, refuters 0/2)

**J2's INP_OSKEYS cohort pins key_canonical only where it is a no-op — a case-normalizer regression at validate.py:762 passes the full arming gate while restoring the r16 #3 wrongful terminal reject for every multi-word bound OS key (caps_lock/num_lock/print_screen)**

The J2 filter (c0d37de) at pipeline/validate.py:762 judges `key_canonical(t) not in bound`. Every bound token in the whole J2 cohort is 'Insert'/'insert' (test_r_loop16.py:40, :51, :60, :91-96, e2e :129-131), and key_canonical('Insert') == 'insert' == 'Insert'.lower() — so the canonical mapping is a no-op for the entire cohort (the r15 #6 class my lane brief names). Proven by execution in a scratch clone at HEAD: mutating line 762 to `t.lower() not in bound` passes the FULL runbook arming gate — '782 passed in 148.21s / ARMING GATE OK: 782 passed (floor 778)', byte-identical summary to my baseline run (782/778). But the delivered CSV token for a bound caps_lock is camel: key_display('caps_lock') == 'CapsLock' (v2.py:117 capitalizes each '_' part), the engine's inventory keys os_keys by that CSV token (tools/analyze_sample.py:236-237, pattern caps.?lock re.I matches), and 'CapsLock'.lower() == 'capslock' != 'caps_lock'. Probe on both trees: HEAD gives reasons [] + advisory "OS-pattern keys BOUND ... {'CapsLock': 40}"; the suite-green mutant gives reasons ['INP_OSKEYS'] (blocking) while normalize_event_key('caps_lock', bound={'caps_lock'}) still returns 'caps_lock' — the planned FIX_KEY_HYGIENE is a provable no-op, the exact loop J2 was landed to stop. The commit message's kill claim ('the display-vs-canonical mutant is killed by the refuse side') is true only for the identity mutant `t not in bound`; the refuse side cannot distinguish key_canonical from str.lower because its only bound token is single-word.

**Scenario:** Iteration-18-or-later executor inlines or 'simplifies' the one-expression filter to a lowercase compare (the same one-line-anchor class as r15 #6, which recurred inside the fix set that closed it). Both host gates stay green 782/778 and the flip arms. In production, every session binding caps_lock, num_lock, or print_screen — the fix's own named rebind population ('insert, caps_lock, an F-key: real rebind targets', c0d37de) — takes blocking INP_OSKEYS, FIX_KEY_HYGIENE keeps the bound key per the locked strip-unless-bound rule, the reason re-fires on both attempts, and a spec-conformant delivery is terminally rejected 'fix retries exhausted' with three paid sweeps burned — the exact r16 #3 MAJOR harm, restored invisibly to the arming gate.

**Finder's proposed fix (vetting in-iteration per the ruling):** Add a camel discriminator to the map-level cohort in pipeline/tests/test_r_loop16.py — e.g. `_os_map({"CapsLock": 3}, frozenset({"caps_lock"}))` asserting no INP_OSKEYS + the BOUND advisory (this kills t.lower(), identity, and every non-canonical transform; my probe output is the fail-first evidence) — and bind caps_lock instead of (or beside) insert in test_oskeys_trigger_bound_aware_end_to_end so the canonicalization is also pinned where the validate_session wiring is live (the r15 #6 lesson the e2e itself cites).

### #5 [MINOR] pipeline/tests/test_r_loop16.py:263 (tests-coverage, refuters 0/2)

**The J3 pin ports the I2 cohort to the fix_sync_from_v1 remap site without its overlap case — a row-level rewrite of the credited strip at fix_sync_from_v1.py:183 passes the full arming gate while shipping uncredited combo halves beside credited keys**

test_fix_sync_remap_strips_uncredited_combo_half (J3, c0d37de) drives remap with three DISJOINT frames: ['e'] alone, ['ctrl','e'], ['w'] alone. The I2 cohort at the sibling _v2_rows site deliberately includes OVERLAP frames — _combo_bundle holds w 0.10-0.90s across the bare-e press 0.20-0.35s (translator/tests/test_r_loop15_translator.py:87-91) and asserts 'a kept E must ride with its modifier (bare halves strip)' (:111-113), which kills a row-level mutant there. The J3 port drops exactly that case. Proven by execution in a scratch clone at HEAD: rewriting tools/fix_sync_from_v1.py:176-183 to the row-level reading `if rules and not actions: kset = set()` passes the J3 pin AND the FULL arming gate — '782 passed in 148.89s / ARMING GATE OK: 782 passed (floor 778)'. Probe with rules {interact: ctrl+e, move_up: w}, frame ['w','e']: HEAD ships keys ['w'] actions ['move_up']; the suite-green mutant ships keys ['e','w'] actions ['move_up'] — an uncredited combo half rides masked behind a credited key, violating the I2 RULED contract ('a kept key token must be credited by a rule whose FULL group set was satisfied and fired', entry 72, whose reserved defect class includes 'the credit accounting diverging from rule satisfaction'). The checker cannot flag it: its keys-have-actions test is row-level and actions is non-null.

**Scenario:** A later edit restates the strip as the row-level invariant the adjacent comment itself uses ('must not ship with null actions') — the natural re-implementation, and indistinguishable from HEAD for every frame the pin exercises. The gate stays green 782/778. Every fix_sync_from_v1 repair batch (the operator tool for v1-era sync deliveries) then ships frames where a combo half held across another key's press is recorded as a keypress with that other key's action — silent per-token key-to-action mislabeling in delivered training data that no checker or QA surface can see, the same silent-delivered-corruption class as r15 #6, bounded to this tool's output.

**Finder's proposed fix (vetting in-iteration per the ruling):** Extend the J3 pin's input with the I2 cohort's overlap case: a fourth frame ['w','e'] (same rules, same call) asserting keys == ['w'] and actions == ['move_up'] — mirrors translator/tests/test_r_loop15_translator.py:111-113 at the fourth writer site and fails the row-level mutant (probe output above is the fail-first evidence) while passing HEAD.

### #6 [MINOR] pipeline/tests/test_r_loop15.py:213 (tests-coverage, refuters 0/2)

**The I7 ruling's coached correction path — a renamed re-upload the dead QUARANTINED row must not block — exists only as docstring prose: deleting the QUARANTINED exclusion at ingest.py:511 passes the full arming gate while the coached re-upload terminally parks as DUPLICATE**

The gone-is-gone ruling (entry 70, I7 f3f131e) is load-bearing on 'BOTH dedupe sites exclude QUARANTINED rows, so the dead row never blocks or dup-rejects the renamed copy' — the quarantine detail coaches exactly that correction ('re-upload under a NEW folder name to re-enter'). That exclusion was verified by inspection only: the r15 tests pin same-path terminality (test_r_loop15.py:202) and the coaching STRING (:241), and the sentence at :213 is docstring prose — no test ever scans a renamed copy against a quarantined row sharing its md5, and no INT_DUP_CROSS test involves a QUARANTINED row (grep: the r6-minors/ingest dup tests all use REJECTED/live rows). Proven by execution in a scratch clone at HEAD: deleting `and r["state"] not in ("QUARANTINED",)` from the scan-time dedupe (pipeline/ingest.py:511) passes the FULL arming gate — '782 passed in 148.14s / ARMING GATE OK: 782 passed (floor 778)', 0 failures. Probe (vanished-folder quarantine via the H5 arm, then a new sid at a new path, md5 'h5-md5', same player): HEAD -> renamed copy DISCOVERED, duplicates []; suite-green mutant -> state DUPLICATE, event detail 'same-player duplicate of 2026-08-14T10-00-00Z_kamla_c_00000000000000a5', discovered []. The same-player arm fires BEFORE the adjudicated-loser strip (ingest.py:515-524 precedes the REJECTED/DUPLICATE filter at :530-531), so the dead row terminally parks the correction. This does not re-litigate the ruled dead end — entry 70 explicitly reserves 'a renamed re-upload that the dead row DOES block' as a finding; this is its tests-coverage form: the ruling's precondition is arming-gate-invisible.

**Scenario:** A refactor harmonizes the three by_md5 filter tuples (the download-time twin at ingest.py:959-961 excludes QUARANTINED plus REJECTED and more — the inconsistent-looking tuples invite 'cleanup'), or tightens dup detection the way line 271's comment urges for quarantined-slot copies. Both host gates stay green and the flip arms. Every player who follows the pipeline's own printed coaching after a vanished folder — the r15 #1≡#2≡#3≡#10 cluster's real population, found independently by four lanes — gets the re-upload silently parked as DUPLICATE of a row that can never deliver: recorded hours lost with no operator surface (a DUPLICATE row raises no alert, no reject reason reaches the player), while the quarantine detail keeps promising that exact correction works.

**Finder's proposed fix (vetting in-iteration per the ruling):** Pin the coached path end-to-end in pipeline/tests/test_r_loop15.py beside the same-path pin: reuse _h5_discovered + the vanish scan, then scan a NEW sid at a different path with md5 'h5-md5' and the same player; assert the row lands DISCOVERED, res.duplicates == [] and no INT_DUP_CROSS/DUPLICATE anywhere (my probe is the fail-first evidence against the mutant). Add the download-time twin (ingest.py:959-961) if cheap — both sites are named by the ruling.
