# R18_FINDINGS.md — review iteration 18, findings of record

**Run 2026-08-19** by the K executor session via the workflow snapshot
`tools/review/flip-review-iter18.js` (the LAST pass before the CHECKPOINT
per R8_IMPLEMENTATION_PLAN §0/§4, RULED 2026-08-19; its confirmed set is
fixed in-iteration with full §2 discipline and lands UNREVIEWED —
reported honestly as landed-but-unreviewed at the checkpoint). 7 finder
lanes, 2-vote refute, 19 agents (19 completed, 0 errors, ~2.71M subagent
tokens, run `wf_e3359c57-a84`). HEAD `324ac8b` (code HEAD `cdd03cc` =
K1–K6 + floor 789 `6f97449`; suite 793/793 both hosts).

**Verdict: NOT QUIET pre-fix — 6 raised → 6 confirmed
(6 major, 0 blockers), 0 killed.** All 0/2 refuted.

**Clusters/classes:** #1≡#2≡#3≡#5 are ONE defect found independently by
four lanes — fix_v1_to_v2's bare float on v1 dx/dy cells, the class the
K3 sweep NOTED-not-settled at entry 82 and this pass was invited to
prove (it did, by execution: a crash-reject whose cure no other plan
step can reach, since the checker early-returns on key_binding.json
before the CSV is ever scanned). #4 is pre-existing (the r-loop-7
'sidecars made it worse' shape left open for present-but-unparseable
metadata). #6 is the r15 #6 no-op-cohort class recurring INSIDE the K
set: K2's keybind-fallback anchor is arming-gate-invisible.

Machine-readable results incl. both refuter verdicts: session
scratchpad `r18-results.json`.

## Confirmed findings (severity-ordered)

### #1 [MAJOR] pipeline/fix.py:1563 (regressions-r17, refuters 0/2)

**fix_v1_to_v2's bare float(dx or 0) turns one junk motion cell into a terminal wrongful reject on the ARR_V1_FORMAT route — a plan no other step can rescue**

Entry 82 leaves this NOTED-not-settled item to this iteration with 'if you can PROVE a concrete harm path, report it' — proven here by execution. pipeline/fix.py:1537-1539 computes has_motion with `r[col["input_mouse_dx"]] not in ("", "0")`, so any non-float cell (the STR_SENTINELS population: locale '1,5', 'None', 'abc') counts as motion; lines 1563-1564 then run `dx = f"{float(dx or 0):.1f}"` on every row. Probe run (scratchpad probe_v1_float.py against the repo at HEAD): direct call → `RAISED: ValueError: could not convert string to float: '1,5'` at pipeline/fix.py:1563, including when the junk cell is the ONLY non-zero motion value. Unlike the sibling K3 fixed (r17 #3), no sentinel repair can ever precede this step: for a v1 payload check_session_v2 early-returns at translator/v2.py:702-705 on 'key_binding.json present' before the CSV is ever scanned, so STR_SENTINELS cannot be emitted — proven: `plan_fixes([ARR_V1_FORMAT], game='kamla', has_raw=False)` returns exactly {'steps': [('FIX_V1_TO_V2', {}), ('FIX_SESSIONJSON_RECOMPUTE', {})], 'unfixable': []}. apply_fixes over that exact plan (probe 3) returned error "FIX_V1_TO_V2: ValueError: could not convert string to float: '1,5'", kind='session' (pipeline/fix.py:425-427 — attempt burned, no host refund), zero steps applied, and the work dir byte-identical (frames.csv still v1, key_binding.json still present, sniff_payload still 'v1'), so revalidation re-derives the identical verdict and plan. The numeric control ('1.5') passes the crash point. K3's own sweep (commit 22614ef) names this site as 'same class on the ARR_V1_FORMAT route'; K2 (bd94829) touched this same function without closing it.

**Scenario:** An operator re-uploads an old v1-format delivery — the exact population the ARR_V1_FORMAT/FIX_V1_TO_V2 route was built for — whose frames.csv carries one malformed dx/dy cell (a locale-decimal '1,5' from an Excel/foreign-tool round-trip; r17 #3 CONFIRMED this population arriving in delivered CSVs one iteration ago). Validation FAILs only 'key_binding.json present' (checker early-return), maps to ARR_V1_FORMAT, and plans [FIX_V1_TO_V2, FIX_SESSIONJSON_RECOMPUTE]. Attempt 1: fix_v1_to_v2 crashes ValueError at fix.py:1563 before writing anything; apply_fixes classifies it session-kind (no refund) and the row goes REVALIDATING (run.py:661-663). Revalidation of the untouched dir reproduces ARR_V1_FORMAT identically; attempt 2 crashes identically; run.py:573-575 then sets REJECTED 'fix retries exhausted (R2)'. A repairable session — a non-crashing conversion followed by the existing FIX_SENTINELS on revalidation clears it — is terminally rejected, the player goes unpaid, and ops sees only the bare fix-failed marker ('could not convert string to float'), misdirecting coaching toward the player's footage. The same ValueError also aborts a re-run of the conversion on any re-entrant path.

**Finder's proposed fix (vetting in-iteration per the ruling):** Give fix_v1_to_v2's motion formatting the same degrade-never-crash rule K3 gave _active (and _num_cell/fix_sentinels already embody): parse dx/dy through a guarded coercion — try float(cell or 0) except (TypeError, ValueError) -> 0.0 ('a junk cell is not motion') — and compute has_motion from successfully-parsed non-zero values so a junk-only column doesn't fabricate a motion track. (The datetime.fromisoformat parse of canonical created_at_utc at fix.py:1576 is the remaining unguarded foreign-value read in the same function — same crash class, worth the sibling guard in the same commit.) Pin with a fail-first test driving the ARR_V1_FORMAT plan over a '1,5' cell (plus the junk-only-motion variant) asserting the conversion completes and the junk cell ships as a repairable/zero cell, with the numeric-cell control in the same call.

### #2 [MAJOR] pipeline/fix.py:1563 (fix-validate, refuters 0/2)

**fix_v1_to_v2 crashes untyped on player-degradable input — ARR_V1_FORMAT route terminally rejects repairable v1 uploads with no shield and no refund**

fix_v1_to_v2 is the only fix whose route has NO upstream shield (validate.py:894-899 short-circuits payload=='v1' to a bare ARR_V1_FORMAT verdict — no checker, so no STR_SENTINELS/STR_SJ_INVALID reason is ever mapped and no repair step is ever ordered before it), yet it parses three player-provenance values bare. (1) fix.py:1563-1564 `dx = f"{float(dx or 0):.1f}"` over delivered v1 CSV cells — entry 82's second named NOTED-NOT-SETTLED site; probe output: `CRASH ValueError: could not convert string to float: '1,5'` (the exact STR_SENTINELS population K3/22614ef documents as real: locale decimals, 'abc', stringified None; v2.py:726-731 documents Excel/regional-tool re-saved frames.csv arriving in production). (2) fix.py:1576 `datetime.fromisoformat(ca.replace(...))` — canonical.created_at_utc is copied VERBATIM from player/tool metadata by the v1 translator (translate.py:334 `"created_at_utc": meta.get("recording", {}).get("started_at_utc")`, no guard), so even a pristine v1 delivery our own binner wrote carries whatever the capture tool emitted; probe: junk string -> `ValueError: Invalid isoformat string`, numeric -> `AttributeError: 'int' object has no attribute 'replace'` (I4/ee35d3f guarded only the NAIVE case at this parse; retranslate_from_sidecars guards the same two stamps with _utc(), fix.py:915-935 — this twin was left bare). (3) fix.py:1493 `s = json.loads((work/"session.json").read_text())` — strict read, unlike the _read_session_json helper (fix.py:82-94) its own siblings use; ingest.sniff_payload:864 returns 'v1' on key_binding.json presence WITHOUT reading session.json, so an unreadable session.json still routes here; probe: `CRASH UnicodeDecodeError`. All three exception types miss apply_fixes' host tuple (fix.py:425-427) -> kind='session', attempt charged, not refunded; probe of apply_fixes over plan [FIX_V1_TO_V2] shows attempts 1 and 2 fail identically (sniff re-says 'v1' each revalidation because key_binding.json persists, so the identical one-step plan is re-derived) -> continuous.py:901-903 REJECTED 'fix retries exhausted (R2)' + _finalize_reject. Because ARR_V1_FORMAT is fixable, reports.session_reject_labels (reports.py:73-83) degrades the operator surface to the bare 'fix-failed' marker.

**Scenario:** A player re-uploads an old v1 session whose frames.csv was re-saved through Excel/a regional tool (one dx cell now '1,5') — or whose capture-tool metadata wrote a junk/numeric started_at_utc that the v1 translator copied verbatim into canonical.created_at_utc. Ingest sniffs 'v1', validate short-circuits to ARR_V1_FORMAT (bin 2, fixable), the fix crashes ValueError on attempt 1, revalidation re-sniffs 'v1' and re-plans the identical step, attempt 2 crashes identically, and the session is terminally REJECTED 'fix retries exhausted' with media finalized — an unpaid player for a session the pipeline repairs in one attempt on the v2 route (fix_sentinels coerces the same junk cell to 0.0; fix_sessionjson_recompute rebuilds the same stamp from ground truth), surfaced to ops as the bare 'fix-failed' marker.

**Finder's proposed fix (vetting in-iteration per the ruling):** Inside fix_v1_to_v2: (a) parse dx/dy with fix_sentinels' _parse semantics (try float, junk/non-finite -> 0.0) instead of bare float(dx or 0); (b) read session.json via the existing _read_session_json ({} fallback — the conversion path already tolerates canonical={} and recompute rebuilds from ground truth); (c) guard the created_at_utc parse with the same _utc pattern retranslate_from_sidecars uses (unusable -> omit the field; fix_sessionjson_recompute already synthesizes it) or at minimum raise a typed FixFailed naming the field. No plan-order change needed; matches the K3/entry-82 degrade-never-crash doctrine.

### #3 [MAJOR] pipeline/fix.py:1563 (translator, refuters 0/2)

**fix_v1_to_v2 bare float() on v1 dx/dy cells: junk cell burns both attempts into a wrongful terminal reject**

pipeline/fix.py:1563-1564 (`dx = f"{float(dx or 0):.1f}"`) parses player-supplied v1 frames.csv cells with a bare float(). check_session_v2 early-returns on 'key_binding.json present' (translator/v2.py:702-705) BEFORE any cell check, so a v1 delivery's only reason is ARR_V1_FORMAT and STR_SENTINELS can never ride along — proven by probe: plan_fixes([ARR_V1_FORMAT], game='kamla', has_raw=False) -> [('FIX_V1_TO_V2', {}), ('FIX_SESSIONJSON_RECOMPUTE', {})], no repair precedes the float. Executed probe output: direct call -> "ValueError: could not convert string to float: '1,5'"; apply_fixes over that plan -> error "FIX_V1_TO_V2: ValueError: could not convert string to float: '1,5'", kind='session' (not in the host tuple at fix.py:425-427, so no refund); the crash precedes _write_csv so nothing changes for attempt 2; run.py:573-575 rejects at FIX_RETRIES=2 with 'fix retries exhausted (R2)'. Control probe: the byte-identical session with a numeric cell converts fine ('converted v1 -> v2 (2 rows)'), and post-conversion junk is exactly the STR_SENTINELS population FIX_SENTINELS repairs. The K3 commit (22614ef) explicitly deferred this site to this iteration ('fix_v1_to_v2 dx/dy float(dx or 0) — NOTED for iteration 18's lanes, same class on the ARR_V1_FORMAT route'). Note has_motion (fix.py:1537-1539) counts any cell not in ('','0'), so one junk cell alone forces the float path. Same function has unguarded sibling reads of the v1 payload: fix.py:1576 datetime.fromisoformat(ca.replace(...)) crashes on a non-str/malformed canonical created_at_utc, and a truthy non-dict canonical/trim crashes .get() at 1500/1574 — identical route and classification.

**Scenario:** A player uploads a legacy v1 delivery (the upload workflow ships CSVs through player hands; Excel re-saves rewrite 1.5 -> '1,5' in comma-decimal locales — the exact STR_SENTINELS population r-loops 3/7/10 confirmed in the wild, and the population K3's confirmed r17 #3 was fixed for one function over). Ingest sniffs payload='v1', validate.py:894 emits ARR_V1_FORMAT (bin 2, fixable), the fix runner dispatches FIX_V1_TO_V2, float('1,5') raises ValueError, kind='session' charges attempt 1; revalidation reproduces the identical single reason and attempt 2 crashes identically -> REJECTED 'fix retries exhausted (R2)'. A session the pipeline is designed to repair (convert, then FIX_SENTINELS the cells) is terminally rejected under the bare fix-failed marker: unpaid player, lost deliverable hours, operator misled about the cause.

**Finder's proposed fix (vetting in-iteration per the ruling):** In fix_v1_to_v2, parse dx/dy with the fix_sentinels._parse discipline (try/except (TypeError, ValueError) -> 0.0; non-finite -> 0.0), matching the _num_cell/K3 'a junk cell is not motion' semantics — the junk degrades to '0.0' and any residual nonconformance is FIX_SENTINELS' designed job on the next validation. Apply the same degrade-or-typed-FixFailed guard to the sibling v1-payload reads in the same function (canonical/trim non-dict at fix.py:1500/1574, created_at_utc parse at fix.py:1576).

### #4 [MAJOR] pipeline/fix.py:891 (translator, refuters 0/2)

**Present-but-corrupt raw/metadata.json makes FIX_RETRANSLATE crash both attempts after superseding the working CSV-level fixes**

retranslate_from_sidecars reads raw/metadata.json with an unguarded json.loads at pipeline/fix.py:891-892 (errors='replace' handles bytes, not malformed JSON), while has_raw_sidecars (fix.py:67-79) tests only that the file EXISTS, and plan_fixes discards every CSV-level fallback when a retranslate is planned (fix.py:225 'csv_fixes = [] # superseded'). Executed probe output: plan_fixes([SYN_TS_NOT_PTS], has_raw=True) -> [('FIX_RETRANSLATE', {'rerouted': False}), ('FIX_SESSIONJSON_RECOMPUTE', {})] vs has_raw=False -> [('FIX_TSREPAIR_PTS', {}), ...]; has_raw_sidecars over a dir whose metadata.json is the truncated '{"recording": {"started_at' -> True; retranslate_from_sidecars on it -> "JSONDecodeError: Unterminated string starting at: line 1 column 16" -> kind='session' (JSONDecodeError is a ValueError; not in the host tuple), typed FixFailed? False. Nothing is written before the crash, revalidation reproduces the same reasons (the qa-side read of the same file at translator/v2.py:1049 deliberately degrades to a warn-skip, so the frame-sync FAIL persists unchanged), and run.py:573-575 rejects at FIX_RETRIES=2. metadata.json is ingested unparsed (documented at translator/v2.py:1040-1042), so corruption is first touched here. This is r-loop 7's exact 'having the required raw sidecars made a session strictly worse off' shape (the in-code rationale at fix.py:180-192), closed there for STR_SJ_INVALID routing and for MISSING sidecar files, left open for present-but-unparseable metadata; no ruling covers it (entry 19's BundleError covers only the raw-only translate path).

**Scenario:** A player uploads a v2 delivery plus the two raw sidecar files (the standard workflow), and metadata.json arrives truncated or hand-mangled (the class the r-loop-8 BundleError guard was written for, seen in the wild). The delivery independently raises any retranslate-routed fixable reason — SYN_TS_NOT_PTS (frame-sync drift from the tool's 12-20% dropped frames, historically the most common fixable class), SYN_LAG_CONST, INP_KEYS_NO_ACTION, STR_ROWS_MISMATCH, STR_SENTINELS or a QA_FAIL_UNMAPPED. Because raw/ exists, plan_fixes supersedes the one-attempt CSV-level repair (FIX_TSREPAIR_PTS et al.) with FIX_RETRANSLATE, which crashes on json.loads on both attempts -> REJECTED 'fix retries exhausted (R2)'. Had the player NOT attached the sidecars, the same session would be repaired in one attempt and delivered: wrongful terminal reject, unpaid player, and the operator-facing reason degraded to the bare fix-failed marker.

**Finder's proposed fix (vetting in-iteration per the ruling):** Two halves: (a) plan-level, per the settled gray-zone rule ('raw-needing fixes fall back to CSV-level'): make has_raw_sidecars also require raw/metadata.json to parse to a dict (cheap json.loads with errors='replace', except -> False), so an unusable sidecar plans the CSV-level fixes instead of superseding them — mirroring how it already treats a missing file; (b) belt-and-braces in retranslate_from_sidecars: wrap the fix.py:891 read in try/except (json.JSONDecodeError, OSError) -> typed FixFailed naming raw/metadata.json, so any residual path stays attributable instead of a bare JSONDecodeError.

### #5 [MAJOR] pipeline/fix.py:1563 (tests-coverage, refuters 0/2)

**fix_v1_to_v2 crashes on junk v1 motion cells - the entry-82 NOTED class, proven: wrongful terminal reject with the cure one step later in the same plan**

fix_v1_to_v2 (pipeline/fix.py:1561-1564) does a bare `dx = f"{float(dx or 0):.1f}"` / `dy = f"{float(dy or 0):.1f}"` over v1 delivered CSV cells. `or 0` handles only ""/None; the STR_SENTINELS population (locale decimals '1,5', 'abc', stringified 'None') raises ValueError out of the fix. I proved the full chain through the REAL machinery in a pristine HEAD scratch copy (probe built on the suite's own _v1_work fixture with one dx cell set to '1,5'): plan_fixes([ARR_V1_FORMAT, STR_SENTINELS], game="kamla", has_raw=False) -> plan steps ['FIX_V1_TO_V2', 'FIX_SENTINELS', 'FIX_SESSIONJSON_RECOMPUTE'] (FIX_V1_TO_V2 is emitted at fix.py:211 before every csv fix, so the sentinel repair can never run first; with has_raw=True the retranslate supersedes csv fixes but FIX_V1_TO_V2 is still first). apply_fixes returned: error='FIX_V1_TO_V2: ValueError: could not convert string to float: '1,5'', kind='session' (apply_fixes:425-427 classifies ValueError session-kind - no refund), applied=[('FIX_V1_TO_V2', False)]; attempt 2 replayed byte-identically ('attempt-2 error: FIX_V1_TO_V2: ValueError ... | kind: session'). This is the exact mechanism of the CONFIRMED r17 #3 (K3) at the sibling call site K3's own sweep listed as 'NOTED for iteration 18's lanes' (memory entry 82 explicitly invites reporting it with a proven harm path). Note also fix.py:1537's has_motion test (`not in ("", "0")`) reads a junk cell as motion, so the guard cannot be bypassed by the no-motion arm. No test in the suite feeds fix_v1_to_v2 a non-numeric motion cell (grepped all callers: test_r_loop11.py:595, test_r_loop15.py:173/193, test_r_loop17.py:187/199/213 - all numeric).

**Scenario:** A foreign v1-format delivery (the ARR_V1_FORMAT route exists precisely for these) arrives with one locale-decimal or stringified-None dx/dy cell - the same real population behind the confirmed K3 finding. Validation maps ARR_V1_FORMAT (fixable); plan = [FIX_V1_TO_V2, ...]; FIX_V1_TO_V2 crashes with ValueError, classified session-kind, attempt burned before any repair writes; revalidation reproduces identical reasons; attempt 2 crashes identically; the session is REJECTED 'fix retries exhausted' under the bare fix-failed marker - a wrongful terminal reject, an unpaid player, and two paid VLM sweeps, exactly the harm chain of the confirmed r17 #3.

**Finder's proposed fix (vetting in-iteration per the ruling):** Guard the two float() reads exactly like the K3 template (fix.py's _moving / v2.py's guarded _active): a try/except (TypeError, ValueError) that degrades the junk cell to 0.0/blank - matching _num_cell/fix_sentinels semantics, so FIX_SENTINELS later in the same plan (or the retranslate) still owns the real repair. Pin it with a test on the ARR_V1_FORMAT route: _v1_work + one '1,5' dx cell -> fix_v1_to_v2 completes and the row's keys/actions survive (fails pre-guard with the exact ValueError).

### #6 [MAJOR] pipeline/fix.py:1525 (tests-coverage, refuters 0/2)

**K2's keybind-fallback anchor is arming-gate-invisible: deleting game_name=slug passes the FULL gate 793/793 while OW v1 conversions with unusable keybinds ship checker-green corrupted frames (the r15 #6 no-op-cohort class inside the K set)**

K2 (bd94829) resolves the session keybind via `resolve_keybind(keybind_path=kbp, game_name=slug, exe_name=None)` (pipeline/fix.py:1525) and its commit message leans on 'resolve_keybind's parsed-but-unusable fallback already lands on the right built-in' - but every test that reaches fix_v1_to_v2 uses either a USABLE custom keybind or NO keybind file (which takes the non-resolve_keybind else-branch at fix.py:1528), and all pass game="kamla" (verified: test_r_loop11.py:595, test_r_loop15.py:173/193, test_r_loop17.py:187/199/213), so the anchor and the whole unusable-fallback contract are no-ops in the entire cohort. PROVEN by mutation in a fixed-tree scratch copy outside the repo: with game_name=slug changed to game_name=None, the FULL arming gate (the documented invocation: uv run --with pytest --with numpy==2.4.6 --with opencv-python-headless==5.0.0.93 --with rerun-sdk==0.36.0 pytest pipeline/tests translator/tests -q) printed '793 passed in 149.06s' - byte-identical to my pristine-HEAD baseline '793 passed in 148.96s' at floor 789. Behavioral divergence proven by probe on an outer_wilds-ledger v1 session (suite's own _v1_work fixture, canonical game 'Outer Wilds', keybind.json={"look_up": 12345} - the r-loop-7 parsed-but-unusable VK-code population): HEAD output 'move_up: rows=200 W-kept=200 empty-keys=0' (same for A/S/E), checker WARN with zero key/action FAILs; MUTANT output 'move_up: rows=200 W-kept=0 empty-keys=200', 'move_left: A-kept=0 empty-keys=200', 'move_down: S-kept=0 empty-keys=200' (E survives only as a KEYBIND_PATCHES literal via bound={e,enter}), checker STILL WARN with zero key/action FAILs - the exact orphan-action corruption K2 was confirmed for, checker-green because keys-have-actions is one-directional. This is precisely the r15 #6 / r17 #4-#6 class (a pin cohort that runs the fixed line only where it is a no-op), recurring inside the very fix set whose K4/K5/K6 commits were dedicated to closing it - and entry 81 reserves 'a mis-anchored slug' as a live defect class at this site.

**Scenario:** A future edit restates the anchor - e.g. reverting game_name to the metadata-derived value during a refactor, the exact regression shape this executor already produced once (r14 H2, where retranslate_from_sidecars anchored on player-typed metadata and I3 had to add a live discriminator because the finder's slug-revert mutant was arming-gate-green at 749). The full suite stays green at 793/793, the gate arms, the flip proceeds - and from then on every outer_wilds session arriving in v1 format with a parsed-but-unusable keybind.json (VK codes, nulls, {}) converts with ALL movement presses deleted from frames.csv while their v1-resolved actions ship orphaned on empty input_keys cells: silent, checker-green corruption of delivered training data that no retranslate is ever planned for, reaching Odyssey undetected.

**Finder's proposed fix (vetting in-iteration per the ruling):** Add the I3-recipe discriminator where the anchor is LIVE: an outer_wilds-ledger v1 conversion test with a parsed-but-unusable keybind.json (e.g. {"look_up": 12345}) at the work root, asserting the W/A/S presses survive with their v1 actions (my probe shows it fails under game_name=None and under a metadata-anchored restatement, and passes at HEAD). Using outer_wilds also makes the currently-unexercised KEYBIND_PATCHES layer at fix.py:1529 live in the same test (every existing fix_v1_to_v2 test is kamla, for which KEYBIND_PATCHES has no entry).
