# R21_FINDINGS.md — review iteration 21, findings of record

**Run 2026-08-20** by the R19/R20/R21 executor session via the workflow
snapshot `tools/review/flip-review-iter21.js` (run per the 2026-08-20 NEW
RULING: automatic launches; NOT quiet → the confirmed set is fixed
in-iteration as the O set and iteration 22 launches automatically as THE
CHECKPOINT pass; QUIET → the independent e2e launches). 7 finder lanes,
2-vote refute, 21 agents (21 completed, 0 errors, ~2.95M subagent tokens,
run `wf_54f4e364-8b9`). HEAD `0fa07fd` (code HEAD `f66d3ed` = N1–N5 +
floor 836; Mac gate 840/836 green at launch; the VM gate was pending a
gcloud reauth and ran green post-launch — see §0).

**Verdict: NOT QUIET pre-fix — 7 raised → 7 confirmed
(0 blockers, 3 major, 4 minor), 0 killed.** Six at 0/2 refuted; #4
(≡#6) survived at 1/2 (one refuter argued it is entry 96's letter; its
twin #6 was 0/2-confirmed on the same mechanism — treated as ONE defect).

**Clusters/classes (doc numbering, severity-ordered):** #1 is the N3
class ONE STEP DOWNSTREAM (payment-surface): reports._stamp's non-CAS
arms (unconditional, recorded-'', CAS-miss-now-'') re-land the LABELS
mark N3 clears when a '' supersede fires inside the send window — the
delivered re-run's hours strand exactly as r20 #2≡#6, through the
stamping side instead of the preserving side. #2 is the M1/N1
fabrication class on the DIRECT-v2 route: fix_sessionjson_recompute's
synthesize-from-now stamp enters the live head-offset contract
(usable sidecars), poisoning the raw verify into a deterministic
wrongful terminal reject — the entry-90 warn-skip shield covers only
M2-unusable sidecars. #3 and #4≡#6 are N1's head-cut gate incomplete:
negative finite junk passes the isfinite-only test and fabricates a
head-offset fact on the live route (#3), and the overflow
disambiguation blames a GOOD stamp for any constructible-but-absurd
head in the ~2.5e11..8.64e13s band, shipping synthesized wall-clock
stamps no-sidecar and misdirecting the refusal diagnosis sidecar
(#4≡#6). #5 is a pre-existing atomicity gap the N-era heal work
surfaced: the quarantined-path heal's three separate commits leave a
torn row unreachable by every recovery arm. #7 is the
arming-gate-invisible class inside N1 itself: the no-sidecar
emit-overflow guard's deletion mutant passes the full gate at 840.

Machine-readable results incl. both refuter verdicts: session scratchpad
`r21-results.json`.

## Confirmed findings (severity-ordered)

### #1 [MAJOR] pipeline/reports.py:384 (payment-split, refuters 0/2)

**Mid-window '' supersede: _stamp's ''-arms re-land the labels mark N3 clears — delivered re-run's hours stranded silently**

N3 (dde54ab) clears the LABELS-only accepted_reported_at at the '' writers (ledger.supersede preserve arm, ledger.py:357-361; heal preserve arms, ingest.py:373-384), but reports._stamp re-lands that same mark when the '' supersede fires BETWEEN the sheet's row read and the accepted stamp: the CAS on the snapshot's real md5 misses (row now ''), and the arm at reports.py:377-385 ('a zip-class writer rewrote the md5 to the UNKNOWABLE sentinel ... ledger.update(sid, **{column: now})') stamps accepted_reported_at onto the post-supersede DISCOVERED row. The recorded-'' arm (reports.py:361-372, no ZIP_ADJ_CHANGED marker yet at stamp time) and recal_regen_sheets' unconditional stamps (tools/recal_regen_sheets.py:404, md5s=None => reports.py:341-344) reach the same state. The arm's self-heal rationale (entry 55: 'identical bytes: the stamp correctly stands') is only valid for HOURS marks; in production every mid-window '' supersede of an accepted-counted root is over a REJECTED root (both supersede callers, ingest.py:473/516, are gated REJECTED/QUARANTINED, and QUARANTINED roots are never counted), so the re-landed mark is always the labels mark — and N3's own premise (commit dde54ab: 'fresh fix budget, nondeterministic VLM ... the 122/122 black-frozen false rejects are the measured precedent') is that identical bytes can DELIVER on re-run. Proved by running production code against a scratch ledger (probe_a_armstamp.py in the session scratchpad): REJECTED root counted (labels) -> ledger.supersede(new_md5='') before mark_accepted_reported -> 'post-stamp: acc_stamped=1 state=DISCOVERED acc_mark=2026-08-20T06:03:59+00:00' -> identical-bytes deferral declines to clear (breadcrumb == local md5) -> re-run DELIVERED 3300s -> build_sheet_rows over 30 consecutive windows: 'DELIVERED hours reached a sheet: False'. No re-entry arm can fire (accepted mark set blocks _tree_has_uncounted_accepted at reports.py:659; uploaded stamp preserved blocks late/uncountable_due) and no loud line prints anywhere. The existing pins do not cover this: test_r_loop20.py:288 (N3) supersedes AFTER the stamp landed; test_r_loop13.py:177 pins the mid-send arm-4 stamp standing only on a DELIVERED seed root — an hours mark, and a state no production caller can supersede.

**Scenario:** Player's zip session is REJECTED (e.g. black-frozen, fix retries exhausted). The 14:00 daily send counts its reject label; while the send's Telegram messages are in flight (or during a crash-resume replay hours later — run.py's _resume_daily_send _bytes_changed pre-filter at run.py:1102 deliberately passes ''-md5 rows through), the coached player re-zips the same folder and hl-S's scan supersedes the slot with new_md5='' (ingest.py:548). mark_accepted_reported then stamps the labels mark onto the fresh DISCOVERED slot via reports.py:384. The download proves video.mp4 byte-identical (ctime-only re-zip), so the deferral correctly declines to clear; the re-run with reset fix_attempts and a nondeterministic VLM verdict DELIVERS. The delivered hours carry an accepted_reported_at that a sheet only ever counted a LABEL under: footage ships to Odyssey, the player's hours reach no payment sheet, forever, with zero loud lines — the exact 'labels mark that still strands a delivered re-run's hours' class entries 92/98 list as reportable, one step downstream of the writers N3 fixed.

**Finder's proposed fix (vetting in-iteration per the ruling):** In reports._stamp, guard the ACCEPTED column only: before stamping through any non-CAS arm (arm 1 unconditional, arm 2 recorded-'', arm 4 CAS-miss-now-''), when column == 'accepted_reported_at' re-read the row and skip LOUDLY if its current state is not in ('DELIVERED', 'REJECTED') — the count belonged to a generation a '' writer has since reset, and the mark would land on the next generation. Money-safe in every direction: if the identical-bytes re-run re-rejects, the label re-prints once (the ruled accepted cost of N3 — no money moves through a label); if it delivers, the hours reach the next sheet exactly once (probe P6 semantics); if bytes prove changed, the deferral's full clear was going to erase the mark anyway. uploaded_reported_at keeps today's behavior unchanged (skipping it on identical bytes would re-open the r12 #1/#2 uploaded double-pay). DELIVERED/REJECTED nodes whose state is unchanged (the entire normal population, split children included) stamp exactly as today.

### #2 [MAJOR] pipeline/fix.py:1492 (fix-validate, refuters 0/2)

**fix_sessionjson_recompute's synthesized created_at_utc enters the live head-offset contract: sidecar-bearing sessions with a junk stamp take a deterministic wrongful terminal reject under the bare fix-failed marker**

The kickoff's own entry-97 question — 'does the synthesized stamp stay out of the sealed head-offset contract' — answers NO. fix_sessionjson_recompute synthesizes created = datetime.now(timezone.utc) whenever the stored stamp is unusable (fix.py:1492, the r-loop-7 arm) or overflows (fix.py:1523, the M6/N2 arm), with no marker that the stamp is synthesized. On a session with USABLE raw sidecars that stamp is consumed as the head-offset fact by translator/v2.py:1052 (_verify_against_raw: head_us = created − started) and pipeline/fix.py:983 (retranslate_from_sidecars: head_s). Proven by executing the real functions on a scratch fixture whose CSV is provably correct (control: with the true stamp, check_session_v2 prints 'OK: off-by-one check — 300 raw mouse events independently re-binned…CSV matches exactly on all 300 rows'): (P2) junk created_at_utc '31/07/2026 10am' → FAILs map to [('STR_SJ_INVALID', True, True)], plan_fixes(has_raw=True) → [FIX_SESSIONJSON_REWRITE, FIX_SESSIONJSON_RECOMPUTE], raw verify warn-skips; (P3) attempt 1 runs fix_sessionjson_recompute → created_at_utc = 2026-08-20T06:07:43.677055Z (NOW); (P4) revalidation → 'FAIL: raw recomputation: dx/dy differs from CSV in 300 frames (max run 300, 100.00% of rows)' (validate.py:87 maps it SYN_TS_NOT_PTS fixable) → plan [FIX_RETRANSLATE {'rerouted': False}]; (P5) retranslate_from_sidecars → FixFailed 'head offset 832063.7s leaves zero events beyond 0 held-key carries…refusing to re-bin' — deterministic, because at validation time now > started + raw-recording-length always. run.py:577/573 then exhausts FIX_RETRIES=2 → REJECTED 'fix retries exhausted'. Entry 90's shield ('the raw verify warn-skips…so no consumer acts on the synthesized stamp') covers only M2-UNUSABLE sidecars (started unparseable); with usable sidecars both consumers act on it. M1/N1 closed exactly this fabrication class inside fix_v1_to_v2 (recover-or-refuse on the live route) but left the direct-v2 rewrite route — reached via the M7 mappings 'session.json unreadable'/'timestamps unparseable' → STR_SJ_INVALID at validate.py:142-145 — fully open.

**Scenario:** A standard-shape upload (out/ + the two raw sidecars — the player-guide workflow, so has_raw_sidecars is True) arrives with a corrupt created_at_utc in session.json (locale-formatted date, truncated/hand-edited file, or the N2 overflow class) and a perfectly good frames.csv. Attempt 1's FIX_SESSIONJSON_REWRITE repairs session.json but stamps created=NOW; revalidation's raw off-by-one check falsely condemns the correct CSV (SYN_TS_NOT_PTS); attempt 2's FIX_RETRANSLATE refuses deterministically; the session is terminally REJECTED with 3 paid VLM sweeps spent — while its sidecar-less twin DELIVERS the identical repaired artifacts (no raw verify runs). 'Having the required raw sidecars made a session strictly worse off' — the doctrine sentence written in plan_fixes' own STR_SJ_INVALID comment. The stored reason at reject time is fixable=true, so the ruled reject-label surface (unfixable-only) prints the false bare fix-failed marker — the exact r19 #11 operator-misleading harm — and the player is unpaid for deliverable footage.

**Finder's proposed fix (vetting in-iteration per the ruling):** Keep the synthesized stamp out of the live contract. Cheapest mechanism: when fix_sessionjson_recompute synthesizes (rather than preserves/re-emits) created_at_utc, record it — e.g. a {'created_synthesized': true} entry via _locked_report_update in the work-root translation_report.json (the file the verify already consults for shift_us) — and have _verify_against_raw warn-skip ('raw verification skipped: created_at_utc synthesized by repair') plus retranslate_from_sidecars raise its typed cannot-derive refusal naming the synthesis, on that marker. Alternatively mirror N1's doctrine: on a work dir where fix.has_raw_sidecars() is True and the stamp is unusable, refuse typed instead of synthesizing, so the reject is truthful, attempt-cheap, and correctly labeled. Direction choice is checkpoint material; either closes the poisoning loop.

### #3 [MAJOR] pipeline/fix.py:1697 (fix-validate, refuters 0/2)

**N1's head-cut usability gate admits NEGATIVE finite junk: head_usable is isfinite-only, so a physically-impossible head_cut_s fabricates the head-offset fact on the live-sidecar route, bypassing the refusal N1 built**

fix.py:1693-1700 (N1, f6fa524) refuses None, bool, unparseable and non-finite head_cut_s values, but accepts any finite float: head_cut = float(hv); head_usable = math.isfinite(head_cut). A trim length is >= 0 by construction, so a negative value is present-but-junk evidence — yet with a usable stamp it skips the refusal gate entirely (the first block at fix.py:1717-1719 emits stamp + negative timedelta and created_str is never None, so _v1_sidecar_started is never consulted), and with an unusable stamp the RECOVERY arm at fix.py:1752-1755 emits started + negative head and writes the false fixlog attestation 'created_at_utc recovered from raw started_at_utc + head_cut'. Contrast: hv=null with the same usable stamp and sidecars is refused typed ('canonical.trim head_cut_s unusable while usable raw sidecars are attached') — the gate treats -30.0 strictly better than null. Proven by executing the real chain on a scratch v1 fixture (usable sidecars, true head 5.0s, CSV provably correct, trim {'head_cut_s': -30.0}): (S1) fix_v1_to_v2(work,'kamla') → 'converted v1 -> v2 (300 rows)', delivered created_at_utc = 2026-08-10T14:59:30.000000Z = started − 30s (truth: started + 5s); (S2) check_session_v2(raw_bundle) → 'FAIL: raw recomputation: dx/dy differs from CSV in 300 frames (…100.00% of rows)' → mapped SYN_TS_NOT_PTS, falsely condemning the still-correct CSV; (S3) retranslate_from_sidecars → succeeds ('re-translated from sidecars (300 frames…)') re-binning at head_s = max(−30, 0) = 0 — motion rows moved from 0..299 to 150..299, every surviving input 5s early, half the input data dropped; (S4) the verify (head −30) and the re-bin (clamped head 0) disagree forever → 'FAIL: raw recomputation: dx/dy differs in 150 frames (…50.00%)' → SYN_TS_NOT_PTS re-fires, FIX_RETRIES=2 exhausted → terminal reject.

**Scenario:** A v1 bundle with usable sidecars arrives with canonical.trim.head_cut_s corrupted to a negative float — same player-file junk population the confirmed r20 #1 falsy family came from (sign flip in a hand-edited file, an inverted-operand producer bug computing started − first_frame). Attempt 1 converts with a fabricated created stamp 30s before the recording even started; revalidation falsely condemns the correct CSV; attempt 2's retranslate corrupts the working copy (all inputs re-binned 5s early) and the reason re-fires; the session takes a terminal 'fix retries exhausted' reject — printed as the bare fix-failed marker since SYN_TS_NOT_PTS is stored fixable=true — with 3 paid VLM sweeps burned, for a deliverable session whose only defect was one junk trim value. On the unusable-stamp variant the fixlog additionally carries a false 'recovered from raw started_at_utc + head_cut' attestation, poisoning the payment-dispute audit trail.

**Finder's proposed fix (vetting in-iteration per the ruling):** Add the physical bound to the usability test at fix.py:1698: head_usable = math.isfinite(head_cut) and head_cut >= 0.0. Negative then joins the refused junk family with exactly N1's committed dispositions: typed canonical.trim refusal on the live-sidecar route, and keep-the-parseable-stamp-at-head-0.0 on the no-sidecar route (r19 #10 semantics), with a failing-side pin (hv=-30.0, usable sidecars → FixFailed; no sidecars → stamp kept at head 0).

### #4 [MINOR] pipeline/fix.py:1729 (regressions-r20, refuters 1/2)

**N1 overflow-disambiguation discards a good v1 stamp on the no-sidecar route, violating the r19 #10 keep-stamp contract**

N1 (f6fa524) rewrote the stamp/head resolution so that when `stamp + timedelta(head_cut)` overflows (line 1719) it disambiguates the junk side: if `timedelta(seconds=head_cut)` constructs it sets `stamp = None` (fix.py:1729). But `stamp = None` also kills the no-sidecar keep-stamp arm at line 1763 (`elif stamp is not None`), so a genuinely-parseable created_at_utc is dropped and `created_str` stays None -> omit -> fix_sessionjson_recompute synthesizes from `now`. The pre-N1 M1 tree set `head_usable = False` instead of nulling the stamp, so the same `elif stamp is not None` arm preserved it. Proven by execution on a no-sidecar v1 work dir with created_at_utc='2026-08-10T15:34:03Z' and trim.head_cut_s in {3e11, 1e12, 1.7e12}: M1 tree (git archive 5524563) -> created_at_utc='2026-08-10T15:34:03.000000Z' (kept); HEAD -> created_at_utc='2026-08-20T06:17:31Z' (synthesized from now). The trigger range is head_cut_s ~2.5e11..8.64e13 s: large enough that 2026+head overflows datetime.max, small enough that timedelta(head) still constructs. The r20 test cohort only covers falsy junk heads (which set head_usable=False, not this path) and near-max STAMPS (which are meant to be discarded), so this finite-large-head no-sidecar case is unpinned.

**Scenario:** A v1 (ARR_V1_FORMAT) session with no usable raw sidecars, a correct created_at_utc, and a corrupt canonical.trim.head_cut_s carrying a large finite value (e.g. a millisecond-epoch value ~1.7e12 pasted into the seconds field by a units/copy-paste error). Entry 96/90 promise the no-sidecar arm 'keeps a parseable stamp at head 0.0'; instead the delivery ships created_at_utc equal to the processing time (now) rather than the real recording time — silent delivered-metadata corruption of exactly the class r15 #7 flagged, on the branch where nothing downstream can re-derive it. No wrongful reject and no money impact (output stays internally consistent, so the checker passes), which bounds severity to minor.

**Finder's proposed fix (vetting in-iteration per the ruling):** On the overflow disambiguation, do not discard a genuinely-parseable stamp when it may still be needed by the no-sidecar keep arm. Either restore M1's `head_usable = False` (which leaves `stamp` intact for the `elif stamp is not None` arm while still refusing on the live route), or capture the good stamp before nulling (e.g. `good_stamp = stamp`) and emit from `good_stamp` in the no-sidecar keep arm. Add a no-sidecar test with a finite-large head_cut_s asserting the real stamp survives at head 0.0.

### #5 [MINOR] pipeline/ingest.py:388 (driver-core, refuters 0/2)

**Quarantined-path heal is not failure-atomic: a fault between its three commits strands the session in QUARANTINED forever, silently**

The heal's slot reset is three separate auto-commit ledger calls — ledger.update(drive_path/md5/bytes/ctime/attribution + clears) at ingest.py:388-397, ledger.set_reasons at :404, ledger.set_state(DISCOVERED) at :405-408 — unlike its sibling ledger.supersede, which performs the identical reset in ONE commit (ledger.py:362-389, commit at :389). If anything interrupts the sequence after the first commit (kill -9/OOM in the window, or an sqlite3.OperationalError on the second/third commit — the disk-full and lock-contention classes this driver has documented incidents of; _lane_loop at continuous.py:576-586 catches it as a transient, alerts once, and reconnects), the row is left state=QUARANTINED with drive_path/md5/bytes/ctime already updated to the HEALED values. From then on no scan pass can ever recover it: the heal branch requires existing.drive_path != ds.drive_path (ingest.py:272-273) — now equal; and the re-upload supersede arm requires changed bytes or a newer ctime (ingest.py:519-521) — both already overwritten to the listing's values. Proved by execution against the real code in a scratch tree: after injecting one OperationalError at the heal's set_reasons call, the row read 'state=QUARANTINED path=kamla/op-fixed/... md5=aaaaaa', and three subsequent clean scans printed 'state=QUARANTINED ... discovered=False superseded=False flags=[]' each time — no flag, no recovery. The control (same error injected BEFORE ledger.update) converged: the next clean scan healed the row ('state=DISCOVERED ... quarantined path healed — re-registered'). So the divergence is specific to the post-first-commit window; every other multi-write slot path I audited (move-heal, vanished arm, supersede, dup-cross rejects) either is single-commit or converges on retry.

**Scenario:** A player's session is quarantined post-download (e.g. validation crash). The operator corrects the tree and the same sid parses clean at a new path — the designed review-r3 #7 correction. During that scan pass, one ledger commit fails mid-heal (ENOSPC boundary during a disk-pressure episode, busy_timeout expiry under a backup/WAL-checkpoint burst, or the process is SIGKILLed in the ~ms window). The scan lane alerts 'scan lane iteration failed (lane continues)' — which reads as transient — reconnects, and every subsequent 5-minute scan silently skips the row forever. The corrected upload never downloads, never validates, never delivers; the session is invisible on every ops surface (QUARANTINED is excluded from the stuck list by design, and once the old path left the listing the chase list drops it too). The player's footage sits on Drive I unprocessed and unpaid with zero loud lines, unless the operator happens to rename the folder a second time or re-upload different bytes — and nothing ever tells them to.

**Finder's proposed fix (vetting in-iteration per the ruling):** Make the heal's row reset atomic, mirroring supersede's shape: extract a Ledger.heal_quarantined_path(...) that executes the UPDATE (including clears), the reasons reset, the state UPDATE, and the event INSERT in a single transaction with one commit, and have ingest.scan's heal branch call it. (Alternative with the same effect: write the DISCOVERED set_state FIRST — a torn remainder then leaves a DISCOVERED row at the old path whose gone-from-listing state the existing move-heal arm re-points on the next scan, so every torn prefix converges.)

### #6 [MINOR] pipeline/fix.py:1727 (translator, refuters 0/2)

**N1's overflow disambiguation misblames the stamp for timedelta-constructible junk heads, discarding a good stamp (no-sidecar) and misdirecting the refusal (sidecar)**

fix_v1_to_v2's r20 #11 arm (pipeline/fix.py:1720-1731) decides which side of a `stamp + timedelta(seconds=head_cut)` OverflowError is junk by testing only whether `timedelta(seconds=head_cut)` constructs — but timedelta's range (~8.64e13 s) is ~340x wider than the span any present-day stamp can absorb (~2.5e11 s to datetime.max). For any finite head_cut in that gap (e.g. 1e12 — an epoch-milliseconds-shaped value; a ~31,700-year 'head cut' on a sane 2026 stamp), the addition overflows, timedelta constructs, and line 1729 sets `stamp = None`, blaming the GOOD stamp for the junk head. Executed proof (probe_n1.py in the session scratchpad, real ffmpeg video + translate_bundle payload): with canonical created_at_utc '2026-08-01T00:00:00.000000Z' and trim.head_cut_s=1e12, NO sidecars, the conversion completed and the delivered session.json carried created_at_utc '2026-08-20T06:04:30.649054Z' / ended '2026-08-20T06:04:40.715721Z' (processing wall-clock, checker WARN-only = ships green); the 1e18 control (whose timedelta overflows, so the head is blamed) kept '2026-08-01T00:00:00.000000Z'. WITH sidecars, head=1e12 produced FixFailed 'raw started_at_utc + head_cut overflows a representable stamp — cannot recover... repair raw/metadata.json or session.json' (lines 1756-1762) instead of the designed canonical.trim refusal (lines 1745-1751) taken by the 1e18 control. This contradicts memory entry 96's recorded semantics ('No-sidecar arms keep the r19 #10 semantics: junk head VALUE keeps a parseable stamp at head 0.0') and resurrects, for this band, the exact harm the committed r19 #10 pin's own docstring names ('discarded the VALID parseable stamp and the delivered created_at_utc silently became processing wall-clock time' — pipeline/tests/test_r_loop19.py:209-215). No committed pin covers the band: the r19/r20 junk-head pins use 'abc', '5,0', 10**400, inf, '1e999', 1e18 — every one keeps the stamp or refuses correctly; 1e12 does neither.

**Scenario:** A v1 payload whose canonical.trim.head_cut_s was corrupted into the 2.5e11-8.64e13 band (e.g. an epoch-milliseconds value ~1.7e12 written where seconds belong) beside a perfectly good created_at_utc. No-sidecar route (zip carrying only the delivery): fix_v1_to_v2 silently discards the good stamp, fix_sessionjson_recompute synthesizes created/ended from translation-time now, and the session ships checker-green with fabricated recording timestamps in the delivered metadata — silent delivered-data corruption of the class M1/r19-#10 was ruled to prevent, invisible to every gate. Sidecar route: a typed terminal refusal whose diagnosis leads with raw/metadata.json — a file nothing in the pipeline can repair — for a defect that lives in session.json's trim, misleading the operator triaging the reject (the 1e18 twin of the same junk class gets the correct 'repair session.json's canonical.trim' coaching).

**Finder's proposed fix (vetting in-iteration per the ruling):** In the OverflowError handler at pipeline/fix.py:1720-1731, blame the stamp only when the stamp is actually implausible: after `timedelta(seconds=head_cut)` constructs, additionally test whether the stamp ALONE emits (`_emit_utc(stamp)` succeeds) and whether the head is sane (e.g. `head_cut <= 366*86400`, or bounded by the probed video duration); if the stamp emits and the head exceeds the bound, set `head_usable = False` (the existing canonical.trim refusal / r19-#10 keep-stamp dispositions) instead of `stamp = None`. Add the missing-band pins: no-sidecar head=1e12 with a good stamp keeps the stamp; sidecar head=1e12 takes the canonical.trim refusal.

### #7 [MINOR] pipeline/fix.py:1768 (tests-coverage, refuters 0/2)

**N1's no-sidecar emit-overflow degrade arm has no failing-side test — its exact deletion mutant passes the full arming gate at 840/836**

The N1 commit (f6fa524) added three _emit_utc call sites; two are pinned, the third is not. fix_v1_to_v2's no-sidecar 'keep the good stamp at head 0' arm (pipeline/fix.py:1763-1773) wraps `created_str = _emit_utc(stamp)` in `try/except OverflowError: created_str = None`. Deleting that guard in a scratch clone (leaving bare `created_str = _emit_utc(stamp)`) and running the exact FLIP_RUNBOOK §6b invocation `SUITE_FLOOR=836 bash tools/run_suite.sh --with numpy==2.4.6 --with opencv-python-headless==5.0.0.93 --with rerun-sdk==0.36.0` produced `840 passed in 166.83s` / `ARMING GATE OK: 840 passed (floor 836)` — identical to the real gate's green. A probe proves the mutant deletes real behavior: input = v1 work dir (test_r_loop15._v1_work), canonical created_at_utc '9999-12-31T20:00:00-05:00' (aware, near-max, negative offset), trim {'head_cut_s': 'abc'} (junk -> head_usable False), NO sidecars. Fixed tree: `COMPLETED: converted v1 -> v2 (800 rows) | created_at_utc = 2026-08-20T06:04:48.326045Z` (omit-and-synthesize). Mutant: `RAISED: OverflowError - date value out of range`. Why the N1 cohort misses this arm: test_v1_nearmax_aware_stamp_emit_overflow_degrades' no-sidecar half uses a USABLE head (5.0), so the overflow fires in the FIRST resolution arm (fix.py:1719) and the r20 #11 disambiguation sets stamp=None before the elif arm ever runs; test_v1_no_sidecar_falsy_trim_keeps_stamp reaches the elif arm but only with a sane 2026 stamp that cannot overflow. No test in the 844 collected combines junk head + emit-overflowing stamp + no sidecars — verified empirically by the gate-green mutant run, not by grep. (Fail-first for the rest of the N set verified separately: 14/19 of test_r_loop20.py fail at pre-fix 5524563; the 5 passers are the declared controls and the N5 pins.)

**Scenario:** This is the exact arming-gate-invisible class the M8 (r19 #7/#8/#9) and N5 (r20 #8) commits exist to close, now re-opened by N1 itself. Concrete path: a future refactor of fix_v1_to_v2 (this function has been rewritten in L1, M1, M3 and N1 — four consecutive iterations) drops or narrows this guard; the suite — the flip's ARMING GATE — stays green at 840, the flip arms, and in production a v1 session whose corrupt canonical carries an aware near-max stamp beside a junk head_cut_s (the same '9999-12-31T20:00:00-05:00' + junk-trim family r20 #9/#10/#11 treat as real) crashes fix_v1_to_v2 with an uncaught OverflowError; apply_fixes classifies it session-kind (fix.py:471-473 host tuple excludes OverflowError), both fix attempts burn, and a repairable session is terminally rejected — the wrongful-reject class M1/N1 were built to prevent, shipped invisibly through the gate.

**Finder's proposed fix (vetting in-iteration per the ruling):** Add one failing-side pin to pipeline/tests/test_r_loop20.py (the N5 recipe): build _v1_work with created_at '9999-12-31T20:00:00-05:00' and canonical trim {'head_cut_s': 'abc'} (and/or a non-dict trim), no sidecars; assert fix_v1_to_v2 completes with 'converted v1 -> v2' in the note and a _TS_RE-conformant created_at_utc not starting with '9999'. The deletion mutant proven gate-green at 840 then fails exactly this pin.

