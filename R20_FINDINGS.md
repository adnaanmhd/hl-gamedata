# R20_FINDINGS.md — review iteration 20, findings of record

**Run 2026-08-20** by the R19/R20 executor session via the workflow snapshot
`tools/review/flip-review-iter20.js` (run per the NEW RULING, Adnaan
2026-08-20, recorded in R8_IMPLEMENTATION_PLAN §0/§4: iteration 20 launches
automatically after the post-M gates; NOT quiet → its confirmed set is fixed
in-iteration with full §2 discipline as the N set and iteration 21 launches
automatically; a QUIET 20 or 21 auto-launches the independent e2e; iteration
22's results are the checkpoint). 7 finder lanes, 2-vote refute, 29 agents
(29 completed, 0 errors, ~3.79M subagent tokens, run `wf_c14f39a5-27d`).
HEAD `8ab9c64` (code HEAD `a239fad` = M1–M8 + floor 817; suite 821/821 both
hosts, Mac 158.7s / VM 566.9s).

**Verdict: NOT QUIET pre-fix — 11 raised → 11 confirmed
(0 blockers, 8 major, 3 minor), 0 killed.** All 0/2 refuted.

**Clusters/classes (doc numbering, severity-ordered):** #1, #5, #10 and #11
all attack the SAME M1 stamp/trim resolution block from four sides — the
falsy `or 0.0` short-circuit readmits the r19 #1 fabrication chain for
""/null/false/[]/{}/true (#1); destroyed head-cut evidence (torn
session.json, non-dict canonical → {}) recovers at a fabricated head 0 on
the live-sidecar route (#5); the created_at emit astimezone at fix.py:1790
is unguarded and crashes AFTER frames.csv is rewritten (#10); and a
stamp-side overflow at stamp+head_cut is misattributed to the head, turning
the designed recovery into a false-diagnosis refusal (#11). #2≡#6 are ONE
defect found independently by two lanes (payment-surface): the M4-preserved
accepted stamp on a REJECTED-counted root is a LABELS-only mark, and
preserving it strands the hours of a later-DELIVERED identical-bytes re-run
off every sheet, silently and permanently. #3 and #7 are M4's ''-sentinel
semantics self-defeating (payment-surface): any md5 write over a stored-''
slot reads 'unknowable' as 'changed' and full-clears the preserved stamps
(#3, incl. the heal sibling), and a second '' supersede over the ''-row M4
itself created does the same (#7) — the r19 #5 double-pay re-emerging on
both paths. #4 and #9 are M6's overflow degrade incomplete: the except arm
re-runs the identical overflow-prone addition uncaught (#4) and the
ended_at_utc emit astimezone sits outside the guard (#9). #8 is the r15
#6 / r19 #7-#9 arming-gate-invisible class recurring INSIDE the M set —
both deletion mutants of _v1_sidecar_started's degrade envelope proven
FULL-gate-green at exactly 821.

Machine-readable results incl. both refuter verdicts: session scratchpad
`r20-results.json`.

## Confirmed findings (severity-ordered)

### #1 [MAJOR] pipeline/fix.py:1668 (regressions-r19, refuters 0/2)

**M1's junk-trim gate is bypassed by falsy head_cut_s — the r19 #1 blocker chain (fabricated head-offset, silent delivered desync) survives for ""/null/false/[]/{}**

cb8cfd4's stamp/trim resolution parses the head cut as `head_cut = float(trim_meta.get("head_cut_s") or 0.0)` (pipeline/fix.py:1668). The `or` short-circuits on every falsy value, so float() never sees it: a PRESENT-but-junk head_cut_s of "" (float("") would raise ValueError), false, [], {} or null reads as head_usable=True with head_cut 0.0, and bool true reads as a fabricated 1.0s cut — while the same-family junk "abc" correctly refuses. Proven by execution against HEAD in a scratch copy (real ffmpeg fixture, sidecars with started_at 2026-08-10T15:33:55Z): CONTROL 'bogus'/'abc' -> 'refused as designed'; but head_cut_s='' / None / False / [] / {} beside an unusable stamp each printed "CONVERTED note='converted v1 -> v2 (800 rows); created_at_utc recovered from raw started_at_utc + head_cut'" with written created_at_utc=2026-08-10T15:33:55.000000Z == started_at exactly (a fabricated created==started plus a fixlog note falsely attesting ground-truth recovery); head_cut_s=True wrote 15:33:56 (fabricated +1.0s); and a GOOD stamp beside head_cut_s='' converted silently at head 0.0 on the live-contract route. This directly violates the commit's own contract ('PRESENT-but-junk is destroyed evidence and never silently reads as 0.0 on the live-contract route') and entry 90's defect clause ('a fabricated head-offset fact any consumer acts on'). The consumers are live: translator/v2.py:1052 re-bins raw events by head_us=(created-started) and pipeline/fix.py:983 derives retranslate's head_s from the same difference.

**Scenario:** A real v1 payload's canonical.created_at_utc is the raw started_at VERBATIM and its head cut is >=5s by construction (R19_FINDINGS r19 #1: translator/translate.py:334; trim.plan_cuts snaps the head to the first keyframe at/after 5s). A v1 bundle with usable sidecars arrives with trim {"head_cut_s": null} — the natural corruption, since JSON serializers (e.g. JS JSON.stringify) emit null for a non-finite number, the same player-file junk population r19 #4/#8/#10 were confirmed from ("5,0", bigints, list stamps). ARR_V1_FORMAT routes to FIX_V1_TO_V2; the falsy junk bypasses the refusal gate and the conversion ships created_at == started_at while the CSV was binned at head >=5s. Revalidation's _verify_against_raw re-bins at head 0, falsely condemns the still-correct CSV ('raw recomputation: dx/dy differs...') -> SYN_TS_NOT_PTS -> FIX_RETRANSLATE re-bins every event >=5s early -> the final checker passes ('CSV matches exactly') -> READY -> DELIVERED: checker-green input/video desync shipped to Odyssey — the exact executed chain of the iteration-19 BLOCKER, reachable through the gate M1 added to seal it, with the fixlog additionally recording a false 'recovered from raw started_at_utc + head_cut' attestation.

**Finder's proposed fix (vetting in-iteration per the ruling):** At pipeline/fix.py:1668, stop letting falsiness stand in for absence: read hv = trim_meta.get("head_cut_s") and treat only hv is None-or-key-absent as the documented v1-optional head-0.0 shape (or, stricter, only key-absent — a null where a number belongs is destroyed evidence too, matching the serializer-writes-null-for-NaN provenance); otherwise parse float(hv) directly so ""/false/[]/{}/true raise into head_usable=False and take the existing refuse-on-live-contract / keep-stamp-no-sidecar arms. Add the falsy family ("", null, false, [], {}, true) to test_v1_sidecar_route_refuses_unusable_trim beside "bogus"/"abc".

### #2 [MAJOR] pipeline/ledger.py:309 (payment-split, refuters 0/2)

**M4-preserved accepted stamp on a REJECTED-counted root strands its later-delivered hours off every sheet, silently and permanently**

ledger.py:284/309: the '' supersede over a real stored md5 preserves accepted_reported_at unconditionally (pay_clears=""), but supersede's only population is REJECTED/QUARANTINED slots (ingest.py:450/493), where that stamp means "reject LABEL counted, zero hours paid" — the tool-side rule recal_refix_reset.py:363-365 states verbatim ('A REJECTED node carrying an accepted mark had its LABELS counted, not hours'). Nothing else clears the stamp on the identical-bytes path (grep: only supersede/heal/deferral/reset tools write accepted_reported_at, none at delivery), and reports.py:904 skips any DELIVERED node carrying it. Probe (probe_m4_strand.py, all production calls): REJECTED root counted on sheet 1 ('2026-08-20,Op,p@x.com,0.5,...,black-frozen'); '' supersede -> "after '' supersede: state=DISCOVERED md5='' uploaded_stamp=... accepted_stamp=... dur_raw=1800.0"; real ingest.download over identical bytes -> "adj_markers=0", stamps stand; re-adjudication DELIVERS (duration_delivered_s=1750, state DELIVERED); sheets 2 and 3: "sheet2 counted: [] accepted: []", header-only CSV, "sheet2 stderr loud lines: ''". Control with the stamp cleared (pre-M4 semantics): accepted_out=[sid], kamla_accepted_hrs=0.49.

**Scenario:** A zip-class re-upload of a rejected session (the 'natural try again' M4's own commit names) carries identical bytes; fix_attempts was reset to 0 by the supersede and the re-run delivers — the routine flip class: VLM verdicts are non-deterministic, fix code improves every iteration, and the black-frozen recalibration proved 122/122 rejects false-positive on identical bytes. 0.49h of footage ships to Odyssey; the preserved stamp says 'counted', so accepted_due (reports.py:794) never fires, no sheet ever carries the hours, no loud line prints, and the player is paid nothing for delivered work. No recovery path exists: recal_refix_reset only selects REJECTED fix-failed rows, and recal_rebuild_reset --allow-reported would record the never-paid node as a PAID piece (recal_rebuild_reset.py:133-136), cementing the loss into the durable memory.

**Finder's proposed fix (vetting in-iteration per the ruling):** At both '' writers (ledger.supersede's zip_unknowable arm and the ingest.py:357 heal's ''-class arm), preserve uploaded_reported_at + duration_raw_s + tree_sealed_at as M4 rules, but clear accepted_reported_at when the node's current state is REJECTED — per the refix tool's own documented rule its accepted mark carries labels, not hours, so the worst cost of clearing is one duplicated reject LABEL on a later sheet if the re-run re-rejects (no money moves through a label), while preserving it silently forfeits shipped hours (money). If the label dup is unacceptable, record prev_state in the supersede breadcrumb and clear the stamp at DELIVERED-finalize time when it predates the generation's re-entry event.

### #3 [MAJOR] pipeline/ledger.py:284 (payment-split, refuters 0/2)

**Any md5 write over a stored-'' slot reads 'unknowable' as 'changed' and clears M4-preserved stamps — the r19 #5 double-pay re-emerges even when the breadcrumb proves the bytes identical**

zip_unknowable (ledger.py:284) requires bool(row['md5_video']), so two writers over a slot ALREADY holding the '' sentinel take the full clear (line 310-311) although the bytes are not KNOWN changed: (i) the scan's payload-switch re-upload (ingest.py:525, supersede(new_md5=vmd5)) with a REAL vmd5 over stored '', and (ii) a second zip re-upload (''-over-'': not new_md5, row md5 '' -> zip_unknowable False -> full clear). The quarantine heal has the same hole at ingest.py:357-361 (clears if vmd5 != existing['']). None of the three consults the prev_md5 breadcrumb that can prove identity, and after a real-md5 write the download deferral is skipped entirely (ingest.py:958 'if not row[md5_video]'), so nothing self-heals. Probe (probe_m4_realmd5_over_blank.py, REAL ingest.scan both times): sheet 1 counts '0.5,...,black-frozen'; zip rescan supersedes to '' with stamps preserved; garbage download -> DownloadError kind=quarantine -> QUARANTINED (stamps intact); plain-file rescan with vmd5 == breadcrumb prev_md5 (printed 'True') -> 'stamps=(None,None) dur=None'; re-reject -> sheet 2: '2026-08-21,Op,p@x.com,0.5,...,black-frozen', 'sheet2 stderr: \'\''. Second probe: ''-over-'' scan supersede -> "stamps=(None,None) dur=None" while the breadcrumb still names the counted real md5.

**Scenario:** A player's zip re-upload of a counted rejected session is corrupt (garbage archive -> quarantine, keeping md5=''); coached to the standard workflow, they re-upload the ORIGINAL plain files (byte-identical, listing md5 = the breadcrumb md5) at the same path — or an operator's typo-fix rename triggers the heal's real-over-'' arm. The scan-time writer clears the M4-preserved stamps as if bytes changed; the identical footage re-rejects (or re-delivers) and the next daily sheet credits the same 0.5h uploaded hours and the same reject label a second time across two SENT payment documents with zero loud lines — the exact silent double-pay M4 was RULED to close (entry 92's invited class: 'a double-pay that still slips through').

**Finder's proposed fix (vetting in-iteration per the ruling):** Wherever a writer meets a stored-'' slot, adjudicate against the latest prev_md5 breadcrumb instead of the '' row state: in ledger.supersede, when row md5 is '', look up the newest 'prev_md5=' event — a real new_md5 EQUAL to it preserves the payment columns (bytes provably identical), a different one keeps the full clear, and a falsy new_md5 over '' preserves (bytes still unknowable; the surviving breadcrumb keeps the download-time deferral sound). Apply the same breadcrumb compare to the heal's clears computation at ingest.py:357-361. By construction a ''-slot carrying stamps always has a breadcrumb (every ''-writer over a real md5 writes one), so no-breadcrumb cases have nothing to preserve.

### #4 [MAJOR] pipeline/fix.py:1520 (translator, refuters 0/2)

**M6's OverflowError degrade re-computes `ended` with the same huge duration and re-overflows uncaught, re-crashing the repair chain it was meant to save**

fix_sessionjson_recompute (fix.py:1502-1520) guards `ended = created + timedelta(seconds=info.duration_s)` with `except OverflowError`, but the except arm at line 1517-1520 sets `created = datetime.now(timezone.utc)` and then re-runs the IDENTICAL `ended = created + timedelta(seconds=info.duration_s)` (line 1520). M6 assumed the overflow always comes from a stamp near datetime.max (fixed by resetting `created` to now), but when `info.duration_s` is itself astronomically large (roughly > 2.5e11 s, i.e. > ~7900 yr, or inf/near-max-float), `now + timedelta(seconds=duration)` overflows REGARDLESS of `created`, so the except-arm addition raises OverflowError a second time — and that raise is inside the except block, uncaught. Probe against the real code (translator.video.probe stubbed in-process to duration_s=1e12, session.json created_at='2026-08-01T10:00:00.000000Z', conformant/parseable): `[M6] CRASH OverflowError out of the M6 degrade arm: date value out of range`. This is the exact 'overflow shape that still crashes the repair chain' that entry 94 names a finding.

**Scenario:** A session with any common fixable reason (STR_SENTINELS from a junk dx cell, INP_TOKEN_CASE, ARR_V1_FORMAT, etc.) reaches Phase III; every fix chain ends in FIX_SESSIONJSON_RECOMPUTE (and FIX_V1_TO_V2 calls it internally at fix.py:1793, FIX_SESSIONJSON_REWRITE for STR_SJ_INVALID). If the uploaded video.mp4 declares an absurd container duration (a crafted or corrupt mp4 — video bytes are player-supplied and only md5-verified, never bounded; V.probe at translator/video.py:51 does `float(v.get('duration') or ...)` with no clamp), V.probe returns a duration whose timedelta added to `now` overflows datetime.max. fix_sessionjson_recompute raises OverflowError; apply_fixes (fix.py:456-476) classifies it session-kind (OverflowError is not in the host tuple), burns the attempt; the crash is deterministic so it recurs, and after FIX_RETRIES the driver rejects the session 'fix retries exhausted (R2)' (run.py:573-575) under the bare fix-failed marker — an unpaid player and an infrastructure-shaped crash reported as a fault in their footage, the precise contract the degrade doctrine exists to prevent.

**Finder's proposed fix (vetting in-iteration per the ruling):** In the except-OverflowError arm, cap the duration before the second addition, e.g. compute `ended` defensively: after `created = datetime.now(timezone.utc)`, clamp `dur = min(info.duration_s, MAX_SANE_DURATION_S)` (the r12 #9 24h backstop already exists as a precedent) or wrap the second `ended = created + timedelta(...)` in its own guard that falls back to `ended = created + timedelta(seconds=0)` / a bounded value. The checker's own arithmetic already degrades on the same bigint/overflow input (v2.py:631, 1054-1055); the repair chain must too.

### #5 [MAJOR] pipeline/fix.py:1663 (translator, refuters 0/2)

**fix_v1_to_v2 fabricates a head-offset of 0 on the live-sidecar route when the head-cut evidence is destroyed (torn session.json / null / empty head_cut_s), while the sibling refuse-arm only fires for truthy-unparseable cuts**

M1 draws the safety line at head_cut PARSEABILITY, not at whether the evidence survived. head_cut/head_usable default to (0.0, True) at fix.py:1663; only a non-dict trim (1664) or a truthy-unparseable head_cut_s (1668-1671, ValueError) sets head_usable=False -> the typed refusal at 1689-1695. But an ABSENT trim block (canonical={} from a torn session.json via _read_session_json's {} fallback, or a non-dict canonical degraded to {} at 1614-1618) and a FALSY-junk head_cut_s (null or '' -> `float('' or 0.0)` short-circuits to 0.0 at 1668) both leave head_usable=True with head_cut=0.0. On the live-sidecar route (1678-1688) this RECOVERS created_out = started_at + 0.0. Genuine HumynCapture v1 deliveries apply the implicit 5s head trim and store canonical.created_at_utc = raw started_at with the trim recorded SEPARATELY in canonical.trim.head_cut_s (translator/translate.py:334,308), so the true delivered created_at is started_at + 5s. Recovering created = started_at fabricates a 0s head offset — the exact head-offset-fabrication class entry 90 rules a finding. Probe against the real fix_v1_to_v2 (torn session.json '{"canonical": {"created_at', usable raw sidecars at work root): CONVERTED with note 'created_at_utc recovered from raw started_at_utc + head_cut', delivered created_at_utc='2026-08-01T10:00:00.000000Z' == raw started_at (fabricated head 0). A second probe drove translator.v2._verify_against_raw directly: correct head (started+5s) PASSes 'CSV matches exactly on all 1800 rows'; the fabricated head-0 FAILs 'raw recomputation: dx/dy differs from CSV in 152 frames (max run 151, 8.44% of rows)'.

**Scenario:** A v1 bundle (key_binding.json present -> check_session_v2 early-returns 'key_binding.json present' at v2.py:702-705 WITHOUT ever reading session.json -> maps to ARR_V1_FORMAT -> FIX_V1_TO_V2) arrives with a torn/partial session.json (kill -9 or disk-full during the original translate write, or a canonical that lost its trim block) plus its usable raw sidecars (inputs.jsonl+metadata.json, the standard player upload = out/ folder + raw files). fix_v1_to_v2 recovers created_at = started_at (head 0) for footage that was really 5s-trimmed. On re-validation _verify_against_raw computes head_us = created-started = 0 and FAILs the off-by-one check (proven) -> SYN_TS_NOT_PTS; has_raw is True (sidecars now in raw/, started_at parseable) so plan_fixes routes FIX_RETRANSLATE, which re-bins raw events at the fabricated head_s=0 onto the trimmed video: the first 5s of raw input (belonging BEFORE the clip) is mapped onto the clip and the clip's last 5s of real input is dropped, and the re-binned frames.csv now self-consistently PASSes the raw verify (both at head 0) — a 5s input/video desync shipped as delivered training data (silent corruption); if the desync instead trips another reject the session is wrongly rejected. Either way M1's anti-fabrication contract is violated for destroyed head-cut evidence.

**Finder's proposed fix (vetting in-iteration per the ruling):** Treat destroyed head-cut evidence the same as truthy-unparseable on the live-sidecar route: when the raw contract is live (started is not None) and the head cut cannot be affirmatively read as a real number — i.e. the trim block is ABSENT because session.json/canonical was unreadable, or head_cut_s is null/'' — refuse (the existing FixFailed at 1689-1695) rather than recovering at head 0. Distinguish a genuinely-absent trim on a READABLE, well-formed session.json (do_trim=False, head 0 truly correct) from an absent trim caused by an unreadable session.json (route _read_session_json's {}-fallback state through to this decision), and stop `float(x or 0.0)` from silently swallowing null/'' head_cut_s into 0.0.

### #6 [MAJOR] /Users/adnaan/Documents/hl-projects/hl-gamedata/pipeline/ledger.py:309 (ops-tools, refuters 0/2)

**M4 '' supersede preserves a REJECTED node's labels-only accepted stamp; if the identical-bytes re-run then DELIVERS, the delivered hours are silently stranded off every sheet forever**

M4 (e9f913b) makes ledger.supersede preserve uploaded_reported_at + accepted_reported_at + duration_raw_s + tree_sealed_at whenever new_md5 is '' over a real stored md5 (pipeline/ledger.py:284, 309-311: pay_clears = '' if zip_unknowable). But ingest's '' supersede caller fires ONLY on REJECTED/QUARANTINED slots (pipeline/ingest.py:493-498, 525), so on the production population the root's own accepted_reported_at at supersede time is a LABELS mark ('a REJECTED node carrying an accepted mark had its LABELS counted, not hours — it is not a paid piece' — the ruled doctrine written verbatim in tools/recal_refix_reset.py:360-364, which CLEARS that mark on its own re-runs so recovered hours become payable; the pre-M4 supersede cleared it too). Labels carry no hours, so preserving this mark protects zero money — while build_sheet_rows suppresses ANY node carrying it: pipeline/reports.py:904 'if sealed or n["accepted_reported_at"]: continue', and every re-entry arm (late needs no uploaded stamp — preserved; accepted_due/uncountable_due need _tree_has_uncounted_accepted — False, reports.py:658-673) is closed. The identical-bytes case is exactly the one the download adjudicator (ingest.py:966-987) is designed NOT to clear, so nothing ever removes the mark. PROVEN BY EXECUTION against HEAD in a scratch copy (probe test_probe_m4.py, 1 passed in 0.32s, real send_daily_report_if_due + real ledger.supersede + real ingest.download over planted identical bytes + real build_sheet_rows): day-1 sheet counts the REJECTED root's 0.5h uploaded + black-frozen label and stamps both marks; '' supersede leaves state=DISCOVERED with both stamps preserved; download of identical bytes writes no ZIP_ADJ_CHANGED and the stamps stand; the re-run delivers 1700s; day-2 sheet output: 'DAY2 sheet mentions player: False', 'DAY2 stderr mentions sid: False', zero payment-relevant stderr lines — the 0.47 delivered hours reach no sheet, silently. CONTROL (the refix doctrine / pre-M4 behavior — mark cleared): 'DAY3 sheet mentions player: True', row '2026-08-22,Op,p@x.com,0.0,0.0,0.47,0.0,,,0.0,0.47' — the money is real and only the preserved mark withholds it. This is a REGRESSION from the pre-M4 tree (at 06ecd72 the supersede cleared the mark unconditionally and these hours were paid) introduced by the unreviewed M-set; entry 92's own defect clause reserves 'hours stranded by a preserved stamp' as a finding.

**Scenario:** A player's session is rejected (e.g. a fix-failed or VLM-class reject) and the 14:00 daily sheet counts its uploaded hours + reject label, stamping uploaded_reported_at and accepted_reported_at on the REJECTED root. The player does the natural 'try again' M4's own commit message describes: re-uploads the SAME zip (identical bytes, new Drive ctime) into the same folder. ingest's REJECTED-slot zip branch calls supersede(new_md5='') — post-M4 both stamps survive onto the fresh DISCOVERED slot. The download backfill hashes identical bytes, correctly writes no ZIP_ADJ_CHANGED, and the stamps stand. The re-run gets a FULL fresh fix budget (supersede resets fix_attempts=0) under newer checkers/tolerances and a nondeterministic VLM — the exact recovery premise the refix tool and the 08-18 rebuild (which flipped most of 1396 re-run sessions to delivered, incl. 122/122 black-frozen false rejects on unchanged bytes) are built on — and this time it DELIVERS. The footage ships to Odyssey on Drive II, but the preserved accepted mark makes build_sheet_rows skip the node on every future sheet, no re-entry arm fires, and not one loud line prints anywhere (proven: empty stderr). The player is never paid for delivered footage, invisibly — the exact conservation breach ('footage that shipped was never paid for') the RULED stamp split existed to close, now re-opened on the zip re-upload path.

**Finder's proposed fix (vetting in-iteration per the ruling):** In ledger.supersede's zip_unknowable arm, keep preserving uploaded_reported_at + duration_raw_s + tree_sealed_at (the money-bearing marks M4 correctly protects) but NULL accepted_reported_at when row['state'] == 'REJECTED' — mirroring recal_refix_reset's ruled labels-mark clear (its lines 360-378). Cost is bounded and money-free: an identical-bytes re-zip that re-REJECTS re-prints one label on one later sheet (labels carry no hours, so no double-pay is possible), while a re-run that DELIVERS pays the genuinely new hours. Since the '' caller can never fire on a DELIVERED row, no hours-bearing accepted mark is ever cleared by this. Pin it with a failing-side test: REJECTED-counted root -> identical-bytes '' supersede -> real download -> _mark_delivered -> next sheet MUST pay the hours (my probe's day-2 assertion inverted).

### #7 [MAJOR] pipeline/ledger.py:284 (tests-coverage, refuters 0/2)

**M4's zip_unknowable guard self-defeats: a second '' supersede over the ''-md5 row the first one created full-clears the preserved payment stamps — proven end-to-end double-pay; the M4 test cohort never seeds the ''-row shape the fix itself creates**

ledger.py:284 (M4, e9f913b) computes `zip_unknowable = (not new_md5) and bool(row["md5_video"])`. The first '' supersede sets md5_video='' while preserving the stamps (the M4 fix), so the very row shape M4 creates — stamps + '' md5 — fails `bool(row["md5_video"])` on the NEXT '' supersede and takes the full-clear branch (ledger.py:309-311: duration_raw_s/uploaded_reported_at/accepted_reported_at/tree_sealed_at all NULLed) although the bytes are exactly as unknowable as the first time; it also writes no prev_md5 breadcrumb (ledger.py:320 guard). Proven in a scratch copy of HEAD with every supersede driven through PRODUCTION ingest.scan (probe pipeline/tests/probe_m4_scan_driven.py, scratch only): sheet 1 counts the root (stamps land) -> root REJECTED -> zip re-upload #1 at the same path -> scan branch ingest.py:493-528 calls supersede(new_md5='') and the probe asserts stamps+duration PRESERVED (M4 works once) -> folder vanishes -> production vanish arm ingest.py:751-762 quarantines the DISCOVERED row -> zip re-upload #2 at the same path (newer ctime/size) -> the same scan branch supersedes with '' again and the probe printed `after scan-driven 2nd '' supersede: {'uploaded': None, 'accepted': None, 'dur': None}` -> _download_with_bytes with the ORIGINAL payload: `assert not _adj_events(ledger, sid)` passed (identical bytes, deferral ingest.py:956-987 rightly declines to clear — and restores nothing) -> next-day send printed `SECOND SHEET ROW: ['2026-08-21,Op,p@x.com,2.0,0.0,0.0,0.0,,,2.0,0.0']` — the same footage's uploaded hours counted on a second SENT sheet; probe result `1 passed`. Tests-coverage half: all three M4 tests in pipeline/tests/test_r_loop19.py (lines 275-371) seed a REAL md5 before superseding (`hashlib.md5(payload).hexdigest()`, `md5(b"old-bytes")`, `"a"*32`), so the ''-over-'' shape has no test and the arming gate cannot see this hole.

**Scenario:** Entry 92's own defect clause ("a double-pay that still slips through"). Mainstream chain needing no ruled dead-end: a zip root is REJECTED and counted on a sent sheet (uploaded hours + reject label — the exact M4 population); the player re-zips at the same path (supersede #1: '' over real md5, stamps correctly preserved); the download finds a bad archive -> DownloadError kind=quarantine at ingest.py:920-922 fires BEFORE the md5 backfill at :987, so the row sits QUARANTINED with md5 still ''; the player re-zips the archive correctly at the same path — the natural correction the branch's own comment invites ("a bad-archive quarantine is exactly what a re-upload corrects", review-r3 #17) — and supersede #2 ('' over '') full-clears the preserved stamps with zero byte evidence. The fixed zip carries the identical footage, the deferral declines to clear (nothing left to protect) and restores nothing, and the next sheet re-counts the same hours: money paid twice across two sent payment documents, silently. The probe proved the sibling chain (vanish-arm quarantine) end-to-end through production scan/download/sheet code with real output.

**Finder's proposed fix (vetting in-iteration per the ruling):** In ledger.supersede make unknowable mean unknowable regardless of the stored md5: `zip_unknowable = not new_md5` (preserve the payment columns for EVERY '' writer), and guard only the breadcrumb on a real prior md5: `if zip_unknowable and row["md5_video"]: detail += f"; prev_md5={row['md5_video']}"` — the deferral's ORDER BY ts DESC lookup then still lands on the last REAL md5 breadcrumb and byte adjudication is unchanged. Add the missing cohort shape to test_r_loop19.py: after the first '' supersede, quarantine (or reject) the row and supersede with '' again, asserting uploaded_reported_at/accepted_reported_at/duration_raw_s survive; the current code fails that test.

### #8 [MAJOR] pipeline/fix.py:1574 (tests-coverage, refuters 0/2)

**M1's _v1_sidecar_started degrade envelope is arming-gate-invisible: both deletion mutants (errors='replace' and the OSError/JSONDecodeError catch) pass the FULL suite at exactly 821 — the identical r19 #7/#8/#9 class, introduced by the commit that answered those findings**

The M1 commit cb8cfd4 added _v1_sidecar_started (fix.py:1551-1579), whose metadata read carries the same two degrade arms M8 just pinned at has_raw_sidecars — `mp.read_text(encoding="utf-8", errors="replace")` at fix.py:1574 and `except (OSError, json.JSONDecodeError): return None` at :1575-1576 — but no test exercises either. Proven by two SEPARATE full-suite mutation runs in a scratch copy of HEAD outside the repo (each mutant applied alone, suite run, tree restored): (1) dropping errors="replace" at :1574 -> `821 passed in 158.73s` — byte-identical to my unmutated scratch baseline `821 passed in 161.38s` and to the recorded Mac gate; under this mutant a probe building the suite's own _v1_work with an unusable stamp ("not-a-date"), usable trim {head_cut_s: 8.0}, and root sidecars whose metadata is JSON-valid-under-replace but contains a latin-1 byte (b'..."player": "Jos\xe9"') failed with `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xe9 in position 72` (UnicodeDecodeError is a ValueError but NOT json.JSONDecodeError, so :1575 cannot catch it), while at HEAD the identical probe passed with the recovered stamp `2026-08-10T15:34:03.000000Z`; (2) deleting the whole try/except at :1573-1576 -> `821 passed in 157.58s`; under it a probe with a junk stamp and PRESENT-but-truncated metadata ('{"recording": {"started_at') failed with json.JSONDecodeError, while HEAD degrades to the designed omit-and-synthesize (probe passed, _TS_RE-conformant stamp). Every test that reaches the probe (test_r_loop19.py's _v1_with_sidecars, lines 97-121) writes pure-ASCII, fully-parseable metadata.

**Scenario:** The probe runs inside fix_v1_to_v2, which apply_fixes wraps at fix.py:456-473: UnicodeDecodeError/JSONDecodeError are outside the host tuple -> kind="session", attempt charged, and it recurs deterministically on attempt 2 (nothing repairs root/raw metadata between attempts) -> terminal "fix retries exhausted" reject of a session HEAD would have recovered (mutant 1's population is the exact scenario the M1 recovery arm exists for) or degraded-and-delivered (mutant 2 = the L2 'unusable equals missing' population on the v1 route): unpaid player hours under the bare fix-failed marker. The probe is consulted precisely when the canonical stamp/trim is already junk — the degraded-payload population where non-UTF-8 or corrupt metadata plausibly co-occurs (r19 #9 established this population as MAJOR, refuters 0/2, and the code's own comment at the retranslate read calls non-UTF-8 metadata reachable). A future simplification of the new probe — the loop's canonical one-token-revert class, proven gate-green here at 821/821 — ships through the flip's arming gate undetected.

**Finder's proposed fix (vetting in-iteration per the ruling):** Tests-only, the M8 recipe at the M1 site: add two failing-side pins to the M1 cohort in pipeline/tests/test_r_loop19.py — (a) _v1_work with an unusable stamp + usable trim + root sidecars whose metadata.json is written via write_bytes with a latin-1 byte inside a string value and a parseable started_at_utc, asserting the conversion completes with the 'recovered' note and the exact recovered stamp; (b) _v1_work with a junk stamp + root inputs.jsonl + truncated metadata.json, asserting the conversion completes and the written created_at_utc matches translator.v2._TS_RE. Both mutants proven gate-green above fail exactly their own pin (verified with the probe files in the session scratchpad).

### #9 [MINOR] pipeline/fix.py:1532 (fix-validate, refuters 0/2)

**M6 hole: ended_at_utc emit astimezone is outside the OverflowError guard — the repair chain still crashes on a checker-conformant near-max stamp**

M6 (78e384e, entry 94) guarded fix_sessionjson_recompute's re-emit astimezone and the `ended = created + timedelta(...)` addition (fix.py:1502-1520), but the emit at fix.py:1532 — `ended_at_utc=ended.astimezone(timezone.utc).strftime(...)` inside s.update — is outside that try. A stamp like '9999-12-31T20:00:00-05:00' matches _TS_RE (verified: `_TS_RE.match(...) -> True`, translator/v2.py:540 allows [+-]HH:MM offsets) and parses, so parsed_ok=True skips the guarded re-emit; the addition survives (20:00 + clip duration stays inside year 9999 in naive fields); but the UTC conversion of `ended` crosses year 10000. Proven by execution against a real probed video: `fix.fix_sessionjson_recompute(work, 'kamla')` raised `OverflowError: date value out of range` at fix.py:1532 (traceback pasted from probe), while both M6-pinned shapes ('9999-12-31T23:59:59.000000Z', '9999-12-31T23:59:59-05:00') correctly degraded to a synthesized stamp in the same run. apply_fixes classifies OverflowError session-kind (fix.py:471-473, not in the host tuple), so the attempt burns; the crash precedes the tmp write at fix.py:1545, so session.json is unchanged and attempt 2 crashes identically. This is entry 94's own defect clause: 'an overflow shape that still crashes the repair chain'.

**Scenario:** A v2 session.json arrives with created_at_utc '9999-12-31T20:00:00-05:00' (regex-conformant, parseable — the exact junk family r19 #12 modeled, whose M6 test pins '9999-12-31T23:59:59-05:00') plus any ordinary STR_SJ_INVALID defect, e.g. platform 'Windows'. Checker FAILs 'platform not in spec enum' -> STR_SJ_INVALID -> bin 2 -> plan [FIX_SESSIONJSON_REWRITE, FIX_SESSIONJSON_RECOMPUTE]. The rewrite crashes at fix.py:1532 on attempt 1 with nothing written; revalidation re-derives the identical verdict; attempt 2 crashes identically; run.py:573-575 rejects 'fix retries exhausted (R2)' — a wrongful terminal reject of sound footage under the bare fix-failed marker (all stored reasons fixable), the exact surface M5 just made truthful. Since FIX_SESSIONJSON_RECOMPUTE is appended to EVERY plan with steps (fix.py:405-406), the same stamp also burns an attempt on any unrelated fix chain (hygiene, bleed, tsrepair).

**Finder's proposed fix (vetting in-iteration per the ruling):** Move the ended-string emission inside the existing OverflowError guard: compute `ended_str = ended.astimezone(timezone.utc).strftime(...)` in the try block at fix.py:1502-1507, and in the except arm (fix.py:1508-1520) recompute it from the synthesized `created` — then pass ended_str to s.update. Pin with the '9999-12-31T20:00:00-05:00' shape (addition survives, conversion crosses) beside the two existing M6 pins.

### #10 [MINOR] pipeline/fix.py:1790 (fix-validate, refuters 0/2)

**M1 hole: fix_v1_to_v2's created_at_utc emit astimezone is unguarded — the v1 route still crashes on a junk shape, after frames.csv is already rewritten**

M1 (cb8cfd4, entry 90) put OverflowError on 'every parse/arithmetic guard' in fix_v1_to_v2's stamp/trim resolution (fix.py:1661, 1671, 1674-1677, 1685-1687), but the emit at fix.py:1790 — `new_s["created_at_utc"] = created_out.astimezone(timezone.utc).strftime(...)` — has no guard, and it runs AFTER _write_csv(work, V2_FRAME_COLS, out) at fix.py:1782. A parseable aware negative-offset stamp near datetime.max whose stamp+head_cut addition survives (guarded at 1674) overflows only at the UTC conversion. Proven by execution: canonical.created_at_utc='9999-12-31T20:00:00-05:00' + trim {'head_cut_s': 5.0}, no sidecars (the route M1 declares all-degrade) -> `OverflowError: date value out of range` at fix.py:1790; post-crash the work dir shows frames.csv already at 36 columns (v2) while session.json is still v1-canonical — the route crashed mid-write instead of degrading (no-sidecar contract) or refusing byte-identically (sidecar contract). The same stamp crashes the sidecar-usable route identically (created_out = stamp + head_cut there too). Entry 90's defect clause names this exactly: 'a junk shape that still crashes the route' — and on this route no other plan step can ever rescue a crash (the checker early-returns on key_binding.json, per the function's own docstring), so the reject is guaranteed.

**Scenario:** A v1 upload (ARR_V1_FORMAT) carries canonical.created_at_utc '9999-12-31T20:00:00-05:00' and a sane trim — the same player-typed junk family M1 was built for (its tests model bigints, '1e999', 1e18, year-9999 stamps). Attempt 1: plan [FIX_V1_TO_V2, FIX_SESSIONJSON_RECOMPUTE]; the conversion rewrites frames.csv to v2 then crashes session-kind at fix.py:1790. REVALIDATING re-derives ARR_V1_FORMAT (session.json still carries canonical); attempt 2 re-runs the conversion over the half-converted dir and crashes at the same line; attempts exhaust -> REJECTED 'fix retries exhausted (R2)': a wrongful terminal reject of a session that, with the emit guarded, would convert (no-sidecar: omit-and-synthesize; sidecar: recover from started+head_cut) and deliver.

**Finder's proposed fix (vetting in-iteration per the ruling):** Resolve the emitted STRING inside the M1 resolution block before any write (fix.py:1672-1701): format created_out.astimezone(timezone.utc) there under `except OverflowError` — sidecar route: treat as unusable stamp (recover from started + head_cut, else typed refusal); no-sidecar route: omit and let fix_sessionjson_recompute synthesize. That also restores the byte-identical property for every refusal shape. Pin with the addition-survives/conversion-crosses stamp on both routes.

### #11 [MINOR] pipeline/fix.py:1677 (fix-validate, refuters 0/2)

**M1 blame misattribution: a stamp-side overflow at stamp+head_cut sets head_usable=False, skipping the designed recovery arm and refusing with a false diagnosis**

In fix_v1_to_v2, `created_out = stamp + timedelta(seconds=head_cut)` (fix.py:1673-1677) catches OverflowError by setting head_usable=False — but that addition overflows for TWO distinct causes: a huge-but-finite head_cut (head genuinely junk) OR a parseable stamp within head_cut of datetime.max (stamp junk, head fine). The code attributes both to the head, so on the sidecar-usable route the recovery arm at fix.py:1683-1687 (`if head_usable: created_out = started + timedelta(head_cut)`) is skipped and the typed refusal at fix.py:1689-1695 fires, naming canonical.trim. Proven by execution: canonical.created_at_utc='9999-12-31T23:59:59Z' + trim {'head_cut_s': 5.0} + usable sidecars (started 2026-08-10T15:33:55Z) -> FixFailed 'canonical.trim head_cut_s unusable while usable raw sidecars are attached... repair session.json's canonical.trim before converting' — a false statement, head_cut_s is 5.0. Control in the same run: the identical dir with created 'not-a-date' RECOVERS ('created_at_utc recovered from raw started_at_utc + head_cut', delivered stamp 2026-08-10T15:34:00.000000Z). So the parseable-but-arithmetic-unusable stamp is the one member of the unusable-stamp class (whose RULED disposition, per entry 90 and the committed pin test_v1_sidecar_route_recovers_unusable_stamp, is recovery from ground truth) that instead takes the unusable-HEAD refusal.

**Scenario:** A v1 upload with usable raw sidecars carries a sane trim and a near-max parseable created stamp (the r19 junk family). Both fix attempts raise the identical typed refusal; the session terminally rejects 'fix retries exhausted' even though started_at_utc + head_cut — the exact recovery M1 shipped, proven working on the unparseable-stamp twin — would have produced a correct stamp and a deliverable session. The fixlog and coaching direct the operator to repair canonical.trim, a field that is provably fine, so the false diagnosis also misleads whoever triages the reject.

**Finder's proposed fix (vetting in-iteration per the ruling):** In the except OverflowError arm at fix.py:1676-1677, disambiguate: if `timedelta(seconds=head_cut)` itself constructs (the head is representable), set stamp = None (the stamp is the junk side) instead of head_usable = False — the existing flow then recovers on the sidecar route and omit-and-synthesizes on the no-sidecar route; only a head whose own timedelta/addition with `started` overflows keeps the canonical.trim refusal. Pin both directions: near-max stamp + sane head recovers; sane stamp + 1e14 head still refuses.

