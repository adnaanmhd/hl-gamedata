# R16_FINDINGS.md — review iteration 16, findings of record

**Run 2026-08-19** by the I1–I8 executor session via the workflow
snapshot `tools/review/flip-review-iter16.js` (FIX-IN-ITERATION pass per
R8_IMPLEMENTATION_PLAN §4, RULED 2026-08-19: confirmed findings are
fixed by the executor in this same iteration; iteration 17 reviews those
fixes; the stream stops at the first quiet iteration). 7 finder lanes,
2-vote refute, 21 agents (21 completed, 0 errors, ~2.70M subagent
tokens, run `wf_ebb4ece9-f00`). HEAD `4dc37b4` (code HEAD `fd3ea1f` =
I1–I8 + floor 761 `35a0433`; suite 765/765 both hosts).

**Verdict: NOT QUIET pre-fix — 7 raised → 7 confirmed
(3 major / 4 minor, 0 blockers), 0 killed.** Two findings
survived at 1/2 refuted (#6, #7 — a finding dies only at 2/2); the rest
0/2.

**Known duplicates/clusters:** #1 ≡ #4 (the I5 lone-surrogate
session_id hole — found independently by regressions-r15 and
fix-validate). #6/#7 are tests-coverage pins for I2/I4 sweep halves
that survive full revert with the arming gate green (the r15 #6/#9
unpinned-half pattern recurring inside the fix set that closed it).
#2, #3, #5 are pre-existing (#2 the fail-OPEN daily resume scan, #3
the keybind-blind INP_OSKEYS trigger, #5 the comma-arm sibling of the
I1-ruled caseless exemption).

Machine-readable results incl. both refuter verdicts: session
scratchpad `r16-results.json`.

## Confirmed findings (severity-ordered)

### #2 [MAJOR] pipeline/run.py:1196 (payment-split, refuters 0/2)

**Day-agnostic resume scan fails OPEN on a transient listing error — fresh send runs past an older pending day and the same hours land on two sent payment sheets**

run.py:1193-1197 reads `try: day_dirs = sorted(p for p in cfg.reports_dir.iterdir() if p.is_dir()) / except OSError: day_dirs = []` — an empty list is indistinguishable from 'no pending days', so a transient OSError (EMFILE class: iterdir is the only fd-allocating call in the sequence; the stat-based guards around it don't need fds) silently skips the resume scan and falls through to the fresh path. This is the exact class r-loop 12 #13 fixed in reports.pending_daily_send/pending_regen_send ('could not check must never read as checked clean', R12_FINDINGS.md:128-130, accepted entry 53) — the fix covered the two reports.py listing sites and the reset-tool gates but missed this third, sibling listing inside send_daily_report_if_due itself, the one that ORDERS resume-before-fresh (the r9 #6/#21 invariant). The regen guard at run.py:1177 fails closed on the same directory two calls earlier, so only an asymmetric transient (first listing succeeds, second throws) reaches it — but adjacent lines (run.py:1029-1056, r-loop 11 #10 / accepted entry 38) were fixed precisely because such transients were judged real on this code path. PROVEN BY EXECUTION against a scratch ledger driving the real send_daily_report_if_due with only telegram faked and one injected OSError on the scan's iterdir: day-N send crashes at the Telegram message (durable record written, NOTHING stamped — the standard pending shape); next day's 14:00 tick hits the one-off OSError at the scan; the fresh path builds today's sheet and re-counts the pending day's unstamped root via the late-arrival guard (printing only the routine '[sheet] LATE ARRIVAL: X ... counted in the current sheet (conservation)' line); the pending day later resumes and re-sends its own CSV crediting the identical root. Probe output: 'day21 sheet totals: (1.0, 0.83, [])' then 'day20 sheet totals: (1.0, 0.83, [])', 'payment documents actually delivered: [payment-2026-08-21.csv, payment-2026-08-20.csv]', 'hours credited across SENT sheets: uploaded 2.0 accepted 1.66 (real footage: 1.0 up / 0.83 acc) -> DOUBLE-PAY'. A second run proved the flaky tick's own sends need NOT succeed: with Telegram still down at the flaky tick, today's record+CSV durably capture the double-counted root ('day21 record now ALSO pending: True [X]') and the resume machinery later faithfully delivers BOTH sheets ('-> credited 2.0 uploaded hrs for 1.0 real'). No output anywhere flags the overlap; both stamps re-land idempotently (CAS matches — md5 unchanged), so no future sheet ever corrects it.

**Scenario:** A Telegram outage interrupts the 14:00 IST daily send after the durable record is written but before the stamps (the message send is the first network call — the common failure point), and persists past IST midnight — exactly the scenario the day-agnostic scan was built for (r9 #6/#21); alternatively any older wedged/pending day with unstamped counted roots, which by design persists for days awaiting a human. From then on, EVERY 600s daily tick performs the two back-to-back reports-dir listings; the moment one tick's second listing throws a transient OSError (fd exhaustion from the concurrent D/V/U threads — the driver is fd-hungry and the codebase already defends this class on the adjacent resume lines) while the first survived, the fresh path runs for today, re-counts the pending day's unstamped cohort as late arrivals, and writes/sends today's sheet. The older day's resume then re-sends its own CSV crediting the same roots. Two authoritative payment documents each pay the same uploaded+accepted hours — a silent double-pay (the unrecoverable direction per the ruled money-safety doctrine), discoverable only by hand-diffing sheets.

**Finder's proposed fix (vetting in-iteration per the ruling):** Make this listing fail CLOSED like its two r12 #13 siblings: replace the inline try/except with day_dirs = reports._report_day_dirs(cfg), and when it returns None print a loud '[daily] reports dir unlistable — cannot order resume-before-fresh; retrying next tick' to stderr and return False (transient-retry doctrine of r-loop 11 #10, no wedge). A FileNotFoundError (reports dir genuinely absent, first-ever send) already yields [] from _report_day_dirs and keeps the fresh path reachable.

### #3 [MAJOR] pipeline/validate.py:745 (fix-validate, refuters 0/2)

**INP_OSKEYS trigger is keybind-blind: bound OS/F-keys create a no-op fix loop ending in a wrongful terminal reject**

map_reasons raises INP_OSKEYS (blocking, fixable) whenever the engine inventory's os_keys is non-empty (validate.py:745-748), and the engine counts os_keys purely by pattern — tools/analyze_sample.py:236 `if _OS_KEY_PAT.search(t) or _FKEY_PAT.match(t)` with no keybind parameter (inventory() at :203 takes none). But the planned fix, FIX_KEY_HYGIENE (fix.py:159-160), deliberately KEEPS bound OS/F-keys: keys.py:86-87 `if s in strip_set and s not in bound: return None` and the r11-F4 design resolves the session's own raw/keybind.json (fix.py:1050-1058), and the locked project rule is 'strip OS/system keys aggressively ... F-keys unless bound'. So for a session whose keybind binds an OS-pattern key, the fix is a provable no-op and the reason re-fires every revalidation. Probe with the REAL modules (engine inventory + map_reasons + fix_key_hygiene on a work dir whose raw/keybind.json binds interact->insert): pass-1 `engine os_keys: {'Insert': 8}` -> reasons `[('INP_OSKEYS', True, True)]` -> plan `['FIX_KEY_HYGIENE', 'FIX_SESSIONJSON_RECOMPUTE']` -> hygiene note `stripped 0 tokens` -> pass-2 `engine os_keys: {'Insert': 8}` -> `INP_OSKEYS re-fired after its own planned fix: True`. The retranslate route no-ops identically (_v2_rows keeps bound+credited tokens, verified in probe p1). R11 finding #11's proposed fix explicitly named 'the INP_OSKEYS trigger' and 'alternatively (or additionally), make the engine's os_keys inventory [keybind-aware]' — the landed F4 fix (memory entry 37) covered only the hygiene-side resolution; the trigger was never made bound-aware and no later loop raised it (no os_key hits in R13/R14/R15 findings).

**Scenario:** A player's keybind.json binds any token matching the OS pattern — insert, caps_lock, an F-key (real rebind targets; OW allows arbitrary keyboard rebinds) — and they press it during play. The standard upload (out/ + raw sidecars, per the recording guide) translates fine: normalize_event_key keeps the bound key, the checker passes it (it has case or is exempt). First validation raises INP_OSKEYS, bin 2. Attempt 1: FIX_KEY_HYGIENE resolves the session keybind, keeps the bound key, 'stripped 0'. Revalidation recomputes the same inventory from the unchanged CSV, re-raises INP_OSKEYS. Attempt 2: identical no-op. Next FIX_QUEUED pick: fix_attempts >= FIX_RETRIES -> REJECTED 'fix retries exhausted' (run.py:573-575 / continuous.py:901-902), surfaced to ops under the bare fix-failed marker (all reasons fixable), after three paid Gemini sweeps. Every session from that player takes the same wrongful terminal reject — a spec-conformant delivery under the locked 'unless bound' rule, and an unpaid player.

**Finder's proposed fix (vetting in-iteration per the ruling):** Make the trigger bound-aware, mirroring the F4 chain: in validate_session/_build_aux resolve the session binding exactly as fix_key_hygiene does (resolve_keybind on work/raw/keybind.json with the ledger/expected game, plus KEYBIND_PATCHES; built-in fallback when no sidecar) and put its bound_literals in aux; in map_reasons filter `inv['os_keys']` to tokens whose key_canonical is NOT bound before emitting INP_OSKEYS (emit an advisory for the filtered bound ones so the dossier still shows them). Unbound OS-key pollution keeps today's blocking reason and hygiene still clears it.

### #5 [MAJOR] translator/v2.py:811 (translator, refuters 0/2)

**Comma-key bind terminal-rejects through the same no-op hygiene loop I1 just fixed for ';'**

check_session_v2's bad-token clause (translator/v2.py:811: `if not t or t != t.strip() or "," in t or " " in t or (t.lower() == t and ...)`) still rejects the comma KEY itself: the `"," in t` arm fires on the exact token the translator's own writer emits for a comma bind, because key_display(',') == ',' (v2.py:103-108, single-char tok.upper(); verified by execution: key_display(",") = ','). I1 (bfd96b7) exempted caseless symbol keys from the CASE arm only and left the comma arm untouched, so the r15 #4 defect class is still live one arm over. Proven end-to-end on HEAD: a Kamla bundle with keybind {"interact": ",", "move_up": "w"} translated via the real translate_bundle_v2 delivered rows (',|W', 'interact|move_up') — real gameplay data, actions resolve fine — and check_session_v2 returned "FAIL: non-v2 key tokens in input_keys: [',']". pipeline/validate.py:83 maps "non-v2 key tokens" -> INP_TOKEN_CASE (blocking, fixable); pipeline/fix.py:159-160 routes INP_TOKEN_CASE to FIX_KEY_HYGIENE unconditionally (even with raw sidecars — never retranslate, and retranslate would re-emit ',' through the same _v2_rows/key_display anyway). Running the real fix_key_hygiene on the delivered session printed "hygiene: stripped 0 tokens, resolved 0 bleed frames, actions re-resolved" and the re-run checker printed the identical "FAIL: non-v2 key tokens in input_keys: [',']" — a provably deterministic no-op fix loop (key_canonical(',')==',', normalize_event_key(',')==',', ',' is bound and credited, key_display(',')==','). Both fix attempts burn, then REJECTED "fix retries exhausted". Entry 71 explicitly reserves this as a finding: "a symbol key stripped or re-FAILed anywhere in the chain".

**Scenario:** A player binds the comma key — a top-tier real-world rebind (lean, weapon-switch, push-to-talk; the same lefty/non-QWERTY population whose ';' bind was r15 #4, confirmed 0/2 by refuters) — and records normally. keybind.json carries {"<action>": ","}. Translate delivers the comma presses with their resolved actions; qa-v2 FAILs the token grammar on every session; INP_TOKEN_CASE -> FIX_KEY_HYGIENE no-ops twice; every session from that player is terminally rejected "fix retries exhausted" and the player goes unpaid for valid gameplay data — at 10-30 min/session against the 1000h Aug-24 target, a whole operator's output can be silently burned. Related foot-gun in the same cell (worth fixing in the same commit): a vendor writing the key as "Comma" instead of "," normalizes to the multi-char token 'comma' which never matches the raw ',' events, so every comma press is silently stripped as unbound instead — data loss in the opposite direction (no _LITERAL_ALIASES entry for 'comma').

**Finder's proposed fix (vetting in-iteration per the ruling):** Give the comma key a NAMED display instead of the bare separator-colliding character: add ",": "Comma" to _KEY_DISPLAY (v2.py:90-98) — _KEY_DISPLAY_INV/key_canonical pick it up automatically, so the writer emits 'Comma' (cased, passes the grammar), hygiene round-trips it, and tools/fix_sync_from_v1's _KEY_INTERNAL inherits it; a bare ',' arriving in a foreign file then genuinely IS repairable by FIX_KEY_HYGIENE in one attempt (key_canonical(',') -> ',' -> key_display -> 'Comma'), turning today's terminal loop into a working fix. Also add "comma": "," to _LITERAL_ALIASES (keys.py) so a keybind written as "Comma" binds the real ',' events. Alternative shape (mirrors the I1 ruling, checker-side): exempt exactly the standalone token in the comma arm — `("," in t and t != ",")` — but the writer-side rename is strictly better because it also removes a raw comma from inside a pipe-joined CSV cell (client-parser ergonomics) and needs no checker change. Either way, needs Adnaan's ruling like I1 did; test: comma-bind e2e (translate -> checker no token FAIL, ',' presses ride with their actions) + hygiene repair of an injected bare-',' row + the existing I1 control stays green.

### #1 [MINOR] translator/translate.py:88 (regressions-r15, refuters 0/2)

**I5's byte-length clause admits unencodable lone-surrogate session ids, which still crash every join instead of taking the designed fallback**

safe_session_id's I5 clause (translator/translate.py:88, added in 7169922) tests `len(session_id.encode("utf-8", "ignore")) <= 200`. errors="ignore" silently DROPS characters utf-8 cannot encode, so the clause measures a different string than the one every join will pass to mkdir, which encodes strict. The only utf-8-unencodable class is lone surrogates, and those are JSON-legal: Python's json.loads parses '"abc\ud800def"' to 'abc\ud800def' without error, so a hand-edited/mojibake metadata.json (HumynCapture is a Windows tool; UTF-16-origin corruption yields unpaired surrogates) delivers such an id. It passes every other arm too (ord(0xD800)=55296 >= 32, no separators, not '..'). Proven by execution in a scratch clone at HEAD 4dc37b4: safe_session_id('abc\ud800def', Path('/tmp/bundle')) returned 'abc\ud800def' (fallback would be 'bundle'); len with ignore = 6 bytes for the 7-char string; os.mkdir on the accepted id raised "UnicodeEncodeError: 'utf-8' codec can't encode character '\ud800' ... surrogates not allowed" — isinstance OSError: False, isinstance ValueError: True. End-to-end through the REAL v2 join (translator/v2.py:333 safe_session_id -> :337 out_dir join -> :342 out_dir.mkdir): translate_bundle_v2 on a real bundle whose metadata session_id carries the surrogate crashed with the same UnicodeEncodeError instead of translating under the folder-name fallback. This is the exact failure shape I5's own docstring claims the shared decision point now prevents ('over-length ids take the designed bundle-folder-name fallback at all join sites instead of crashing mkdir'), and entry 74 marks defects in this mechanism reportable.

**Scenario:** A player-side or transfer-corrupted metadata.json carries "session_id": "...\ud800..." (a legal JSON escape that json.loads accepts silently — the same player-supplied-garbage class that produced H7's control-char ids and r15 #8's 300-byte id). The bundle ingests normally (only video.mp4 is md5-verified). On the ARR_RAW_ONLY route, fix_translate_raw (pipeline/fix.py:838) calls translate_bundle_v2, which crashes with UnicodeEncodeError at the out_dir.mkdir join. UnicodeEncodeError is a ValueError, not in the host tuple (OSError/MemoryError/OperationalError/TimeoutExpired), so apply_fixes' generic arm records a session-kind failure: attempt 1 burns, attempt 2 fails identically, and the session terminal-rejects 'fix retries exhausted' — a wrongful reject/unpaid player for an id the designed folder-name fallback translates fine (the exact harm chain H7 and I5 were written to close). The same id crashes both G7 operator tools (tools/fix_actions_from_v2.py, tools/fix_sync_from_v1.py route delivered session ids through the same safe_session_id) mid-batch, aborting every remaining --v1/--v2 pair in the invocation.

**Finder's proposed fix (vetting in-iteration per the ruling):** Make the byte-length arm encode strict and treat unencodability as garbage that takes the existing fallback: wrap the accept test so UnicodeEncodeError returns Path(bundle_dir).name (e.g. compute `blen = len(session_id.encode('utf-8'))` inside try/except UnicodeEncodeError -> fallback), or equivalently add `and not any(0xD800 <= ord(c) <= 0xDFFF for c in session_id)` to the shared accept condition. Pin with a unit test at the shared decision point (safe_session_id('a\ud800b', d) == d.name) plus an e2e sibling of I5's own test using a \ud800 JSON escape in metadata.json.

### #4 [MINOR] translator/translate.py:88 (fix-validate, refuters 0/2)

**safe_session_id accepts lone-surrogate ids (encode 'ignore'), crashing every join site past the designed fallback**

I5 (7169922) added `len(session_id.encode('utf-8', 'ignore')) <= 200` to the shared accept condition (translate.py:88). 'ignore' silently drops the exact characters that make an id unjoinable: JSON permits unpaired surrogate escapes, json.loads('"\\ud800bad-id"') yields a lone-surrogate str, and every filesystem call on it raises. Probe (real safe_session_id): `parsed: '\ud800bad-id'` -> `safe_session_id returned: '\ud800bad-id'` (accepted — ord(U+D800)=55296 passes the ord<32 control-char guard from H7, and encode-with-ignore under-counts it) -> `mkdir raised: UnicodeEncodeError` and `resolve raised: UnicodeEncodeError`, i.e. even translate_bundle_v2's containment assert at v2.py:340 (`out_dir.resolve()...`) crashes before mkdir. This is precisely the input class H7 (entry 67) and I5 (entry 74) exist to route to the bundle-folder-name fallback — 'ids that crash the join take the designed fallback' — left open at the shared decision point that covers all five join sites, and the 'ignore' handler additionally makes the 200-byte bound dishonest for such ids.

**Scenario:** A hand-edited or foreign-tool metadata.json carries session_id with an unpaired \ud800-family escape (valid JSON; same provenance class as H7's embedded-NUL, which was accepted as r14 #8). On the pipeline's ARR_RAW_ONLY route, FIX_TRANSLATE_RAW -> translate_bundle_v2 raises UnicodeEncodeError at the v2.py:340 resolve; apply_fixes' classifier (fix.py:425-427) types UnicodeEncodeError (a ValueError subclass, not in the host tuple) as SESSION — both fix attempts burn on the identical crash and the session is REJECTED 'fix retries exhausted' although the designed folder-name fallback translates it fine: a wrongful terminal reject with the bare fix-failed marker. On macOS operator-tool runs the same id can instead surface as OSError EILSEQ from mkdir, which the fix lane classifies HOST — the forever-retry FIX_QUEUED park with hourly alerts that I5 was written to close.

**Finder's proposed fix (vetting in-iteration per the ruling):** Encode strictly at the shared decision point: in safe_session_id wrap the length test in try/except — `session_id.encode('utf-8')` (no error handler); a UnicodeEncodeError routes to the bundle-folder-name fallback like every other rejected shape, and the 200-byte bound is then computed on the true on-disk byte length. Add a twin test beside the I5 boundary pins: json-parsed '"\\ud800x"' falls back to the folder name at the v2 join.

### #6 [MINOR] tools/fix_sync_from_v1.py:183 (tests-coverage, refuters 1/2)

**I2's fourth writer site (fix_sync_from_v1 remap credited-strip) has no test — full revert survives the arming gate at 765/761**

I2 (348c93d) fixed four keep/strip writer sites but its commit's own test list covers only three routes (translate e2e, retranslate, hygiene); the tools/fix_sync_from_v1.py half (lines 176-183, `kset &= credited` at 183) is exercised by no test — the only test touching the tool (pipeline/tests/test_r_loop13.py::test_fix_sync_tool_contains_a_traversal_session_id) pins the safe_session_id join with no combo binds. Mutation-proven in a scratch clone OUTSIDE the repo (repo untouched, mutant restored): reverting the block to the exact pre-I2 shape (`actions = resolve_actions(kset | bset, moving, rules)[0] if rules else []`, no credited strip), then running the runbook's pinned gate `SUITE_FLOOR=761 bash tools/run_suite.sh --with numpy==2.4.6 --with opencv-python-headless==5.0.0.93 --with rerun-sdk==0.36.0` → real output `765 passed in 146.85s / ARMING GATE OK: 765 passed (floor 761)` — identical to the clean-clone baseline I reproduced first (765/761). Behavioral divergence proven by direct execution of remap() with rules = build_resolver({'interact': {'modifier':'ctrl','key':'e'}, 'move_up':'w'}): HEAD → `frame0: keys=[] actions=[]` (bare half stripped); mutant → `frame0: keys=['e'] actions=[]` — the exact keys-have-actions violation I2 closed. The tool's frames.csv writer (line 287, `kept = [key_display(k) for k in keys if k in bound]`) keeps both combo halves (both are in bound_literals), so the remap strip is the ONLY thing between a bare combo half and the delivered CSV.

**Scenario:** An executor refactor or partial revert of the operator tool — the exact class this stream keeps documenting as recurring ('the r13 #10 class, recurring inside the fix set that closed it', and again here: I2/I4 recreate the r15 #6/#9 unpinned-half pattern inside the very fix set that closed those two findings) — passes the flip's arming gate 765/761 on both hosts. The next fix_sync_from_v1 batch over a combo-bind v1 delivery (the r15 #5 population: {modifier,key} binds, e.g. Ctrl+E interact) then ships frames.csv rows with input_keys populated and input_actions empty. check_session_v2 FAILs the keys-have-actions invariant on that delivery: if it re-enters the pipeline the row burns a fix attempt (hygiene now repairs it — I2's other half), and if the operator uploads the tool's output directly (its documented v1-era-delivery purpose) the violation reaches the client — either way a regression the arming gate provably cannot see.

**Finder's proposed fix (vetting in-iteration per the ruling):** Add a pin beside the r13 twin (or in test_r_loop15.py) that drives the REAL remap with a combo rule set — e.g. rules from {'interact': {'modifier':'ctrl','key':'e'}, 'move_up':'w'}, src frames [['e'], ['ctrl','e'], ['w']] — asserting the bare-half frame comes out with keys==[] (and no row has keys without actions), the chord frame keeps ctrl+e+interact, and the single bind is untouched; fail-first provable against the pre-I2 shape (my probe shows exactly that divergence).

### #7 [MINOR] tools/retrim_v2_session.py:103 (tests-coverage, refuters 1/2)

**I4's sweep-half (retrim_v2_session naive created_at guard) has no test — deleting it survives the arming gate at 765/761 with a −5h30m stamp shift**

I4 (ee35d3f) fixed two sites; only the fix_v1_to_v2 half is pinned (pipeline/tests/test_r_loop15.py::test_v1_naive_created_at_is_not_shifted_by_the_host_offset drives fixmod.fix_v1_to_v2 only). The sweep-FOUND second site — tools/retrim_v2_session.py:103-104 `if created.tzinfo is None: created = created.replace(tzinfo=timezone.utc)` — is exercised by no test: every test that reaches the real retrim tool (test_r_loop9.py:752, test_review_r4_fixval.py) builds sessions via _make_session, which writes an AWARE stamp (test_fix_cut_gate.py:144 appends 'Z'). Mutation-proven in a scratch clone OUTSIDE the repo (repo untouched, mutant restored): deleting both guard lines, then the pinned gate invocation → real output `ARMING GATE OK: 765 passed (floor 761)` — identical to baseline. Behavioral divergence proven by executing retrim() under TZ=Asia/Kolkata on a real 80s session whose session.json carries the naive stamp '2026-08-10T15:34:03': HEAD writes created_at_utc '2026-08-10T15:34:07.000000Z' (naive-UTC preserved, +4s keyframe-snapped cut); the mutant writes '2026-08-10T10:04:07.000000Z' — shifted −5h30m by the host offset.

**Scenario:** A regression of the two-line guard (revert, refactor of the datetime block) passes the arming gate 765/761. In-pipeline the site is shielded only by plan ordering (STR_SJ_INVALID's rewrite precedes FIX_RETRIM_HEAD — which I4's own commit message calls 'an ordering assumption, not a guard', so any future plan reordering silently re-exposes it), but the tool's PRIMARY documented use is direct operator invocation: head-trimming an already-delivered v2 session whose raw bundle no longer exists. On the IST batch Mac, one such run over a naive-stamped legacy delivery writes created_at_utc 5h30m early — silent delivered-metadata corruption on the exact r15 #7 [MINOR-confirmed] harm chain, now undetectable by the gate that arms the flip.

**Finder's proposed fix (vetting in-iteration per the ruling):** Add a TZ-forced naive pin mirroring test_v1_naive_created_at_is_not_shifted_by_the_host_offset but driving the real tool: load tools/retrim_v2_session via the existing _load idiom, _make_session + rewrite created_at_utc to naive '2026-08-10T15:34:03', TZ=Asia/Kolkata + tzset (restored in finally), call retrim(d, 3.0, d, make_rrd=False), assert the written stamp's wall clock is the naive-UTC one offset only by the actual head cut (e.g. startswith '2026-08-10T15:34:'), plus the aware control; fails pre-guard on every host (my probe output is exactly the fail-first signature).
