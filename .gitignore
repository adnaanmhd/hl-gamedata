game-data/
.DS_Store
*.mp4
*.zip
*.rrd

# Build artifacts
__pycache__/
*.pyc

# Python virtual environments (any location in the repo, any common name)
.venv/
venv/
env/
ENV/

# PyInstaller build output (capture_tool/HumynCapture.spec)
capture_tool/build/
capture_tool/dist/
capture_tool/dist-standalone/

# .NET build output (unity_plugin/CameraLogger — dotnet build)
unity_plugin/**/bin/
unity_plugin/**/obj/
*.user

# BepInEx runtime output some contributors may have copied in locally
BepInEx/LogOutput.log

# Sample bundles and generated delivery outputs (kept untracked)
samples/
Game Samples/
out/
