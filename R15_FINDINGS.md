# R15_FINDINGS.md — review iteration 15, findings of record

**Run 2026-08-19** by the H1–H9 executor session via the workflow
snapshot `tools/review/flip-review-iter15.js` (CONFIRMATION pass per
R8_IMPLEMENTATION_PLAN §4: nothing is fixed in-iteration; a not-quiet
result STOPS the stream and hands Adnaan the list). 7 finder lanes,
2-vote refute, 27 agents (27 completed, 0 errors, ~3.23M subagent
tokens, run `wf_0098c165-80b`). HEAD `d16d504` (code HEAD `82c86da` =
H1–H9 + floor 745; suite 749/749 both hosts).

**Verdict: NOT QUIET — 10 raised → 10 confirmed (6 major / 4 minor,
0 blockers), 0 killed.** Every finding survived both refuters (0/2
refuted across the board). Per Adnaan's binding sequencing
(2026-08-19), the executor STOPPED at this result: findings are
recorded here and handed over severity-ordered; nothing was fixed.

**Known duplicates/clusters:** #1 ≡ #2 ≡ #3 ≡ #10 (the H5
vanished-arm same-path heal gap — found independently by
regressions-r14, payment-split, driver-core AND tests-coverage; a
regression-class gap in the H5 fix: its own commit message's
self-heal claim is false for the same-path case). #6 and #9 are
tests-coverage pins for H-fix halves that survive deletion with the
arming gate green (H2's slug half; H6's CNT_EDGE_NONGAMEPLAY tail
arm). #4, #5, #7, #8 are pre-existing (the qa-v2 caseless-symbol key
grammar reject, the {modifier,key} combo-bind invariant violation,
the v1 naive-timezone shift, the H7-sibling session_id length
bound).

Machine-readable results incl. both refuter verdicts: session
scratchpad `r15-results.json`.

## Confirmed findings (severity-ordered)

### #1 [MAJOR] pipeline/ingest.py:710 (regressions-r14, refuters 0/2)

**H5 vanished-DISCOVERED quarantine has no same-path heal: a restored/re-uploaded folder is silently ignored forever**

The new H5 arm (ingest.py:710-719, commit c731e32) moves a DISCOVERED row to QUARANTINED when its folder is absent from one healthy listing. Entry 65 and the commit both claim 'a reappearance at a clean path re-registers via the existing quarantined-path heal' — but that heal (ingest.py:272-274) is gated on `existing["drive_path"] and existing["drive_path"] != ds.drive_path`, so it fires ONLY when the path CHANGED. A reappearance at the SAME path (the natural case: the path game/<operator>/<player>/<sid> is deterministic) falls through every arm: line 415 needs a DIFFERENT md5, the line 459 elif needs `ds.payload == "zip" or not existing["md5_video"]` (false for plain-file sessions whose listing carried an md5) — then `continue` at line 505 with no event, no line, no flag. Probe output (scratchpad/probe_h5_samepath.py + probe_h5_events.py, run against HEAD): after the arm quarantines the row, a scan listing the folder back at the identical path leaves it QUARANTINED ('after scan3 (same-path reappearance): QUARANTINED, discovered: [] | superseded: [] | flags: []'); a same-md5 re-upload with a NEWER ctime is also stuck ('after scan4: QUARANTINED'); five further restored-folder scans write ZERO new events ('events before restore=2, after 5 restored-scans=2'). The row is invisible afterward: no INT_PATH reason by design (folder_issues: []), QUARANTINED is a KEEP state in recal_rebuild_reset (KEEP_STATES line 35) so even a full reset preserves the dead row, and _sweep_terminal_work reclaims its local media after CONT_QUARANTINE_RECLAIM_H. Pre-H5, this exact sequence was harmless — the row stayed DISCOVERED and the next download succeeded once the folder was back.

**Scenario:** An operator accidentally deletes a session folder from Drive I (or reorganizes and moves it out of the game tree). The next scan — correctly, per H5's design — quarantines the DISCOVERED row with 'folder gone from Drive I — dropped from intake'. The operator notices and restores it from Drive trash (Drive restores to the original parent: identical path, identical md5, identical ctime), or the player re-uploads the same recording into the same tree (identical path and md5, newer ctime — probe scan4). Every subsequent scan sees the folder, prints nothing, writes nothing, and never downloads it: the session's hours are permanently lost to delivery and the player is never paid, with zero ops surface after the one quarantine-time line — the operator who verified the folder is back on Drive reasonably believes it re-entered intake, because entry 65 says reappearance self-heals. The same silent dead-end fires on a transient Drive listing gap (subtree missing from one otherwise-healthy listing): one flap permanently kills the row where pre-H5 it retried and recovered.

**Finder's proposed fix (NOT vetted — the stream stopped here):** Give the H5 quarantine class a same-path heal: in scan's existing-row branch, when existing.state == QUARANTINED, existing.drive_path == ds.drive_path, and the row's latest event detail is the vanished-arm detail ('folder gone from Drive I — dropped from intake'), re-register it (the same reset-the-slot semantics as the different-path quarantine heal at line 274, or simply set_state back to DISCOVERED with a loud '[vanished-reappeared]' line). Keying on the arm's own detail keeps all other quarantine classes terminal exactly as today.

### #2 [MAJOR] pipeline/ingest.py:710 (payment-split, refuters 0/2)

**H5 vanish-arm: same-path folder reappearance (delete + restore-from-trash) is permanently, silently unrecoverable**

The r-loop-14 H5 arm (ingest.py:710-719, commit c731e32) moves a DISCOVERED row to QUARANTINED with no INT_PATH reason when its folder is absent from a healthy listing. Entry 65 asserts "a clean-path reappearance re-registers via the existing quarantined-path heal" — but that heal (ingest.py:272-274) fires only when existing["drive_path"] != ds.drive_path. For a reappearance at the SAME path with the SAME bytes, every branch dead-ends: the md5-supersede branch (line 415) requires a DIFFERENT vmd5; the zip/no-md5 branch (lines 459-464) requires (payload zip OR stored md5 empty) AND (changed bytes OR newer ctime) — a plain-files row quarantined by the vanish arm keeps its real md5, so this branch is unreachable regardless of ctime; execution then hits the bare `continue` at line 505 with zero output. Proved by running the real scan() against a scratch ledger: scan1 registers S1 DISCOVERED (md5-S1); scan2 with the folder absent prints '[vanished-discovered] ... dropped from intake' and quarantines (arm working as designed); scan3 and scan4 with the folder fully restored (same path, bytes, createdTime) leave S1 QUARANTINED with res.discovered=[], res.superseded=[], integrity_flags=[] — silent, forever. Controls confirm the only exits: a DIFFERENT path heals ('quarantined path healed — re-registered at kamla/op two/...'), and DIFFERENT bytes supersede. No other path recovers it: the download lane picks only DISCOVERED rows; build_folder_issues lists only INT_PATH quarantines (reports.py:1154-1160) and H5 deliberately writes no reason; recal_refix_reset filters out QUARANTINED roots; recal_rebuild_reset's KEEP_STATES preserves QUARANTINED rows across resets. Pre-H5, the identical sequence self-healed loudly: the row stayed DISCOVERED and the D-lane re-queued it with an alert each retry window (continuous.py:536-540), succeeding as soon as the folder was back.

**Scenario:** An operator (free-text folders, hand-managed — the same population the move-heal exists for) accidentally deletes a player's complete session folder from Drive I, or Drive briefly drops the subtree. The next scan tick quarantines the row with one stderr line. The operator restores the folder from Drive trash — same path, same files, same createdTime. From that moment the fully-uploaded, payable session is invisible forever: never downloaded, never validated, never on a payment sheet (build_sheet_rows skips QUARANTINED roots), never on the folder-issues chase list (no INT_PATH), never rejected (no dossier or coaching), no recurring alert. The player is silently never paid for footage sitting complete on Drive, and an operator who checks Drive sees a perfectly healthy folder — a misled operator plus lost hours against the 1000h target with zero surface to catch it. Before c731e32 this exact sequence recovered by itself.

**Finder's proposed fix (NOT vetted — the stream stopped here):** In scan(), add a same-path re-registration branch before the collision/supersede checks: if existing['state']=='QUARANTINED' and existing['drive_path']==ds.drive_path and the row carries no reasons (reasons_json empty — the vanish arm's signature; or discriminate on the latest event detail 'folder gone from Drive I — dropped from intake'), re-enter it as DISCOVERED with identity preserved (same bytes: keep md5/duration/payment stamps, exactly like the identical-md5 heal) and log '[vanished-discovered] reappeared — re-registered'. Scope it to reason-less vanish quarantines so INT_PATH and download-crash quarantines keep their current behavior.

### #3 [MAJOR] pipeline/ingest.py:716 (driver-core, refuters 0/2)

**H5 vanished-arm quarantine is permanent when the folder reappears at the same path — sessions silently lost forever**

The H5 arm (ingest.py:710-719, commit c731e32) sets a DISCOVERED row QUARANTINED when its folder is absent from one healthy listing. Every recovery path then requires something that a move-back/restore/identical re-upload does not provide: the quarantined-path heal (ingest.py:272-274) fires only when `existing["drive_path"] != ds.drive_path`; the md5 supersede (ingest.py:415) needs a DIFFERENT video md5; the zip/no-md5 supersede (ingest.py:459-464) needs changed bytes or a NEWER createdTime (Drive moves/restores preserve createdTime) and is unreachable outright for files payloads with a stored md5 (`not existing["md5_video"]` is False). The row falls through to `continue` at ingest.py:505 with no event, no flag, forever. Proven by execution (scratchpad probe, repo untouched): scan1 both sessions present -> DISCOVERED; scan2 folder absent, kamla tree healthy -> `[vanished-discovered] ... dropped from intake`, state QUARANTINED; scan3 folder BACK at the SAME path, same md5/bytes/ctime -> `state: QUARANTINED, discovered: [] | superseded: [] | flags: []`; scans 4-6 identical -> still QUARANTINED; control at a DIFFERENT path -> heals to DISCOVERED; zip-payload same-path variant -> also stuck; `reasons_json: []` so the folder-issues chase list excludes it by H5's own design. Before c731e32 this scenario was fully self-healing (the row simply stayed DISCOVERED and the next scan/download succeeded), so this is a regression introduced by the H5 fix — and accepted-entry 65's premise 'a clean-path reappearance re-registers via the existing quarantined-path heal' is only true for a DIFFERENT path; the entry itself says defects in the mechanism are findings.

**Scenario:** During collection an operator accidentally drags a player folder (or their whole operator folder) out of the game tree at 12:00 and drags it back at 12:10 — or restores a deleted session folder from trash, or re-uploads the identical bytes after an accidental delete. The 12:05 scan quarantines every DISCOVERED session under it in one pass ([vanished-discovered] goes to the service journal only; the digest prints an unnamed 'quarantined +N' once). From 12:10 on, every scan silently ignores those sessions: no state change, no integrity flag, no folder-issues line (no INT_PATH by design), no reclaim/heal ever fires. The operator believes the move-back fixed it; the sessions never download, never deliver, and the player's hours are never counted or paid — permanent silent intake loss recoverable only by hand ledger surgery that nothing prompts anyone to perform. This is exactly the 'state that can never be left' class, introduced by the r-loop-14 commit this pass audits.

**Finder's proposed fix (NOT vetted — the stream stopped here):** Give the vanished-arm quarantine a same-path heal leg: in scan's `existing is not None` flow, when `existing["state"] == "QUARANTINED"` and the session now parses clean at the SAME recorded path, and the row's quarantine is the vanished-arm kind (latest detail == "folder gone from Drive I — dropped from intake", or equivalently reasons_json empty of INT_PATH), run the existing heal body (re-register DISCOVERED, same changed-bytes-only stamp-clear rules, work-dir wipe, integrity flag) instead of falling through to the silent continue at ingest.py:505. Keep the different-path leg and the download-crash/md5-mismatch quarantines (which should NOT auto-heal) unchanged; pin with a same-path-reappearance test beside the existing different-path one in test_r_loop14.py.

### #4 [MAJOR] translator/v2.py:781 (translator, refuters 0/2)

**qa-v2's key-token grammar rejects every caseless symbol key the translator's own writer emits — a keybind binding ';'/'-'/'[' etc. terminal-rejects after a provably no-op fix loop**

check_session_v2 flags a key token as non-v2 when `(t.lower() == t and not t.isdigit())` (translator/v2.py:781), but the writer's own key_display (v2.py:103-108) returns `tok.upper()` for single-char tokens, which for symbol keys (';', '-', '=', '[', '/', etc.) is the caseless token itself; _v2_rows keeps any such key the session keybind binds (v2.py:205). Proven by execution (scratch copy, real translate_bundle_v2 -> check_session_v2): a Kamla bundle with keybind {"interact": ";", ...} delivered rows `(';|A|W', 'interact|move_up|move_left')` (actions resolve fine) and qa returned `FAIL: non-v2 key tokens in input_keys: [';']`. validate.py:83 maps it to INP_TOKEN_CASE (blocking, fixable) and plan_fixes routes it to FIX_KEY_HYGIENE unconditionally — even with raw sidecars (fix.py:159-160, confirmed in R12_FINDINGS.md:88). fix_key_hygiene rewrites tokens via the same key_display (fix.py:1124): running the real fixer on the delivered session printed `hygiene: stripped 0 tokens...` and the re-run checker printed the identical `FAIL: non-v2 key tokens in input_keys: [';']`. No other fix touches token case, so both attempts burn deterministically.

**Scenario:** A player supplies a keybind.json (the documented, authoritative per-session file) that binds any symbol key — e.g. a non-QWERTY or lefty layout binding interact to ';'. Every session they record translates cleanly, resolves actions correctly, then FAILs qa-v2's token check; the pipeline plans FIX_KEY_HYGIENE (attempt 1: ok-but-no-op, FAIL re-fires), attempt 2 identically, and the session is terminally REJECTED 'fix retries exhausted' — every session from that player, unpaid, with the defect attributed to their footage while the contradiction is between our own writer (v2.py:205 + key_display) and our own checker (v2.py:781). Neither retranslate nor hygiene can ever converge because the writer itself produces the flagged token.

**Finder's proposed fix (NOT vetted — the stream stopped here):** Make the writer and checker agree on caseless keys: either exempt tokens with no case (t.upper() == t.lower()) from the v2-case test at v2.py:779-781, or rule symbol keys unmappable in the v2 vocabulary and strip-and-count them in _v2_rows and fix_key_hygiene exactly as entry 44 ruled for 'Mouse4'/'LMB' buttons — so the FAIL can never re-fire. Pin with a symbol-key-bind e2e test (translate -> qa PASS/typed-outcome, hygiene idempotent).

### #5 [MAJOR] translator/v2.py:796 (translator, refuters 0/2)

**A {modifier,key} combo bind makes its bound halves violate the keys-have-actions invariant — INP_KEYS_NO_ACTION re-fires through BOTH fix routes and terminal-rejects the session**

bound_literals (keybind.py:185-192) includes every alt token of a combo group, so both halves of a {"modifier":"ctrl","key":"e"} bind are 'bound' and _v2_rows keeps them (v2.py:203-206), yet resolve_actions fires nothing unless ALL groups are held (keybind.py:136-137) — so any frame holding one half alone ships keys with null actions, and check_session_v2 FAILs it (v2.py:776, 796). Proven by execution: a Kamla bundle with keybind {"interact": {"modifier": "ctrl", "key": "e"}, ...} and a bare 'e' press delivered pairs [('E', ''), ...] and qa returned `FAIL: 7 frames have input_keys but null input_actions`. Both designed fix routes re-fire it, proven by running each real fixer on that session: (a) FIX_KEY_HYGIENE (no-sidecar route, fix.py:144-145) strips only tokens NOT in bound (fix.py:1091-1093) — 'e' IS bound — and re-resolution yields '' again: fixer said 'stripped 0 tokens', re-check `FAIL: 7 frames have input_keys but null input_actions`; (b) FIX_RETRANSLATE (has_raw route, fix.py:136-139) — retranslate_from_sidecars(work, ledger_game='kamla') returned ok ('re-translated from sidecars (60 frames...)') and the re-check printed the identical FAIL. The r10 #9 hygiene strip (fix.py:1084-1093) covers only UNBOUND tokens; this is its bound sibling.

**Scenario:** A player keybind.json using the documented {modifier,key} vendor form (a form r-loop 8's own comments and R8_FINDINGS #776 show exists in real files, carrying VK numbers) — e.g. crouch = Ctrl+C. In every real session the modifier and key are pressed non-atomically, so modifier-lead / key-lead frames (and any bare press of either half) produce bound-keys-with-null-actions rows. qa-v2 FAILs INP_KEYS_NO_ACTION (blocking, fixable); attempt 1 (retranslate with sidecars, or hygiene without) reproduces the identical output; attempt 2 likewise; the session is terminally REJECTED 'fix retries exhausted'. Every session from that player is rejected and unpaid for a contradiction between our binner's bound-set semantics and our checker's invariant — with fix budget and (on revalidation) paid VLM sweeps burned each time.

**Finder's proposed fix (NOT vetted — the stream stopped here):** Restore the delivery invariant at the writer: in _v2_rows (and mirrored in fix_key_hygiene / retranslate output), strip-and-count a kept token when the frame resolved no action crediting it (extend resolve_actions' credited-literals return to the no-context path, or drop tokens whose every satisfied rule needs an unheld co-group), so a combo half pressed alone is stripped exactly like an unbound key. Alternatively split bound_literals into 'strippable-survivors' vs 'action-crediting' sets. Pin with a combo-bind e2e test through both fix routes.

### #6 [MAJOR] pipeline/fix.py:912 (tests-coverage, refuters 0/2)

**H2's ledger-slug anchor (the slug half of the fix) survives deletion with the full arming gate green — a silent-corruption regression the suite cannot see**

H2 (1d54775) fixed TWO sites in retranslate_from_sidecars: the resolve_keybind fallback anchor (fix.py:949-951) and the slug at fix.py:912 (`slug = ledger_game or game_key_from_name(...)`) that keys KEYBIND_PATCHES (fix.py:951), the outer_wilds context gating (fix.py:1019-1024), and fix_sessionjson_recompute (fix.py:1030). The r14 #1/#6 fix specs explicitly required the slug half (R14_FINDINGS.md line 51: 'and for the KEYBIND_PATCHES slug at fix.py:931'; line 101: 'Use the same fallback slug for KEYBIND_PATCHES, the ctxmod context test, and fix_sessionjson_recompute'), and the commit message claims all downstream consumers are 'fixed by the same assignment'. But all three H2 tests (pipeline/tests/test_r_loop14.py:192-243) use kamla as the ledger game, and every slug consumer is a no-op for kamla: KEYBIND_PATCHES == {'outer_wilds': {'general_confirm': ['e','enter']}} only, CONTEXT_GAMES == {'outer_wilds'} (verified by execution). Mutation proof in scratch clones (repo untouched): reverting ONLY line 912 to `slug = game_key_from_name(game_name or "", exe_name)` -> the 3 H2 tests pass ('3 passed, 15 deselected'), 174 adjacent tests pass, and the FULL runbook arming gate is green in two independent clones: '749 passed in 139.77s / ARMING GATE OK: 749 passed (floor 745)'. Behavioral divergence proven through the production chain (plan_fixes -> apply_fixes -> _dispatch): outer_wilds ledger row + degraded metadata {'name': 12345} + usable custom raw/keybind.json {'jump':'space'} + an 'e' press -> HEAD ships 'keys: [E], E actions: [general_confirm]'; the suite-green mutant ships 'keys: [], E actions: []' with apply_fixes error None — every 'e'/'enter' press silently stripped from the delivered frames.csv, plus the mandatory OW context gating silently skipped (slug 'unknown_game' not in CONTEXT_GAMES).

**Scenario:** An executor refactor or revert of the one-line slug anchor — the exact class H9's own commit message says recurs ('the r13 #10 class, recurring inside the fix set that closed it') — passes the flip's arming gate 749/749 on both hosts. In production, every outer_wilds session with degraded or wrong player-typed metadata that hits a non-reroute FIX_RETRANSLATE (SYN_TS_NOT_PTS etc., 'the universal strong fix') then ships frames.csv with its patch-bound key presses stripped (the r-loop 11 #4 silent delivered-data-corruption class) and the OW frozen-context gating skipped without any note — undetectable by the gate that arms the flip, and invisible in the fix chain (apply_fixes reports no error).

**Finder's proposed fix (NOT vetted — the stream stopped here):** Add OW-ledger discriminator tests beside the existing H2 block in pipeline/tests/test_r_loop14.py: (a) ledger game outer_wilds + degraded metadata {'name': 12345} + custom raw/keybind.json, assert the 'E' press survives with action general_confirm (fails on the slug mutant — proven by my probe); (b) a wrong-game variant on an outer_wilds ledger row pinning that context gating/patches key on the ledger slug. The kamla tests stay as-is; the discriminator must run where KEYBIND_PATCHES/CONTEXT_GAMES are live.

### #7 [MINOR] pipeline/fix.py:1541 (fix-validate, refuters 0/2)

**fix_v1_to_v2 shifts a naive v1 created_at_utc by the host's UTC offset (only unguarded naive->astimezone in the fix chain)**

fix_v1_to_v2 (pipeline/fix.py:1538-1544) parses the v1 canonical stamp with `created = datetime.fromisoformat(ca.replace("Z", "+00:00"))` and then writes `created.astimezone(timezone.utc)`. For a naive stamp (no tz suffix — the exact input class r-loop 2/3 established as real for HumynCapture metadata), fromisoformat returns a NAIVE datetime and .astimezone interprets it as HOST-LOCAL time. Proved by execution under TZ=Asia/Kolkata (this Mac's TZ): input "2026-08-10T15:34:03" (UTC by contract) is written as "2026-08-10T10:04:03.000000Z" — 5h30m early — while the sibling guard in fix_sessionjson_recompute (fix.py:1424-1428, comment: "repair it in place so .astimezone below can't shift it by the host's offset") preserves 15:34:03. Every sibling site has this guard: fix_sessionjson_recompute:1426-1428, cutter.py:72-73, retranslate's _utc (fix.py:925), translator/v2._utc_aware. fix_v1_to_v2 is the sole omission. The naive stamp reaches this code unchecked because the v1 payload path returns ARR_V1_FORMAT from validate_session (validate.py:871-877) BEFORE the qa checker (whose "not timezone-aware" FAIL would otherwise flag it) ever runs, and fix_sessionjson_recompute — called immediately after at fix.py:1546 — cannot repair it: the stamp is by then aware and _TS_RE-conformant, just wrong by the host offset.

**Scenario:** A legacy v1 delivery whose canonical created_at_utc is naive (e.g. "2026-08-10T15:34:03") is processed on a non-UTC host — the Mac that runs batch passes sits in Asia/Kolkata. sniff_payload -> ARR_V1_FORMAT -> FIX_V1_TO_V2 writes created_at_utc 5h30m early. Branch (a), no raw sidecars: the converted session is internally consistent (recompute sets ended = created + duration), passes qa-v2 and the final gate, and DELIVERS with a wrong absolute session timestamp — silent delivered-metadata corruption, checker-green. Branch (b), raw sidecars present (fix_v1_to_v2:1551-1555 moves them to raw/): on revalidation _seed_shift_record's base_head_us goes hugely negative (no match, silently no record), and qa's _verify_against_raw — which derives the head offset from created_at_utc minus metadata started_at_utc (translator/v2.py:993-994) — re-bins at the wrong head and FAILs "raw recomputation: dx/dy differs" -> SYN_TS_NOT_PTS -> FIX_RETRANSLATE, whose head_s = max((created-started).total_seconds(), 0.0) clamps the negative offset to 0 (fix.py:936) and re-bins ~5s off the true v1 head trim -> sync FAILs again on attempt 2 -> REJECTED "fix retries exhausted": a wrongful terminal reject (unpaid player) manufactured by the pipeline's own timestamp corruption.

**Finder's proposed fix (NOT vetted — the stream stopped here):** In fix_v1_to_v2, after parsing `ca`, add the exact sibling guard: `if created.tzinfo is None: created = created.replace(tzinfo=timezone.utc)` (two lines, mirroring fix.py:1426-1428 and cutter.py:72-73), before the += head_cut_s adjustment and the astimezone write.

### #8 [MINOR] translator/translate.py:75 (translator, refuters 0/2)

**safe_session_id has no length bound — a >255-byte session_id crashes every join's mkdir with OSError, which the fix lane misclassifies as HOST and retries forever (H7's remaining sibling)**

The H7 accept condition (translator/translate.py:75-80) checks separators, '..', and now ord(c)<32, but never length. Proven by execution: safe_session_id('x'*300, ...) returns the 300-char id, and translate_bundle_v2 on a bundle whose metadata.json carries that session_id raised `OSError: errno=63 [Errno 63] File name too long` from out_dir.mkdir (v2.py:321; the resolve() containment assert passes because realpath swallows the error). On the ARR_RAW_ONLY path this surfaces inside fix_translate_raw -> apply_fixes, whose classifier (fix.py:425-427) marks any OSError kind='host' — so per the settled host contract (accepted entries 11/12) the row parks FIX_QUEUED with cooldown and an hourly alert and is re-picked forever: unlike H7's ValueError (2 attempts then terminal reject), this garbage id never reaches ANY terminal outcome and never takes the designed folder-name fallback. The same mkdir crash kills both G7 operator tools mid-batch (tools/fix_actions_from_v2.py:61, tools/fix_sync_from_v1.py:270) — the exact harm class H7's commit message cites for control characters.

**Scenario:** A raw-only bundle arrives with a corrupted or hand-edited metadata.json whose session_id exceeds NAME_MAX (255 bytes) — the same 'legal JSON garbage' provenance the confirmed r14 #8 NUL finding was accepted on. Validation types it ARR_RAW_ONLY; FIX_TRANSLATE_RAW crashes at mkdir with OSError; apply_fixes refunds it as host-class; the row parks and retries on every cooldown expiry indefinitely — media held forever past the reclaim design, a runner slot burned each cycle, and an hourly alert telling the operator the HOST is failing when the session data is bad. The player never receives a reject; the session never resolves without manual surgery.

**Finder's proposed fix (NOT vetted — the stream stopped here):** Extend the shared decision point exactly as H7 did: add `and len(session_id.encode('utf-8', 'ignore')) <= 200` (or 255) to safe_session_id's accept condition so over-length ids take the designed bundle-folder-name fallback at all five join sites; add the unit pin plus an e2e twin ('x'*300 through the real v2 join -> folder-name fallback, no raise) beside the H7 tests.

### #9 [MINOR] pipeline/validate.py:580 (tests-coverage, refuters 0/2)

**H6's joint-edge composition: the CNT_EDGE_NONGAMEPLAY tail arm survives deletion with the arming gate green**

_joint_edge_short's tail accumulation for CNT_EDGE_NONGAMEPLAY (validate.py:580, `tail_cut = min(tail_cut or 1e9, p["cut_at_s"])`) is exercised by no test: the three composition tests cover nongameplay-HEAD + chat-tail (test_r_loop14.py:525), notif-head + chat-tail (:494), and the individual-CNT_SHORT defer (:543) — never a tail-side CNT_EDGE_NONGAMEPLAY. Mutation proof in a clean scratch clone: deleting the else-branch passes all H6 tests plus test_validate_mapper/test_r_loop12 ('85 passed') and the FULL runbook arming gate: '749 passed in 141.36s / ARMING GATE OK: 749 passed (floor 745)'. Behavioral divergence proven: reasons [CNT_NOTIF_EDGE t=2.0 head, CNT_EDGE_NONGAMEPLAY tail cut_at_s=71.0] -> HEAD appends CNT_SHORT {'post_cut_s': 68.0}; the mutant appends nothing (probe output 'A: CNT_SHORT emitted: True/False'). The chat-head sub-branch (`t <= 3.0` at validate.py:583) is likewise untested — same class.

**Scenario:** A regression of the tail branch passes the arming gate; a 70-78s clip with a confirmed head notif/chat plus a tail nongameplay window then reverts to exactly the pre-H6 behavior r14 #7 was fixed for: a burned fix attempt, a pointless lossless ffmpeg cut, and a terminal reject under the misdirecting 'split produced no >=70s segment' instead of CNT_SHORT's truthful post_cut_s coaching — with the suite unable to detect the regression.

**Finder's proposed fix (NOT vetted — the stream stopped here):** Add a composition unit test mirroring test_joint_edge_composition_includes_edge_nongameplay_cuts but with the nongameplay cut on the TAIL (e.g. head notif t=2.0 + CNT_EDGE_NONGAMEPLAY tail cut_at_s=71.0 -> exactly one CNT_SHORT post_cut_s 68.0), and a chat-head (t<=3.0, no edge param) sibling.

### #10 [MINOR] pipeline/ingest.py:710 (tests-coverage, refuters 0/2)

**H5's vanished-folder quarantine is a permanent dead end when the folder reappears at the SAME path — restore-from-trash or an identical re-upload never re-registers**

The new arm (ingest.py:710-719) quarantines a DISCOVERED row on listing evidence, and both the commit message and accepted entry 65 claim self-healing via 'a reappearance at a CLEAN path'. That heal (ingest.py:272-274) fires only when `existing["drive_path"] != ds.drive_path`. For the SAME path, every escape hatch is closed: the md5 supersede (ingest.py:415) needs DIFFERENT bytes; the zip/no-md5 arm (ingest.py:459-464) needs `ds.payload == "zip" or not existing["md5_video"]` AND changed bytes or newer ctime — a restored folder has the same md5, same bytes, and (with --drive-use-created-date, ingest.py:130) the same createdTime; then `continue` at ingest.py:505. Proven by execution against HEAD with the real scan (probe output): 'after first scan: DISCOVERED / after vanish: QUARANTINED / after restore x3: QUARANTINED' — three healthy listings carrying the folder at its registered path never re-register it. The row is deliberately off the folder-issues chase list (no INT_PATH; the H5 test itself pins reports.build_folder_issues(ledger) == []), so the dead end is also invisible to the operator's chase report.

**Scenario:** An operator deletes a registered folder by mistake (r14 #5's own 'routine drive hygiene') and corrects it the natural way — restore from Drive trash, or re-upload the identical files at the same path. The session is permanently out of intake: never downloaded, never validated, never delivered, the player's hours never paid — with total visibility of one [vanished-discovered] stderr line at prune time and a +1 in the digest quarantine counter. Every other quarantine class has a working correction path (different path heals, different bytes supersede); this new arm is the first whose natural correction is same-path-same-bytes, and exactly that correction is a no-op.

**Finder's proposed fix (NOT vetted — the stream stopped here):** Give the arm a symmetric heal on the same evidence it prunes on: in scan, when a listed sessions entry matches a QUARANTINED row at the SAME drive_path whose latest event detail is the vanished-arm marker ('folder gone from Drive I — dropped from intake'), re-register it to DISCOVERED (the folder is demonstrably back), mirroring the existing quarantined-path heal's slot reset. Alternatively, surface vanished-quarantines on a visible report so a human can chase them. Pin with a same-path-reappearance test beside test_vanished_discovered_reappearing_folder_heals.

