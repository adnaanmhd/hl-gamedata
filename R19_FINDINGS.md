# R19_FINDINGS.md — review iteration 19, findings of record

**Run 2026-08-20** by the R19 executor session via the workflow snapshot
`tools/review/flip-review-iter19.js` (the FIRST of the two RULED extra passes
per R8_IMPLEMENTATION_PLAN §0/§4, RULED by Adnaan 2026-08-20: iterations 19
and 20 BOTH run; this pass is the first review the UNREVIEWED r18 L set gets,
its confirmed set is fixed in-iteration with full §2 discipline as the M set,
and iteration 20 reviews those fixes). 7 finder lanes, 2-vote refute, 33
agents (33 completed, 0 errors, ~3.81M subagent tokens, run `wf_215a5af0-f51`).
HEAD `06ecd72` (code HEAD `f57b3ff` = L1–L3 + floor 798 `74b4a17`; suite
802/802 both hosts).

**Verdict: NOT QUIET pre-fix — 13 raised → 13 confirmed
(1 blocker, 8 major, 4 minor), 0 killed.** All 0/2 refuted.

**Clusters/classes (doc numbering, severity-ordered):** #4≡#6 are ONE defect
found independently by two lanes — OverflowError escapes L1's
(TypeError, ValueError) net at the stamp/trim arm. #1 (the BLOCKER) and #10
attack the SAME L1 stamp/trim block from the other side: the degrade arms
FABRICATE head-offset facts (head_cut 0.0 from a junked trim; a synthesized
now-UTC stamp) that the raw-sidecar verify/retranslate machinery then acts
on — silent delivered desync or a wrongful terminal reject. #2, #11 and #9
are L2's gate: the dict-parse test admits semantically-unusable metadata
(#2), validate's aux['has_raw'] drifted from the tightened plan gate (#11),
and the errors='replace' arm is coverage-free (#9). #3 breaks L1's
every-read-degrades contract via a ragged CSV row. #7/#8/#9 are the r15 #6
arming-gate-invisible class recurring INSIDE the L set (non-dict-canonical
arm, str() coercion, errors='replace' — each proven gate-green at 802/802
under the exact mutant). #5 is a PRE-EXISTING payment defect (not L-caused):
the zip-class supersede clears payment stamps unconditionally while an
identical-bytes re-upload restores nothing — hours counted on two sent
sheets. #12/#13 are pre-existing sibling crashers in the same degrade
doctrine (recompute's OverflowError; the unmapped non-object-session.json
FAIL routed to a retranslate that cannot succeed).

Machine-readable results incl. both refuter verdicts: session scratchpad
`r19-results.json`.

## Confirmed findings (severity-ordered)

### #1 [BLOCKER] pipeline/fix.py:1632 (translator, refuters 0/2)

**L1's fabricated head offset silently desyncs (or wrongly rejects) sidecar-bearing v1 conversions**

L1 (e197244, landed unreviewed) made fix_v1_to_v2's stamp/trim degrade arms fabricate head-offset facts that the raw-sidecar machinery then acts on. pipeline/fix.py:1631-1633 normalizes a junk truthy non-dict canonical.trim to {}, and 1644-1645 then add head_cut 0.0 to a parseable created_at_utc, shipping created_at = raw started_at even though the delivered video begins at started_at + head_cut (>=5s by construction: translator/trim.py plan_cuts snaps the head to the first keyframe at/after 5s, and v1 canonical.created_at_utc is the raw started_at verbatim, translator/translate.py:334). That violates the created-minus-started == binning-head contract consumed by translator/v2.py:1052 (_verify_against_raw) and pipeline/fix.py:960 (retranslate_from_sidecars). Proven by execution on a real ffmpeg bundle (30fps, real keyframes, head_cut 8.0s, kamla built-in keybind), driving the actual production functions in sequence: (1) fix_v1_to_v2(work,'kamla') -> 'converted v1 -> v2 (2312 rows)', session.json created_at_utc = 2026-08-01T10:00:00.000000Z (= started_at, head cut dropped) while the CSV is still CORRECT ('e' presses recorded at raw t=20s/60s sit at rows 360/1565, ts 12000/52167ms); (2) check_session_v2(work, raw_bundle=work/'raw') -> 'FAIL: raw recomputation: dx/dy differs from CSV in 1522 frames (max run 4, 65.83% of rows...)' because the verify re-bins at head_us=(created-started)=0; _map_qa_issues maps it [('SYN_TS_NOT_PTS', True, True)] and plan_fixes(game='kamla', has_raw=True) -> [('FIX_RETRANSLATE', {'rerouted': False}), ('FIX_SESSIONJSON_RECOMPUTE', {})]; (3) retranslate_from_sidecars(work, ledger_game='kamla') -> 're-translated from sidecars (2312 frames...)' with head_s = created-started = 0.0, rewriting the CSV so the same 'e' presses now sit at rows 600/1805 (ts 20000/60200ms) — every input 8 seconds early relative to the video content; (4) final check_session_v2 -> status WARN with 'OK: off-by-one check — 1362 raw mouse events independently re-binned to containing frames; CSV matches exactly on all 2312 rows' and mapped reasons [] -> READY -> DELIVERED. The _seed_shift_record rescue cannot fire (its exact-match scan is +/-15 frames; the offset is >=150), frame-sync passes (timestamps are real PTS), and the sync grounding's +/-120-frame scan (+/-4s at 30fps) cannot represent a >=5s offset, so it reads 'correlation too weak' -> benign advisory unless the deliberately-tight corr<0.05 AND motion_frac>=0.10 AND video_active gate fires SYN_UNMEASURABLE_SUSPECT (validate.py:227-241) — a wrongful unfixable reject of footage that was correctly binned before the pipeline touched it. Sibling arm, also proven by execution: a junk created_at_utc ('31/07/2026 10am') is OMITTED (fix.py:1648-1653) and fix_sessionjson_recompute synthesizes created=NOW (fix.py:1469), so the raw verify FAILs identically ('dx/dy differs in 1500 frames') and retranslate then refuses — 'FixFailed: head offset 1589816.9s leaves zero events beyond 0 held-key carries... refusing to re-bin' — burning attempt 2 into a terminal 'fix retries exhausted' reject (plus two paid VLM sweeps) for a session whose CSV was correct and whose true stamp was derivable from raw started_at. Pre-L1 both shapes crashed (AttributeError on trim.get / ValueError on fromisoformat) into the same terminal reject — bad but safe; L1 converted the trim shape into silent delivered corruption, and the landed test pins exactly the corrupting half ('a junk trim must not cost the valid stamp (head cut 0.0)', pipeline/tests/test_r_loop18.py:118-133) without ever exercising the sidecar interaction. Entry 86 explicitly lists 'a parseable stamp wrongly omitted or shifted' as a reportable defect in this mechanism.

**Scenario:** A player uploads a v1 delivery in the standard shape (out/ files plus the two raw sidecars; ingest moves them into raw/, so has_raw is True and metadata.json parses fine) whose session.json canonical.trim block got mangled to a non-dict value — the exact junk class iteration 18 proved real ('a truthy non-dict canonical/trim crashes .get()', R18_FINDINGS.md line 54) and L1 was written to absorb. The session has normal keyboard and mouse play. Attempt 1 converts it keeping created_at = started_at; revalidation's raw off-by-one check falsely condemns the still-correct CSV (SYN_TS_NOT_PTS); attempt 2's FIX_RETRANSLATE re-bins every event at head 0 and the final validation comes back clean — the session is DELIVERED to Odyssey with every key press, click and mouse delta placed ~5-8s (one head-cut) earlier than the frame it happened on, undetectable to frame-sync, the raw recheck (which now agrees with the corruption), and the lag scan (offset beyond its +/-4s range). That is precisely the event-to-frame desync class the delivery contract and this whole pipeline exist to prevent, shipped silently. On the alternate sync-check fork (corr<0.05 with visible motion) the same session is instead terminally rejected as SYN_UNMEASURABLE_SUSPECT — an unpaid player for a pipeline-created defect. And for the broader r18-proven class this commit claims to rescue — any sidecar-bearing v1 upload with an unusable created_at_utc — the synthesized-NOW stamp poisons the same verify and retranslate refuses on both attempts: wrongful terminal reject, two burned attempts, two paid VLM sweeps.

**Finder's proposed fix (vetting in-iteration per the ruling):** In fix_v1_to_v2, never fabricate the head offset while the sidecar contract is live. When canonical.trim/head_cut_s or created_at_utc is unusable AND raw/metadata.json carries a usable started_at_utc: recover the head offset from ground truth by exact-matching the raw mouse events against the (correct) v1 CSV over the delivered video's PTS — validate.py's _seed_shift_record.matches() is exactly this machinery — and write created_at = started_at + recovered_head; if unrecoverable, raise a typed FixFailed naming canonical.trim/created_at_utc (the safe pre-L1 end state) instead of writing a stamp that head-0 or head-NOW falsifies. Keep today's degrade arms for the no-sidecar route, where nothing downstream consumes the created-minus-started contract. Pin with a sidecar-bearing twin of test_v1_conversion_nondict_trim_and_canonical_degrade that asserts the delivered event placement (not just the stamp) survives the SYN_TS_NOT_PTS -> FIX_RETRANSLATE chain.

### #2 [MAJOR] pipeline/fix.py:95 (regressions-r18, refuters 0/2)

**L2 gate says 'usable' for dict metadata whose started_at_utc is absent/junk — retranslate still supersedes a working CSV fix and double-burns to a wrongful reject**

has_raw_sidecars (pipeline/fix.py:67-95, rewritten by L2 21c983e) returns True for ANY metadata.json that json-parses to a dict, and its docstring claims this is 'the condition retranslate_from_sidecars actually needs'. That claim is false: retranslate_from_sidecars unconditionally hard-requires a parseable recording.started_at_utc (fix.py:951-960) and raises typed FixFailed ('cannot derive the head offset: raw metadata started_at_utc (unusable)...') when it is missing or junk — and NO fix in the codebase writes raw/metadata.json, so the crash repeats identically on both attempts. Proved by execution against HEAD: a work dir with raw/inputs.jsonl + raw/metadata.json = {"game": {"name": "Kamla"}, "recording": {}} gives has_raw_sidecars=True; plan_fixes([SYN_TS_NOT_PTS], has_raw=True) returns {'steps': [('FIX_RETRANSLATE', {'rerouted': False}), ('FIX_SESSIONJSON_RECOMPUTE', {})], 'unfixable': []} — the CSV fix discarded per fix.py:241 — and retranslate_from_sidecars raises 'FixFailed: cannot derive the head offset: raw metadata started_at_utc (unusable) / session.json created_at_utc (ok) — session.json must be repaired before a re-translate'. The counterfactual plan with has_raw=False is [('FIX_TSREPAIR_PTS', {}), ('FIX_SESSIONJSON_RECOMPUTE', {})], which repairs the session. FixFailed is session-kind (apply_fixes fix.py:441-443), FIX_RETRIES=2 (pipeline/config.py:113), so continuous._fix_one (continuous.py:986-988, 901-903) burns both attempts and rejects 'fix retries exhausted'. The error message even misdirects the operator to repair session.json, which the same message says is 'ok'.

**Scenario:** A player's zip carries raw sidecars whose metadata.json parses (a hand-assembled bundle, an older/partial capture write, or a mojibake-damaged started_at_utc value that still parses under errors='replace') but has no usable recording.started_at_utc. qa FAILs sync (SYN_TS_NOT_PTS — or any retranslate-routed code). L2's gate reads the sidecars as USABLE, plan_fixes supersedes the CSV-level FIX_TSREPAIR_PTS that would have delivered the session, retranslate raises the same typed FixFailed on attempt 1 and attempt 2, and the session is terminally REJECTED 'fix retries exhausted' with the media wiped and the player unpaid. Had the player NOT attached the sidecars, the identical session repaired in one attempt — the exact 'having sidecars makes you strictly worse off' harm r18 #4 was confirmed for, surviving L2 for the semantic-unusable class its own rule ('unusable equals missing') claims to close.

**Finder's proposed fix (vetting in-iteration per the ruling):** Extend has_raw_sidecars to require what its docstring already claims: after the dict test, also require a parseable recording.started_at_utc (the same isinstance(str)+fromisoformat test retranslate's _utc applies), returning False otherwise so the CSV-level fallback plans — mirroring the L2 parse test. Keep the belt-and-braces FixFailed for residual paths.

### #3 [MAJOR] pipeline/fix.py:1594 (regressions-r18, refuters 0/2)

**L1's 'every read degrades' contract broken by one ragged v1 CSV row — IndexError burns both attempts into a wrongful terminal reject on the unrescuable route**

fix_v1_to_v2 (L1 e197244) indexes every row at fixed column positions with no length guard: the has_motion generator at fix.py:1594-1596 reads r[col['input_mouse_dx']] (index 5 of 7) and the row loop at 1597-1626 reads indexes 2-6. _read_csv (fix.py:810-814) returns csv rows verbatim, ragged rows included. Proved by execution against HEAD: truncating ONE body row of a v1 frames.csv to 3 cells makes apply_fixes return kind='session' error='FIX_V1_TO_V2: IndexError: list index out of range'. The L1 sweep's frames.csv verdict ('already typed — FixFailed missing input columns', commit e197244) covers only header-level column absence (fix.py:1544-1545), not row-level raggedness — and 'ragged/short rows (lost columns)' is an anticipated real class named in pipeline/validate.py's own r-loop-3 notes (validate.py:110). The route is unrescuable by construction, exactly as L1's own docstring says: check_session_v2 early-returns on key_binding.json present (translator/v2.py:702-705) before the CSV is ever scanned, so ARR_V1_FORMAT is the only reason emitted and no rows-surgery/header fix can ever precede this step. IndexError is not in apply_fixes' host tuple (fix.py:441-443) → session-kind → REVALIDATING re-derives the identical single-reason plan → attempt 2 crashes identically → FIX_RETRIES=2 exhausted → REJECTED.

**Scenario:** A v1 delivery whose frames.csv lost trailing cells on one row (truncated write, an unquoted newline, hand editing — the 'ragged/short rows (lost columns)' population validate.py already anticipates for v2 CSVs) enters the pipeline. The checker early-returns on key_binding.json, plan = [FIX_V1_TO_V2, FIX_SESSIONJSON_RECOMPUTE], FIX_V1_TO_V2 raises IndexError on both attempts, and a repairable session takes a terminal REJECTED 'fix retries exhausted' with wiped media and an unpaid player — the identical harm chain iteration 18 confirmed for the '1,5' motion cell, one commit after L1 claimed 'Every read of the player-typed v1 payload DEGRADES instead of crashing' (entry 86 names 'a junk shape that still crashes the route' a finding).

**Finder's proposed fix (vetting in-iteration per the ruling):** After _read_csv in fix_v1_to_v2, pad short rows to the header width (r += [''] * (len(header) - len(r))) — a missing cell degrades to the empty value every downstream parse already handles ('' keys split to none, _parse_motion('')=0.0), consistent with the L1 degrade doctrine. Add a ragged-row test beside the existing L1 pins.

### #4 [MAJOR] pipeline/fix.py:1644 (regressions-r18, refuters 0/2)

**L1's stamp/trim guard misses OverflowError — a JSON-legal huge or Infinity head_cut_s still crashes fix_v1_to_v2 on both attempts**

The L1 guard at fix.py:1635-1653 catches only (TypeError, ValueError), but the guarded arithmetic raises OverflowError for JSON-legal player-typed values: timedelta(seconds=float(head_cut_s)) at fix.py:1644-1645 and the datetime addition overflow. Proved by execution against HEAD through apply_fixes: canonical.trim.head_cut_s = 1e18 → kind='session' error='FIX_V1_TO_V2: OverflowError: Python int too large to convert to C int'; also proved raising for json.loads-accepted Infinity ('cannot convert float infinity to integer'), string '1e999' (float saturates to inf — entry 57's own documented semantics), and 1e12 ('date value out of range'). OverflowError is not in apply_fixes' host tuple (fix.py:441-443) → session-kind → identical crash on attempt 2 (the route re-plans the same single-step plan, translator/v2.py:702-705 early return) → REJECTED 'fix retries exhausted'. This directly violates the L1 commit's contract ('Every read of the player-typed v1 payload DEGRADES instead of crashing', e197244) and the house OverflowError doctrine that already guards five sibling sites against exactly this class (r12 #10 / r13 G4, entries 52/57: player-typed numbers raising OverflowError degrade, never crash).

**Scenario:** A v1 bundle whose canonical.trim.head_cut_s is a junk magnitude (a corrupted or hand-typed number: 1e18, Infinity — which json.loads accepts — or the string '1e999') beside a valid created_at_utc enters the pipeline. ARR_V1_FORMAT routes it to FIX_V1_TO_V2, the timedelta raises OverflowError past the (TypeError, ValueError) net on both attempts, and the repairable session is terminally REJECTED with wiped media and an unpaid player — the same wrongful-reject class L1 was written to close, one except-tuple short.

**Finder's proposed fix (vetting in-iteration per the ruling):** Add OverflowError to the except tuple at fix.py:1648 (except (TypeError, ValueError, OverflowError)) — the existing omit-and-synthesize arm is the correct degrade for it. Mirror the r13 G4 pattern used at the checker's numeric sites.

### #5 [MAJOR] pipeline/ledger.py:287 (payment-split, refuters 0/2)

**Identical-bytes zip re-upload over a rejected slot double-counts its hours on two sent payment sheets**

ingest.py:493-499 (zip-class re-upload branch) fires on a ctime-only signal — `(total_bytes != existing.bytes) OR (ds.ctime > existing.drive_ctime)` — and calls ledger.supersede(new_md5="") at ingest.py:526-529. supersede's UPDATE (ledger.py:284-291) nulls uploaded_reported_at, accepted_reported_at, tree_sealed_at and duration_raw_s UNCONDITIONALLY, before the bytes are knowable ('' is the zip class's unknowable-md5 sentinel). The download-time deferral (ingest.py:956-987) then adjudicates the prev_md5 breadcrumb: changed bytes re-clear (idempotent), but IDENTICAL bytes restore nothing — while the codebase's own ruled semantic for exactly this event is the opposite: reports.py:381-385 'identical bytes (ctime-only re-zip) let it stand', and test_r_loop13.py:177 test_identical_rezip_supersede_stamp_stands pins 'the same hours must never re-enter a later sheet' — but only through the mid-send arm-4 race, where the stamp happens to re-land on the reset slot. On the ordinary path (stamps landed on an earlier, fully-settled sheet) the cleared stamps stay cleared, the download probe refills duration_raw_s (ingest.py:1052-1054), and build_sheet_rows' in_window arm (reports.py:766, gated only on `not uploaded_reported_at`) re-counts the root. Executed proof (real ledger.supersede + real build_sheet_rows/mark_*_reported + the deferral block verbatim, scratch sqlite in /tmp): sheet N printed 'kamla_hrs_uploaded: 0.5 … kamla_rejection_reasons: afk', both stamps landed; supersede then showed "post-supersede: md5= '' up_rep= None acc_rep= None raw= None"; the identical-bytes deferral printed 'breadcrumb parsed: True | identical bytes -> True' and cleared nothing more but restored nothing; sheet N+1 printed the identical row again ('kamla_hrs_uploaded': 0.5, 'kamla_rejection_reasons': 'afk', counted: ['ZR'] accepted: ['ZR']) — 0.5h of byte-identical footage credited as uploaded hours on TWO sent sheets (1.0h total) plus the reject label twice, with zero payment-side loud lines (only the generic scan 'superseded' flag).

**Scenario:** A player's zip-origin session is validation-REJECTED and counted on a daily sheet (the normal population: every countable zip reject gets its uploaded hours + label counted and both stamps). The player re-uploads the SAME zip into the same Drive folder — the natural 'try again' action for a non-technical player, and the coached response to a quarantine ('a bad-archive quarantine is exactly what a re-upload corrects', ingest.py:508-511). Drive gives the re-uploaded file a new createdTime, so ds.ctime > drive_ctime satisfies the OR at ingest.py:497-498 with bytes unchanged, and the supersede clears both payment stamps. The re-download hashes identical, the deferral declines to clear (correct) but nothing restores the stamps, the probe refills duration_raw_s, and the next daily sheet re-credits the same uploaded hours and the same reject label on a second sent payment document. The operator pays per the sheets: the same footage's hours are paid twice, silently — the exact 'silent double-pay, the unrecoverable direction' class this lane's rulings name. Contrast proving it is a defect and not the ruled design: the md5-visible plain-file path refuses an identical-bytes supersede outright (ingest.py:449 requires vmd5 != stored md5 — 'the supersede rule needs new bytes', accepted entry 25), and the different-path quarantine heal PRESERVES stamps for vmd5=='' and defers to the same adjudicator (ingest.py:348-374). Only the same-path zip re-upload violates the counted-once invariant, and no accepted entry blesses it: entry 55 covers the mid-send arm-4 race and the breadcrumb, and its own identical-bytes arm states the stamp should stand.

**Finder's proposed fix (vetting in-iteration per the ruling):** Make the zip-class ('' ) supersede defer its payment clears exactly like its sibling '' heal: in ledger.supersede, when new_md5 is falsy and the row holds a real md5, PRESERVE uploaded_reported_at / accepted_reported_at / tree_sealed_at / duration_raw_s (keep every other reset: state->DISCOVERED, fix_attempts, reasons, delivered fields, dossier archive, breadcrumb). The download-time deferral (ingest.py:966-987) already applies the full supersede-style clear on changed bytes via the prev_md5 breadcrumb and writes the durable ZIP_ADJ_CHANGED marker, so changed bytes still re-enter loudly and identical bytes keep the stamps (counted once) — money-safe in the never-downloads case too (preserved stamps = no re-count). Both r13 pins (test_zip_supersede_mid_send_self_heals_at_download, test_identical_rezip_supersede_stamp_stands) still hold; add the missing ordinary-path regression: settled sheet -> identical-bytes zip re-upload supersede -> download -> next sheet must count nothing.

### #6 [MAJOR] pipeline/fix.py:1648 (fix-validate, refuters 0/2)

**L1's degrade arm misses OverflowError: junk head_cut_s still crashes fix_v1_to_v2 on both attempts — wrongful terminal reject**

L1 (e197244, UNREVIEWED) wrapped fix_v1_to_v2's stamp arm in `except (TypeError, ValueError)` (fix.py:1648), but the guarded operations raise OverflowError, which is NOT a subclass of either (verified: `issubclass(OverflowError, (TypeError, ValueError))` -> False). Three shapes escape: (1) canonical.trim.head_cut_s as a JSON bigint -> `float(10**400)` at fix.py:1645 raises `OverflowError: int too large to convert to float`; (2) '9'*400 or 'inf' -> float() gives inf, then `timedelta(seconds=inf)` raises `OverflowError: cannot convert float infinity to integer`; (3) '1e300' -> `created += timedelta(...)` at fix.py:1644 raises `OverflowError: Python int too large to convert to C int`. Proven end-to-end through the real plan/apply path: plan_fixes([ARR_V1_FORMAT]) -> [('FIX_V1_TO_V2', {}), ('FIX_SESSIONJSON_RECOMPUTE', {})]; apply_fixes returned `error='FIX_V1_TO_V2: OverflowError: ...' kind='session'` for all three shapes, and a second apply on the half-converted work dir (frames.csv already v2, key_binding.json still present so revalidation re-derives ARR_V1_FORMAT) crashed identically: attempt 2 output byte-identical to attempt 1. The bigint junk class is one this codebase already recognizes as real ('9'*400 cells — r12 #10; OverflowError arms in raw_int, the checker's ts casts and its duration compare per entries 19/52/57), and entry 86 explicitly declares 'a junk shape that still crashes the route' a finding. On this route no other repair can precede (check_session_v2 early-returns on key_binding.json present, translator/v2.py:702-705, so the plan is always exactly these two steps).

**Scenario:** A v1 upload arrives whose canonical.trim.head_cut_s was mangled to an absurd numeric (JSON bigint, '9'*400, '1e300', 'inf' — the same player-typed-junk population all four confirmed r18 L1 shapes came from, in the same file). Attempt 1: FIX_V1_TO_V2 raises OverflowError; apply_fixes classifies session-kind (no refund); driver sets REVALIDATING; revalidation re-derives ARR_V1_FORMAT (key_binding.json untouched — the crash precedes the unlink at fix.py:1656). Attempt 2: identical crash. Row is REJECTED 'fix retries exhausted (R2)'; since every stored reason is fixable, the reject surfaces as the bare fix-failed marker. A repairable session (the correct behavior — omit the unusable value and let fix_sessionjson_recompute synthesize, per the L1 design itself — is one except-tuple entry away) takes a wrongful terminal reject, the player goes unpaid, and up to two paid VLM sweeps are burned reaching it.

**Finder's proposed fix (vetting in-iteration per the ruling):** Add OverflowError to the except tuple at fix.py:1648 (making it `except (TypeError, ValueError, OverflowError)`), matching the sibling degrade arms the G4/r12-#10 sweep installed in translator/v2.py and translator/keybind.raw_int. Optionally clamp head_cut_s to a sane bound before the timedelta so a large-but-finite junk value cannot shift the stamp by centuries.

### #7 [MAJOR] pipeline/fix.py:1535 (tests-coverage, refuters 0/2)

**L1's non-dict-canonical degrade arm is arming-gate-invisible; the test claiming it covers only the trim half**

Deleting the L1 guard at pipeline/fix.py:1535-1539 (`if not isinstance(canonical, dict): canonical = {}`, landed in e197244 and recorded as a fix arm in settled entry 86) passes the FULL arming gate: in a scratch copy of HEAD outside the repo, `SUITE_FLOOR=798 bash tools/run_suite.sh --with numpy --with opencv-python` printed `802 passed in 150.59s` / `ARMING GATE OK: 802 passed (floor 798)` — byte-identical to the recorded Mac gate and to my unmutated baseline (`802 passed in 151.42s`, same gate line). Under the same mutant, a probe driving the suite's own `_v1_work` fixture with session.json `"canonical"` set to a non-dict printed: `canonical='bogus-string': CRASH AttributeError: 'str' object has no attribute 'get' at pipeline/fix.py:1624 in fix_v1_to_v2` (likewise for None and [1,2] — the first `.get` is `canonical.get("session_id", ...)`; `canonical.get("game", ...)` at line 1546 is short-circuited because the drivers always pass a truthy ledger `game`). At HEAD the same probe prints `OK -> 'converted v1 -> v2 (800 rows)'` for all three shapes. The one test whose name and docstring claim this arm — test_r_loop18.py:118 `test_v1_conversion_nondict_trim_and_canonical_degrade` ("a non-dict canonical would crash every .get. Both now degrade") — only ever sets `s["canonical"]["trim"] = "bogus"` (line 127): the canonical half of its own claim is never exercised, so the arm has no failing-side test anywhere in the 802-test gate.

**Scenario:** The L set landed unreviewed, and this loop's worst findings have repeatedly been executor regressions of prior fixes that the gate could not see (r15 #6, r18 #6/L3 — the finder's game_name=None mutant was gate-green at 793/789). Any future refactor of fix_v1_to_v2 that drops or bypasses this five-line guard re-arms the exact crash L1 closed while the flip's arming gate stays green at 802/802: a player-typed v1 payload whose session.json carries `"canonical": null` or a string (the same junk population L1's own tests model — its sibling test writes `game: 12345`, and sniff types the payload v1 on key_binding.json alone without parsing session.json) raises AttributeError, which apply_fixes' classifier (fix.py:441-443) types "session" — no refund — on BOTH attempts, and the driver terminally rejects a repairable session as "fix retries exhausted (R2)" (continuous.py:901-903): unpaid player hours under a bare fix-failed marker, invisible until it happens in production.

**Finder's proposed fix (vetting in-iteration per the ruling):** Add a failing-side test to the L1 cohort in pipeline/tests/test_r_loop18.py (or extend test_v1_conversion_nondict_trim_and_canonical_degrade to honor its own docstring): build `_v1_work`, rewrite session.json with `"canonical": "bogus"` (and/or null), assert `fix_v1_to_v2(work, "kamla")` returns the converted note, session_id falls back to the folder name, and recompute rebuilt session.json — the guard-deletion mutant proven gate-green above must fail it.

### #8 [MAJOR] pipeline/fix.py:1636 (tests-coverage, refuters 0/2)

**L1's str() coercion of created_at_utc is arming-gate-invisible: a non-str stamp still has no failing-side test**

Reverting pipeline/fix.py:1636 from `datetime.fromisoformat(str(ca).replace("Z", "+00:00"))` to `datetime.fromisoformat(ca.replace(...))` — deleting exactly the "str()-coerced parse" the L1 commit message and settled entry 86 record as a fix arm — passes the FULL arming gate in a scratch copy: `802 passed in 150.66s` / `ARMING GATE OK: 802 passed (floor 798)`. Under the mutant, a probe with canonical `created_at_utc: 12345` printed `CRASH AttributeError: 'int' object has no attribute 'replace' at pipeline/fix.py:1636 in fix_v1_to_v2` (likewise for a list value); AttributeError is outside the `except (TypeError, ValueError)` at line 1648, so the omit-and-synthesize degrade never engages. At HEAD both shapes print `OK -> 'converted v1 -> v2 (800 rows)'` with a synthesized stamp. The only unusable-stamp test, test_r_loop18.py:99 test_v1_conversion_unusable_created_at_is_synthesized, uses the STRING "not-a-date" — which exercises only the ValueError path that already existed inside the try — so the str() token protecting non-str stamps is coverage-free.

**Scenario:** A non-str created_at_utc is squarely inside the modeled junk population: the L3 test in this same file degrades a canonical field to the number 12345 (`s["canonical"]["game"] = 12345`) as its realism argument, and fix_v1_to_v2's own docstring says every read of the player-typed v1 payload must degrade. If the executor's next pass simplifies the parse and drops the str() (a one-token revert, the exact mutant class r15 #6/r18 #6 proved gate-green before), the gate stays 802/802 while any v1 session whose canonical stamp is a JSON number/list crashes AttributeError on both fix attempts -> session-kind classification (fix.py:441-443) -> terminal "fix retries exhausted" reject of a repairable session, an unpaid player, and no test that could ever have caught it.

**Finder's proposed fix (vetting in-iteration per the ruling):** Add one failing-side test beside test_v1_conversion_unusable_created_at_is_synthesized: set `s["canonical"]["created_at_utc"] = 12345` in the _v1_work session.json, assert the conversion completes and the written created_at_utc matches translator.v2._TS_RE (the synthesized-stamp assertion already used at test_r_loop18.py:113). The str()-drop mutant proven gate-green above must fail it.

### #9 [MAJOR] pipeline/fix.py:92 (tests-coverage, refuters 0/2)

**L2's errors='replace' read in has_raw_sidecars is arming-gate-invisible: non-UTF-8 metadata.json has no test at the plan gate**

Dropping `errors="replace"` from the L2 read at pipeline/fix.py:91-92 (`json.loads((raw / "metadata.json").read_text(encoding="utf-8", errors="replace"))`, landed in 21c983e and recorded in settled entry 87) passes the FULL arming gate in a scratch copy: `802 passed in 150.84s` / `ARMING GATE OK: 802 passed (floor 798)`. Under the mutant, a probe with `raw/metadata.json` containing `b'{"recording": {"started_at": 1}, "player": "Jos\xe9"}'` (JSON-valid once decoded with replace; latin-1 byte inside a string value) printed `CRASH UnicodeDecodeError: 'utf-8' codec can't decode byte 0xe9 in position 47` — UnicodeDecodeError is a ValueError but NOT a json.JSONDecodeError, so the `except (OSError, json.JSONDecodeError)` at fix.py:93 does not catch it and it escapes has_raw_sidecars. At HEAD the same probe prints `has_raw_sidecars -> True`. Every L2 test (test_r_loop18.py:158-218) writes pure-ASCII metadata via write_text, so the encoding arm is exercised by nothing in the 802-test gate; the non-UTF-8 population is acknowledged reachable by the code's own comment at the retranslate read (fix.py:906, 'player-typed free text, non-UTF-8 reachable').

**Scenario:** has_raw_sidecars is called bare at the drivers' plan step (continuous.py:909, run.py:582) — before apply_fixes' host/session classifier exists. If a future edit drops the errors= argument (a one-token revert of an unreviewed fix, the loop's canonical gate-green mutant class), the gate stays 802/802; in production a player-typed non-UTF-8 metadata.json then raises UnicodeDecodeError out of _fix_one into _session_runner's generic except (continuous.py:688-694): 'session runner crashed' alert + crash cooldown with the attempt at line 905 already charged, FIXING triage -> REVALIDATING -> re-plan -> second crash, until attempts hit FIX_RETRIES and the session is terminally rejected 'fix retries exhausted (R2)' (continuous.py:901-903) — a session HEAD would have retranslated or CSV-repaired, plus repeated crash alerts misleading the operator toward a host fault.

**Finder's proposed fix (vetting in-iteration per the ruling):** Add a failing-side test to the L2 cohort: write `raw/metadata.json` via write_bytes with the latin-1-inside-string payload above plus raw/inputs.jsonl, and assert `fix.has_raw_sidecars(work) is True` (parse succeeds under replace); optionally a sibling asserting structurally-broken non-UTF-8 bytes return False. The errors='replace'-drop mutant proven gate-green above must fail it.

### #10 [MINOR] pipeline/fix.py:1648 (regressions-r18, refuters 0/2)

**A junk head_cut_s VALUE discards the valid, parseable created_at_utc — delivered stamp silently becomes processing wall-clock time, contradicting L1's own committed claim**

L1 wrapped the stamp parse and the trim coercion in ONE try block (fix.py:1635-1653): float(trim_meta.get('head_cut_s') or 0.0) at 1644-1645 raising ValueError takes the except arm that omits the WHOLE stamp, and fix_sessionjson_recompute then synthesizes created_at_utc from datetime.now(timezone.utc) (fix.py:1468-1482) — not from any capture-time ground truth. Proved by execution against HEAD: canonical = {created_at_utc: '2026-08-10T15:34:03Z', trim: {'head_cut_s': 'abc'}} converts successfully but ships created_at_utc='2026-08-19T19:27:54.864203Z' (the probe's run time) — the valid capture stamp destroyed. The commit message (e197244) claims 'junk trim keeps the valid stamp' and its test asserts 'a junk trim must not cost the valid stamp' — but test_v1_conversion_nondict_trim_and_canonical_degrade pins only the NON-DICT trim shape ('bogus'), which the isinstance guard at 1632-1633 normalizes to {} before the float ever runs; the dict-with-junk-value shape was never covered. Entry 86 names 'a parseable stamp wrongly omitted' a finding class. Control proved in the same run: the non-dict shape keeps '2026-08-10T15:34:03.000000Z'.

**Scenario:** A v1 bundle carries a valid created_at_utc beside trim: {'head_cut_s': '5,0'} (the exact locale-comma junk class L1's dx/dy fix was built for, hitting the same payload). The conversion succeeds but the delivered session.json's created_at_utc and ended_at_utc silently become the pipeline's processing time — days after capture — the same silent delivered-metadata-corruption class as confirmed r15 #7. Worse, if the bundle has usable sidecars and revalidation later FAILs any retranslate-routed code, retranslate_from_sidecars derives head_s = (now-stamp − started_at_utc) ≈ days (fix.py:960), the rebase leaves zero in-band events, and the zero-events refusal (fix.py:996-1002) burns the remaining attempt into a terminal reject of a session whose only original defect was one junk trim value.

**Finder's proposed fix (vetting in-iteration per the ruling):** Parse head_cut_s in its own nested try inside the stamp block: on (TypeError, ValueError, OverflowError) degrade the head cut to 0.0 and KEEP the parsed stamp — making the code match the commit's own claimed contract. Add a dict-with-junk-value trim test beside the existing non-dict pin.

### #11 [MINOR] pipeline/validate.py:974 (fix-validate, refuters 0/2)

**L2 drift: aux['has_raw'] stays existence-based while the plan gate is parse-based — unusable-sidecar rejects mislabeled as 'fix-failed'**

L2 (21c983e, UNREVIEWED) tightened fix.has_raw_sidecars to require raw/metadata.json to PARSE to a dict, but validate.py:974 still sets `aux["has_raw"] = bool(raw_by_sid)`, where raw_by_sid is populated on file EXISTENCE alone (validate.py:931-933). Pre-L2 the two sides agreed (existence vs existence); post-L2 a present-but-corrupt metadata.json makes validate store QA_FAIL_UNMAPPED with fixable=true (bin 2) while plan_fixes, fed has_raw=False by the gate, classifies the same code unfixable. Proven: plan_fixes([QA_FAIL_UNMAPPED fixable=true], has_raw=False) -> {'steps': [], 'unfixable': ['QA_FAIL_UNMAPPED']} so both drivers reject with 'unfixable: [QA_FAIL_UNMAPPED]' (run.py:604-608, continuous.py:928-932); reports.session_reject_labels on the STORED reasons (blocking AND NOT fixable, per the ruled reporting contract) then returns ['fix-failed'] — verified by execution — because the stored fixable field says true. The kickoff's own check ('no has_raw consumer drifts from the gate ... in a way that ACTS on the disagreement') is violated on the reject-reporting surface.

**Scenario:** A v2 upload carries sidecars whose metadata.json is truncated/corrupt (exactly r18 #4's population), plus one unmapped qa FAIL (e.g. the deliberately-unmapped 'frame spacing: timestamp_ms or fps values out of range'). Validation stores QA_FAIL_UNMAPPED fixable=true and bins 2; the fix phase increments fix_attempts, plans, and immediately rejects 'unfixable'. The Telegram/daily reject line — the surface Adnaan ruled must show unfixable reasons exhaustively (08-15, commit 6b1b0db) — prints only the fix-failed marker: a false statement (no fix ran or failed), hiding the truthful cause (unmapped FAIL + unusable sidecar) from the operator and misdirecting player re-record coaching. The ledger also records a consumed fix attempt that never dispatched.

**Finder's proposed fix (vetting in-iteration per the ruling):** Make validate_session compute the aux flag with the same rule as the plan gate — lazy-import fix.has_raw_sidecars (fix.py already lazy-imports validate helpers, so no cycle) and set aux['has_raw'] from it, keeping raw_by_sid existence-based for the engine/seed paths which degrade internally. Then a session with unusable sidecars stores fixable=false, bins 3 directly, and its reject label prints the truthful unfixable reason.

### #12 [MINOR] pipeline/fix.py:1483 (fix-validate, refuters 0/2)

**fix_sessionjson_recompute overflows on a parseable near-year-9999 stamp — the rewrite crashes on the class it exists to repair**

fix_sessionjson_recompute guards the created_at_utc PARSE with `except (AttributeError, ValueError)` (fix.py:1468) but not the arithmetic: `ended = created + timedelta(seconds=info.duration_s)` at fix.py:1483 raises OverflowError ('date value out of range') whenever the stamp parses to within clip-duration of datetime.max. Proven by execution against a real probed video: session.json with created_at_utc '9999-12-31T23:59:59.000000Z' -> `fix.fix_sessionjson_recompute(work, 'kamla')` raised `OverflowError: date value out of range`. Such a stamp is regex-conformant and parseable, so both the parsed_ok branch and the re-emit branch keep `created` at year 9999 and reach the addition. The checker itself was hardened for exactly this junk family (OverflowError arms at translator/v2.py:795/917/934, entries 52/57), but the fix that its STR_SJ_INVALID verdict routes to was not: every plan containing FIX_SESSIONJSON_REWRITE — and every plan's terminal FIX_SESSIONJSON_RECOMPUTE — crashes session-kind.

**Scenario:** A v2 session.json arrives with an absurd-but-parseable created_at_utc in the year-9999 tail (player-typed junk, same threat model as the confirmed r18 set). The checker FAILs 'ended_at_utc <=' -> STR_SJ_INVALID -> plan [FIX_SESSIONJSON_REWRITE, ...]. The rewrite — whose r-loop-8 design is precisely to overwrite present-but-invalid stamps from ground truth — raises OverflowError instead; apply_fixes classifies session-kind, the attempt burns, revalidation re-derives the same verdict, attempt 2 crashes identically, and a session with sound footage is REJECTED 'fix retries exhausted' under the bare fix-failed marker: an unpaid player and a wrongful terminal reject the degrade-never-crash doctrine forbids.

**Finder's proposed fix (vetting in-iteration per the ruling):** Guard the ended/astimezone arithmetic: catch OverflowError around fix.py:1483 (or test `created > datetime.max - timedelta(seconds=info.duration_s)`) and fall back to the parsed_ok=False path (synthesize created from now, its designed degrade), mirroring the checker's own OverflowError arms.

### #13 [MINOR] pipeline/validate.py:132 (fix-validate, refuters 0/2)

**Non-object session.json routes to a FIX_RETRANSLATE that provably cannot succeed; the registry's own cure is never planned**

check_session_v2 FAILs 'session.json is not a JSON object' and early-returns (translator/v2.py:712-714), so the FAIL always arrives alone; it matches no needle in _QA_STR_MAP -> QA_FAIL_UNMAPPED. With sidecars, plan_fixes routes that to FIX_RETRANSLATE — but retranslate_from_sidecars READS session.json for the head offset (_read_session_json -> {} for non-dict) and deterministically raises FixFailed. Proven by execution: sniff_payload on a work dir whose session.json is '["not","an","object"]' returns 'v2' (ingest.py:870 — the garbage arm catches only JSONDecodeError/OSError, so parseable-non-object payloads pass download and reach validation); _map_qa_issues mapped the checker's exact FAIL to QA_FAIL_UNMAPPED (blocking, fixable); plan_fixes produced [('FIX_RETRANSLATE', ...)]; apply_fixes returned `error="FIX_RETRANSLATE: FixFailed: cannot derive the head offset: raw metadata started_at_utc (ok) / session.json created_at_utc (unusable) — session.json must be repaired before a re-translate" kind='session'` — the failure message itself names the cure (FIX_SESSIONJSON_REWRITE, which rebuilds from the {} read; the exact dependency r-loop 7 fixed for STR_SJ_INVALID at fix.py:195-209) and nothing ever plans it. The r-loop-3 comment at validate.py:104-113 claims 'no fix can clear them ... retranslate-when-sidecars-exist behaviour is already the right answer', which r-loop 7's _read_session_json/{}-rebuild capability falsified for this member: retranslate works for the OTHER unmapped members (frames.csv unreadable, ragged rows — it rewrites the CSV without reading it) but cannot ever clear this one.

**Scenario:** A producer bug or hand edit leaves session.json as a JSON array or string (parseable, non-object) beside a clean video, clean frames.csv and usable raw sidecars. Download sniffs v2; validation stores QA_FAIL_UNMAPPED fixable=true; attempt 1 dispatches FIX_RETRANSLATE which FixFails naming the repair; revalidation re-derives the identical single-FAIL verdict; attempt 2 fails identically; REJECTED 'fix retries exhausted' under the fix-failed marker — two burned attempts and paid VLM sweeps to terminally reject a session FIX_SESSIONJSON_REWRITE would have delivered in one step (recompute rebuilds a valid session.json from probe+CSV ground truth from a {} start — its designed r-loop-7 job). The sidecar-less twin is bin-3 rejected on the spot, equally wrongfully: having the required sidecars again made a session strictly worse off, the exact r-loop-7/L2 shape.

**Finder's proposed fix (vetting in-iteration per the ruling):** Map the two checker strings 'session.json unreadable' and 'session.json is not a JSON object' to STR_SJ_INVALID in _QA_STR_MAP (insertion point validate.py:132) — STR_SJ_INVALID already plans FIX_SESSIONJSON_REWRITE before any retranslate and never routes through it (entry 11 semantics preserved), so the rewrite repairs the artifact and revalidation decides; update the stale validate.py:104-113 rationale for these two members.
