# R12_FINDINGS.md — review iteration 12, findings of record

**Run 2026-08-19** by the F1–F11 executor session via the workflow
snapshot `tools/review/flip-review-iter12.js` (fix-in-iteration per plan
§5; regressions lane targeted at the F-commits). 7 finder lanes, 2-vote
refute, 37 agents (37 completed, 0 errors), HEAD 11af5a0 (code HEAD
2f5ca04 + floor 09cbf20).

**Pre-fix verdict: NOT QUIET — 15 raised → 15 confirmed (0 blockers),
0 killed.** ALL 15 were FIXED IN-ITERATION (commits 986368f..0ad8747 +
floor bump 6b87023; every behavioural fix fail-first-proven at 11af5a0,
every pin mutation-proven against the finders' exact mutations), after
which the iteration judges **QUIET per R5_TRIAGE §7** (zero confirmed
blockers, every confirmed finding fixed in-iteration, gates green on
both hosts at the new floor 692). Iteration 13 runs REGARDLESS
(re-ruled by Adnaan 2026-08-19, reverting his same-day amendment).

**Known duplicates (same defect, multiple lanes):** #1 ≡ #2 ('' md5
sentinel read as byte change), #3 ≡ #12 (rebuild-reset bare lock check),
#5 ≡ #8 (fix_actions_context on the built-in keybind), #11/#13 fixed
together (regen interlock at the daily send + fail-closed interlock).

Machine-readable results incl. both refuter verdicts: session
scratchpad `r12-results.json`.

## Confirmed findings (all fixed in-iteration)

### #1 [MAJOR] pipeline/reports.py:319 (regressions-r11, refuters 0/2) — FIXED 986368f (with #2)

**F7 CAS treats the '' (unknowable) md5 as byte identity — zip-heal md5 backfill falsely skips the stamp and the same uploaded hours land on two sent payment sheets**

F7's _stamp (reports.py:309-327) makes the payment stamps compare-and-set on build_sheet_rows' md5 snapshot, guarding only `if m is None` (line 319) before CAS-ing via update_where_md5 (ledger.py:190-211). But md5_video='' is this codebase's standard 'md5 UNKNOWABLE' sentinel for zip payloads — ingest.py:281-289 explicitly rules ''-comparisons must not be read as byte change (r-loop 10 #4) — and '' changes to a real hash WITHOUT any byte change on the download-time backfill: the QUARANTINED-path heal writes md5_video=vmd5='' while deliberately preserving duration_raw_s and the (absent) payment stamps (ingest.py:290-307, clears={} when vmd5 is falsy), and the re-download then recomputes the local hash, finds prev_md5 == local (same bytes, no clear) and writes `ledger.update(session_id, md5_video=local_md5)` (ingest.py:857-878). Probe against HEAD (scratch ledger, production wiring build_sheet_rows(md5_out)→mark_uploads_reported(md5s)): sheet1 counts the healed root (kamla_hrs_uploaded 1.0, snapshot {root: ''}); after the mid-window backfill, output: "[sheet-stamp] …zheal: bytes changed since the sheet counted them (supersede/heal mid-send) — uploaded_reported_at SKIPPED", stamped: 0; the next window's sheet re-enters the root through the late guard (reports.py:618-620) and prints kamla_hrs_uploaded 1.0 again — 'DOUBLE-COUNT CONFIRMED: the same 1.0h of uploaded hours landed on BOTH sent sheets'. Differential against the pre-fix tree at 1500d95 (git-archive copy, same scenario):…

**Fix applied** (see the commit for the final shape): In _stamp, treat a falsy snapshot value like a missing one — `if not m:` → unconditional stamp — because '' means the bytes are UNKNOWABLE, not changed (the same doctrine ingest.py:281-289 already applies to heals: never treat a ''-comparison as a supersede signal). This keeps the F7 CAS fully effective for every root with a real md5 (the motivating supersede-mid-send race stamps real hashes on both sides) and reverts only the unknowable zip class to the pre-F7 unconditional stamp, whose byte-change adjudication already belongs to the download-time prev_md5 comparison (ingest.py:867-877). Optionally extend _resume_daily_send._bytes_changed (run.py:1094-1095) with the same `rec_md5` falsy-gua…

### #2 [MINOR] pipeline/run.py:1095 (payment-split, refuters 0/2) — FIXED 986368f (with #1)

**Zip-class heal's md5→"" rewrite defeats the counted-once invariant: resume/CAS read 'unknowable' as 'changed bytes' and the identical-bytes root is paid on two sheets**

The quarantine heal for listings with no Drive-side video md5 deliberately PRESERVES payment stamps and duration_raw_s (r-loop 10 #4: vmd5=="" is 'UNKNOWABLE, not different', ingest.py:281-294) but still rewrites md5_video to "" (ingest.py:300, `md5_video=vmd5` unconditionally). The resume pre-filter `_bytes_changed` (run.py:1094-1095: `rec_md5 is not None and rows[sid]["md5_video"] != rec_md5`) and the `_stamp` CAS (reports.py:318-327 via ledger.update_where_md5) then read "" != recorded-md5 as 'a clearing tool ran here' and SKIP the stamp — but no clearing happened: duration_raw_s survives, so the root stays countable and unstamped. RAN probe (probe_d.py, output above): fresh send counts root Z (1.0 h, record md5 map {'Z':'abc123'}) but Telegram is down → pending; in the gap Z is quarantined (worker double-death class), the operator renames its folder and the zip-class heal fires (exact ingest.py column writes: clears={}, md5_video=""); the resume then prints '[daily] resume: Z was re-uploaded with new bytes ... NOT re-stamping' and sends the on-disk CSV whose row reads '...,1.0,0.0,...' — while Z is left `up_stamp None | dur 3600.0`. Identical local bytes mean the download-time deferred clear correctly does NOT fire ('prev abc123 vs local abc123 -> clear fires: False', ingest.py:872-877), so nothing ever reconciles the miss: the next sheet re-counts Z via the LATE guard ('counted AGAIN: [Z]', kamla_hrs_uploaded 1.0) with only a routine LATE ARRIVAL line. The r10 #4 doctrin…

**Fix applied** (see the commit for the final shape): Teach the two skip sites the same 'unknowable' vocabulary the heal already has: in `_bytes_changed` (run.py:1084-1095) and `_stamp` (reports.py:309-327), when the row's CURRENT md5_video is "" and its newest heal event's prev_md5 equals the recorded snapshot md5, treat the bytes as unchanged and stamp. This composes correctly with the existing deferral: if the download later proves the bytes different, ingest.py:872-877 clears duration_raw_s + both stamps (supersede semantics) and the new hours re-enter; if identical, the stamp stands and the hours are counted exactly once. Pin with a test mirroring probe_d.py.

### #3 [MINOR] tools/recal_rebuild_reset.py:38 (payment-split, refuters 0/2) — FIXED cd8546a (with #12)

**recal_rebuild_reset still uses the bare run-lock existence check r-loop 1 condemned — a systemd-started driver can run a full pipeline concurrently with the blanket teardown**

recal_rebuild_reset.py:38-39 guards with `if cfg.lock_dir.exists(): ... return 2` and never acquires or holds the lock, while BOTH sibling recal tools were explicitly fixed for this exact hole in r-loop 1: recal_refix_reset.py:107-131 and recal_regen_sheets.py:196-210 each carry the docstring 'HOLDS the run lock for the tool's whole duration (r-loop 1): the old bare existence check left a TOCTOU where systemd Restart=always could start the continuous driver mid-reset' and wrap `_locked_main` in acquire_lock/release_lock. Verified in the systemd dir: hl-pipeline.timer.in fires `OnCalendar=*:0/30` and hl-continuous.service.in has `Restart=always` + `RestartSec=10` (grep output above). The rebuild tool's run spans minutes (dossier-archive shutil.move loop over every session — ~1400 on the 08-18 rebuild — then per-root UPDATEs, then line 128's blanket `DELETE FROM sessions WHERE parent_id IS NOT NULL`, then work-dir wipes), so a 30-minute timer tick or a Restart=always respawn landing inside it is likely whenever the freeze step is missed; the arriving driver calls acquire_lock, finds the lock FREE (the tool never took it), and runs the full pipeline concurrently.

**Fix applied** (see the commit for the final shape): Adopt the sibling pattern verbatim: `from pipeline.run import acquire_lock, release_lock`; in main() replace the bare `cfg.lock_dir.exists()` check with `if not acquire_lock(cfg): ... return 2` and run the body in a try/finally that releases the lock — exactly the r-loop 1 shape already proven in recal_refix_reset.py:113-131.

### #4 [MINOR] pipeline/run.py:1990 (driver-core, refuters 0/2) — FIXED 362ced9

**DISCOVERED-media reclaim grace is permanently zero after the first reclaim; hourly sweep then wipes minutes-old resumable partials**

The reclaim at run.py:1990-1992 (shutil.rmtree + _drop_shift_entry) writes NO ledger event and no marker (verified: no set_state/event anywhere on that path), while the F8 anchor at run.py:1981-1987 and continuous.py:1356-1368 computes first_disc as MIN(DISCOVERED ts) after the first DOWNLOADING of the current intake stint — a boundary that only moves on an event OUTSIDE {DISCOVERED, DOWNLOADING}. A never-successful row bouncing DISCOVERED<->DOWNLOADING has no such event, so first_disc stays frozen at its first failure forever, including AFTER a reclaim. Proved by executing the shipped functions against a seeded ledger (first failure 20h ago, latest retry re-created the partial 10 min ago): `after hourly sweep, 10-min-old partial exists: False`, and the digest's _stuck_lines printed `['s-neversucc2 (DISCOVERED(media) 20.0h)']` for the 10-minute-old bytes. Control test in the suite (test_never_successful_row_still_reclaimed) only pins the FIRST reclaim; nothing covers re-created partials after it. This is not the settled F8 scope statement ('never-successful rows keep today's behaviour') — that ruled the anchor change's blast radius; the post-reclaim collapse contradicts the r-loop-6 fix's own stated purpose in the adjacent comment (run.py:1962-1966: the documented bug was precisely 'the next hourly sweep deleted a partial multi-part transfer that rclone --checksum would otherwise have resumed', i.e. effective grace ZERO).

**Fix applied** (see the commit for the final shape): Re-arm the anchor at reclaim time: before the rmtree at run.py:1990, write a marker event (e.g. ledger.set_state(sid, 'DISCOVERED', 'media reclaimed by sweep') — same-state audit event, the codebase's established marker pattern), and add `AND e.ts > COALESCE((SELECT MAX(ts) FROM events WHERE session_id=? AND detail='media reclaimed by sweep'), '')` to the first_disc term at BOTH sites (run.py:1981-1987 and continuous.py:1356-1368), so the age restarts from the first failure after the last reclaim. Alternatively (no schema/event change): additionally gate the rmtree on the newest file mtime under the dir being >= CONT_DISCOVERED_RECLAIM_H old, which directly encodes 'these bytes had their gra…

### #5 [MAJOR] pipeline/fix.py:1115 (fix-validate, refuters 0/2) — FIXED 02bdeac (with #8)

**F4 incomplete: FIX_ACTIONS_CONTEXT re-resolves actions under the BUILT-IN keybind, undoing fix_key_hygiene's session-keybind resolution in the same plan**

F4 (commit 206815c) made fix_key_hygiene resolve the session's own raw/keybind.json (fix.py:1016-1021), but fix_actions_context still builds its resolver from the built-ins: fix.py:1115-1119 `kb = dict(KEYBINDS[game]); kb.update(KEYBIND_PATCHES...); apply_context_to_rows(rows, track, game, build_resolver(kb), ...)`. apply_context_to_rows (translator/v2.py:127-173) REWRITES input_actions for EVERY row from the passed rules (`row[-4] = new`) and dead-strips literals whose built-in semantic is context-blocked. plan_fixes couples the two unconditionally on OW: fix.py:320-322 appends FIX_ACTIONS_CONTEXT after any FIX_KEY_HYGIENE (and directly for INP_FANOUT). The correct model the F4 comment cites — retranslate_from_sidecars — passes SESSION-resolved rules to apply_context_to_rows (fix.py:910-914, 981-985); fix_actions_context is now the only action-writer left on the built-ins. Probe (real output, /private/tmp/.../scratchpad/probe1_context_undo.py): custom OW keybind {general_primary_interact: g, movement_jump: x}; after hygiene: keys=['g'] actions=['general_primary_interact']; after fix_actions_context with the verbatim fix.py:1115-1119 kb: frame 0 -> keys='g' actions='' (checker's `input_keys but null input_actions` FAIL re-fires), frame 1 -> keys='' actions='' with summary dead_press_strips {'x': 1} — the player's jump press is DELETED entirely (built-in binds x to cockpit-only flight_autopilot), leaving a legal null row no checker test can see.

**Fix applied** (see the commit for the final shape): In fix_actions_context (fix.py:1093-1128), resolve the keybind exactly as the F4'd fix_key_hygiene does: if (work/'raw'/'keybind.json').exists(), kb = resolve_keybind(keybind_path=kbp, game_name=game, exe_name=None), else the built-in; then kb.update(KEYBIND_PATCHES.get(game, {})). This matches retranslate_from_sidecars' behaviour, which already applies context gating with session-resolved rules.

### #6 [MAJOR] pipeline/fix.py:320 (fix-validate, refuters 0/2) — FIXED af35f44

**plan_fixes emits FIX_ACTIONS_CONTEXT (and FIX_KEY_HYGIENE) before FIX_ROWS_SURGERY in cut-less plans — the context fix hard-fails on any rows/video mismatch, terminally rejecting the INP_FANOUT pairing**

The 'structural surgery first' rule (review-r4 #23) is enforced only via _pre_cut_csv_fixes, which is invoked solely on cut/gate/retrim-bearing plans (fix.py:248, 274, 294, 305). In a plain-CSV plan the hygiene loop (fix.py:311-317) and the context step (fix.py:320-322) are appended BEFORE the structural loop (fix.py:325-331). Probe (real output, /private/tmp/.../scratchpad/probe2_plan_order.py, calling the real plan_fixes): reasons [INP_FANOUT, STR_ROWS_MISMATCH], game=outer_wilds, has_raw=False -> steps ['FIX_ACTIONS_CONTEXT', 'FIX_ROWS_SURGERY', 'FIX_SESSIONJSON_RECOMPUTE']; [INP_OSKEYS, STR_ROWS_MISMATCH] -> ['FIX_KEY_HYGIENE', 'FIX_ACTIONS_CONTEXT', 'FIX_ROWS_SURGERY', ...]. fix_actions_context raises FixFailed('context track N != M rows') at fix.py:1112-1114 whenever CSV rows differ from decoded video frames, because classify_video returns one label per VIDEO frame (translator/context.py:118-131). The checker emits 'video frame count N != csv rows M' (v2.py:839, mapped STR_ROWS_MISMATCH) WITHOUT early return, so it co-occurs with the fan-out FAIL (v2.py:816) in one pass. apply_fixes stops at the first error (fix.py:402-422), so FIX_ROWS_SURGERY — which would fix the count to the video and let context succeed — never runs.

**Fix applied** (see the commit for the final shape): Emit the structural repairs ahead of hygiene/context in the cut-less path too: call _pre_cut_csv_fixes() (or equivalently move the _CSV_ORDER structural loop) before the hygiene loop at fix.py:311, mirroring what every cut/gate/retrim plan already does. fix_actions_context's own track-length precondition (fix.py:1114) is precisely the dependency the r4 #23 rule exists to serve.

### #7 [MINOR] pipeline/validate.py:491 (fix-validate, refuters 0/2) — FIXED 9270d50

**CNT_NOTIF_EDGE/CNT_CHAT_PII edge cuts have no post-cut length check — a 70-74s clip burns its fix budget reaching an inevitable CNT_SHORT reject under a degraded reason**

The CNT_EDGE_NONGAMEPLAY arm checks the remaining length at map time and emits CNT_SHORT directly when trimming would leave < MIN_CLIP_S (validate.py:332-341, 344-350). The sibling notification/chat arms do not: validate.py:490-495 (CNT_NOTIF_EDGE) and 500-509 (CNT_CHAT_PII edge) emit a fixable reason unconditionally, and plan_fixes turns them into head_cut = t + 1.0 / tail_cut = t - 1.0 with no length test (fix.py:173-178). For a probed 70-74s clip with a confirmed edge notification at t<=3, the retrim leaves duration - (t+1.0) < 70s: revalidation's probed-duration CNT_SHORT (validate.py:567-572, blocking unfixable) then rejects; the tail variant rejects via cutter's 'split produced no >=70s segment' (run.py:664-670).

**Fix applied** (see the commit for the final shape): Mirror the CNT_EDGE arm: in _map_flags, when (probed duration - (t + 1.0)) < C.MIN_CLIP_S for a head-edge notification (resp. (t - 1.0) < C.MIN_CLIP_S for tail), emit CNT_SHORT (blocking, unfixable, with post_cut_s) instead of the fixable edge code — using aux['probed_duration_s'] the way map_reasons' own CNT_SHORT already does.

### #8 [MAJOR] pipeline/fix.py:1115 (translator, refuters 0/2) — FIXED 02bdeac (with #5)

**FIX_ACTIONS_CONTEXT resolves with the built-in keybind, silently re-labeling every custom-bound OW session — the exact defect F4 just fixed in hygiene, one call site over**

fix_actions_context builds its resolver from `kb = dict(KEYBINDS[game])` (fix.py:1115) — never work/raw/keybind.json — and apply_context_to_rows (translator/v2.py:165-169) REPLACES input_actions on every row from those rules. Translate (v2.py:337-343), retranslate_from_sidecars (fix.py:910), and post-F4 fix_key_hygiene (fix.py:1017, commit 206815c) all resolve the session's own keybind.json; this step and its operator twin tools/fix_actions_from_v2.py:86 are the only sites left on the built-ins. Probe (real fix_actions_context call on a work dir with raw/keybind.json binding Space→general_primary_interact, J→movement_jump, context stubbed to on_foot): 'fix returned: context gating: 8 frames changed' — Space-frames rewrote 'general_primary_interact' -> 'movement_jump' (built-in Space semantic, checker-green: keys still have actions), and J-frames rewrote 'movement_jump' -> '' with 'J' left in input_keys (4 keys_no_action frames). The step is mandatory after hygiene on OW (plan_fixes fix.py:320-322 appends FIX_ACTIONS_CONTEXT whenever hygiene is planned and game=='outer_wilds'), and INP_OSKEYS/INP_TOKEN_CASE/INP_BLEED plan hygiene even with raw sidecars present (fix.py:159-160); INP_FANOUT on OW routes straight to context_fix (fix.py:152-154) without setting retranslate.

**Fix applied** (see the commit for the final shape): In fix_actions_context (and tools/fix_actions_from_v2.py), resolve the keybind exactly as F4 made fix_key_hygiene do: if work/raw/keybind.json exists, kb = resolve_keybind(keybind_path=kbp, game_name=game) (built-in fallback comes along for unusable files), else dict(KEYBINDS.get(game, {})); then apply KEYBIND_PATCHES and build the resolver from that.

### #9 [MAJOR] tools/analyze_sample.py:587 (translator, refuters 0/2) — FIXED 1f981c1

**vlm_sweep builds its sampling grid from session.json's CLAIMED duration_seconds — an Infinity/huge claim hangs the validation worker forever, and nothing bounds the worker**

analyze() takes a.duration_s from the player-supplied session.json claim (analyze_sample.py:1327, `_num(s.get("duration_seconds"), float, 0.0)` — floats pass through, including inf) and hands it to vlm_sweep (line 1432), whose grid loop `while t < duration_s - 0.6: base.append(...)` (lines 585-589) has no bound but the claim itself. Probe output: json.loads('{"duration_seconds": 1e999}') -> inf; _num keeps it; running the real vlm_sweep with duration inf under a 3s alarm: 'ALARM after 3s: still executing analyze_sample.py:588 line: base.append(round(t, 1))' — unbounded loop confirmed. check_session_v2 emits only non-terminating typed FAILs for the same claim (duration_ms/duration mismatch at v2.py:576-584, video-duration mismatch at 840) and analyze() continues past them to the sweep. Neither driver bounds the worker: run.py:303 `ex.map(_validate_worker, jobs)` and continuous.py:743-745 `ex.map(_WORKER_FN, [job])` pass no timeout. D2 (memory entry 26) already ruled the claimed duration untrusted for the length gates — the sweep still consumes it raw. A finite ms-in-seconds corruption (e.g. 8.2e7) is the same bug without OOM: ~20M sample times, each a grabber.jpeg seek+decode, days of a pinned worker.

**Fix applied** (see the commit for the final shape): Clamp the sweep to the video's real timeline: analyze() already runs probe_streams before the sweep — pass min(claimed, probed format duration) (or derive duration_s from the probe alone) into vlm_sweep, and add a hard cap on len(base) (e.g. duration capped at 24h) as defense in depth; separately consider a timeout on the validation worker map.

### #10 [MINOR] translator/v2.py:850 (translator, refuters 0/2) — FIXED 75db7fd

**Unhandled OverflowError in the qa-v2 frame-sync compare and in analyze_sample's inventory cast — a huge numeric cell turns a typed, fixable FAIL into terminal QUARANTINED**

Two sibling sites miss the OverflowError arm that raw_int (binner.py:66-85) and _px (v2.py:247-256) were explicitly given in r-loops 8/9. Site B: check_session_v2's `worst = max(abs(ts[i] - pts[i] / 1000.0) ...)` (v2.py:850) — ts comes from int() which parses arbitrary-precision integers, and bigint-minus-float converts the int to float. Probe on a real 90-frame ffmpeg video with one timestamp_ms cell of '9'*400: 'site B CONFIRMED: OverflowError(int too large to convert to float) raised OUT of check_session_v2 at v2.py:850 — typed FAIL verdict destroyed' (the checker had already recorded the non-monotonic FAIL at line 726-727, which the crash discards). Site A: inventory()'s `ts.append(int(float(r[col["timestamp_ms"]])))` at analyze_sample.py:223 catches only (TypeError, ValueError, IndexError); a '1e999'/'inf' cell floats to inf and int(inf) raises: 'site A CONFIRMED: OverflowError(cannot convert float infinity to integer) at analyze_sample.py:223' — this fires even when check_session_v2 has just returned the typed 'timestamp_ms column unparseable' FAIL, because analyze() continues to inventory regardless. _validate_worker classifies OverflowError as kind='crash' (run.py:181-182, not in the host tuple), and both drivers write QUARANTINED 'validation crashed' for crash-kind (run.py:331-334, continuous.py:829-833).

**Fix applied** (see the commit for the final shape): Site A: add OverflowError to the except tuple at analyze_sample.py:223. Site B: keep the comparison out of the float domain (compute abs(ts[i]*1000 - pts[i]) in integer microseconds) or wrap the max() in try/except OverflowError -> r.fail('timestamp_ms values out of range'), matching the checker's degrade-never-crash contract.

### #11 [MAJOR] /Users/adnaan/Documents/hl-projects/hl-gamedata/pipeline/run.py:1211 (ops-tools, refuters 0/2) — FIXED b1bea89 (with #13)

**Daily send ignores a pending regen record — cohort hours land on two sent payment sheets with no gate firing (F10 one-sided)**

F10 taught pending_daily_send (pipeline/reports.py:438-440) that a reports/<day>/.regen-v2-counted.json without its .regen-v2-done marker is a pending resumable send, but only the two reset tools consult it. send_daily_report_if_due (run.py:1145-1230) never does: its day-agnostic scan (run.py:1174-1199) looks only for .daily-counted.json, and the F1 fresh-path guard (run.py:1211) checks only today's .wedged/.daily-counted.json. Ran the code (probe in scratchpad, real output): with a pending regen record for 2026-08-16 whose counted list holds root1, `reports.pending_daily_send(cfg)` returned '2026-08-16' — yet `run.send_daily_report_if_due(...)` returned True, printed '[sheet] LATE ARRIVAL: root1 ... counted in the current sheet (conservation)', wrote root1's 1.0 uploaded h / 0.97 accepted h into payment-2026-08-19.csv, sent message+document, and stamped both marks. A subsequent `recal_regen_sheets --send` re-run then exited rc=0 with no abort: the stray-stamp gate (tools/recal_regen_sheets.py:257-294) skips root1 because it is inside the regen's own resume record, and the resume branch (tools/recal_regen_sheets.py:347-354, 394-404) re-sent the stored day-2 SUPERSEDES CSV — the same root1 hours — captioned 'Old sheet is VOID for payment; pay per this sheet.'

**Fix applied** (see the commit for the final shape): In send_daily_report_if_due, before the fresh path (and ideally before the resume scan), refuse loudly while any reports/<day>/.regen-v2-counted.json exists without .regen-v2-done — one call to the F10 logic (factor the regen half of pending_daily_send out, or call pending_daily_send and test whether the pending day is a regen day). Same doctrine as the reset tools: the regen resumes verbatim, so nothing else may count or stamp until it settles.

### #12 [MAJOR] /Users/adnaan/Documents/hl-projects/hl-gamedata/tools/recal_rebuild_reset.py:38 (ops-tools, refuters 0/2) — FIXED cd8546a (with #3)

**recal_rebuild_reset tears the ledger down without holding the run lock — the TOCTOU r-loop 1 fixed in every sibling tool**

The tool guards with a bare `if cfg.lock_dir.exists(): return 2` (line 38) and then runs a multi-minute unlocked teardown (lines 96-149: dossier archiving for every sid, UPDATE loop nulling uploaded_reported_at/accepted_reported_at and resetting roots to DISCOVERED, `DELETE FROM sessions WHERE parent_id IS NOT NULL` at :128, work-dir rmtrees and _locked_report_remove at :131-140). All three sibling flip tools acquire and HOLD the lock for their whole duration precisely because 'a bare existence check left a TOCTOU where systemd Restart=always could start the driver mid-tool' (recal_refix_reset.py:108-131, recal_regen_sheets.py:196-210, recal_verify_tree.py:40-55; PIPELINE_CONTINUOUS_DESIGN.md:384-388 lists exactly those three). The daily-report CLI's own lock comment states the invariant this tool breaks: 'the tools hold the lock and check pending_daily_send only at entry' (run.py:2249-2253) — rebuild_reset checks pending_daily_send at entry (:47-59, the F10 interlock) but holds nothing, so the interlock it just passed can be invalidated mid-teardown by any process that acquires the free lock. The tool is live, maintained surface: D7 added its pending interlock and paid-pieces NOTE (commit a8f6267), F10's commit message calls it one of 'both reset tools', and rebuild-sessions-2026-08-18.csv was captured 'for possible rerun'.

**Fix applied** (see the commit for the final shape): Give it the identical shape as recal_refix_reset.main(): `from pipeline.run import acquire_lock, release_lock; if not acquire_lock(cfg): abort; try: _locked_main(...) finally: release_lock(cfg)`. That also fixes the stale-lock false refusal for free (acquire_lock pid-reclaims).

### #13 [MINOR] /Users/adnaan/Documents/hl-projects/hl-gamedata/pipeline/reports.py:426 (ops-tools, refuters 1/2) — FIXED b1bea89 (with #11)

**pending_daily_send fails OPEN on OSError — the money interlock silently reports 'nothing pending' when it could not look**

pending_daily_send wraps its reports-dir listing in `try: day_dirs = sorted(...iterdir()...) except OSError: return None` (reports.py:423-427). None is the same answer as 'no pending send', so a transient EMFILE/EIO/permission failure at exactly reset-tool entry disables the interlock silently and both recal reset tools proceed to teardown (recal_refix_reset.py:141-153, recal_rebuild_reset.py:47-59 both treat None as clear). This inverts the codebase's own F5 doctrine for the same class of error one layer down: the daily resume distinguishes transient OSError (print + retry, never conclude) from parse errors (run.py:1029-1056, r-loop 11 #10), and _resume_daily_send explicitly refuses to draw conclusions from a failed stat. A guard whose breach re-opens the stale-sheet/double-count class F10 exists to prevent should fail CLOSED or at least loudly.

**Fix applied** (see the commit for the final shape): In pending_daily_send, on OSError from iterdir return a sentinel the callers refuse on (or raise and have both reset tools print 'could not verify pending sends — refusing' and exit 2). 'Could not check' must never read as 'checked clean' in a teardown gate — same three-valued lesson vm_setup.sh's lock_free probe already encodes (tools/vm_setup.sh:132-135).

### #14 [MAJOR] pipeline/run.py:162 (tests-coverage, refuters 1/2) — FIXED 0ad8747 (with #15)

**F6 (r11 #6) producer half is unpinned — deleting the probed_duration_s wiring keeps the gate green while NULL-duration roots silently lose their uploaded hours**

F6's fix has two halves: (a) the validate probe rides into the worker result so both drivers backfill a NULL duration_raw_s, restoring countability; (b) the sheet's third re-entry arm. Half (a)'s PRODUCER is untested. The only path that carries the real probe into the worker dict is run.py:162-163 (`"probed_duration_s": (res.metrics or {}).get(...)`) fed by validate.py:720 (`_metrics`) <- validate.py:870 (`aux["probed_duration_s"]`). Both driver consumers read only that dict key: run.py:341 `pd = r.get("probed_duration_s")` and continuous.py:837 `pd = res.get("probed_duration_s")`. PROVED: (1) deleting run.py:162-163 in a scratch clone -> full pinned gate `SUITE_FLOOR=670 bash tools/run_suite.sh --with numpy==2.4.6 --with opencv-python-headless==5.0.0.93 --with rerun-sdk==0.36.0` = `674 passed ... ARMING GATE OK`; (2) independently deleting validate.py:720 -> `674 passed`. Both survive because ALL three backfill tests (test_r_loop11.py:407-460) fake `_WORKER_FN`/`_validate_worker` to hand back a dict that already contains `probed_duration_s`, and grep confirms the string appears in tests only inside those three fabricated dicts (lines 423/443/458); the r10 #12 'end-to-end' test checks only CNT_SHORT via aux, never the metrics->worker->backfill chain. A probe running the REAL `_validate_worker` (validate_session stubbed to return metrics={probed_duration_s:3600.0}) returns 3600.0 at HEAD and None after the deletion — the line is load-bearing, not dead.

**Fix applied** (see the commit for the final shape): Add a test that exercises the REAL producer end to end: run `_validate_worker` (batch) and `_validate_one` via the real `_WORKER_FN` (continuous) against a session with a probe-able video and a NULL ledger duration_raw_s, and assert the ledger row's duration_raw_s becomes the probed value — WITHOUT stubbing the worker's probed_duration_s. Alternatively assert `_metrics` populates `probed_duration_s` and that `_validate_worker` forwards `res.metrics['probed_duration_s']`, so deleting either link fails.

### #15 [MINOR] translator/translate.py:258 (tests-coverage, refuters 1/2) — FIXED 0ad8747 (with #14)

**F9 (r11 #12) v1-join half is unpinned — the only v1-side test calls the helper, not translate_bundle, so reverting the call-site sanitization survives with the gate green (path traversal returns on the v1 CLI)**

F9 claims the traversal guard is 'applied at BOTH joins'. The v2 join (v2.py) is pinned by test_traversal_session_id_stays_inside_out, which calls `translate_bundle_v2`. The v1 join is NOT: translate.py:258 `session_id = safe_session_id(meta.get("session_id"), bundle_dir)` plus the containment assert at translate.py:265 are only 'covered' by test_v1_translate_join_is_sanitized (test_r_loop11_translator.py:53), which calls the `safe_session_id` HELPER directly and never invokes `translate_bundle`. PROVED: reverting the call site to the pre-fix `session_id = meta.get("session_id") or bundle_dir.name` and deleting the assert in a scratch clone -> full pinned gate = `674 passed ... ARMING GATE OK`. A path-math probe confirms the reverted v1 join with session_id='../../../../../../tmp/ESCAPED_dir' resolves out_dir to `/Users/tmp/ESCAPED_dir` (is_relative_to(out_root) == False), while the HEAD helper falls back to the bundle folder name and stays inside out_root.

**Fix applied** (see the commit for the final shape): Add a v1-side test that calls `translator.translate.translate_bundle` (do_trim=False, make_rrd=False) with a metadata.json session_id of '../../..' and asserts the returned out_dir is inside out_root and no file was created outside it — mirroring the v2 traversal test — so a regression at translate.py:258/265 fails.
