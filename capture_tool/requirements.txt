PySide6>=6.5
pynput>=1.7
psutil>=5.9
# Required for the finalize step's session.rrd generation
# (app/core/finalize/pipeline.py -> translator.rrd, in_process=True).
# Missing entirely before this was noticed on a real delivery: rrd_creation.py
# got written but session.rrd never did, because generation also used to
# shell out via sys.executable, which doesn't work in a frozen exe either
# (see pipeline.py's comment) — both problems had to be fixed together.
rerun-sdk
# Optional but strongly recommended — used by the in-tool sync self-test
# (app/core/finalize/pipeline.py -> translator.sync) and by check_session_v2's
# QA gate. Without them, sync verification is skipped with a warning.
numpy
opencv-python-headless
# NOTE: the `translator` package (repo root's translator/) is a required
# import (app.core.finalize.pipeline) but is not pip-installable — there's
# no pyproject.toml/setup.py for it. It's resolved via PYTHONPATH at both
# dev-run and PyInstaller-build time. See "Packaging" in README.md.
