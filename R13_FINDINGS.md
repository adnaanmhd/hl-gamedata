# R13_FINDINGS.md — review iteration 13, findings of record

**Run 2026-08-19** by the F/r12 executor session via the workflow
snapshot `tools/review/flip-review-iter13.js` (confirmation pass,
RE-RULED by Adnaan 2026-08-19 to run regardless of iteration 12's
verdict). 7 finder lanes, 2-vote refute, 31 agents (31 completed, 0
errors; one lane returned an empty list), HEAD a4e85d1 (code HEAD
0ad8747 + floor 6b87023).

**Verdict: NOT QUIET — 12 raised → 12 confirmed (0 blockers), 0
killed.** Per the standing ruling (plan §11, kickoff): iteration 13 not
quiet ⇒ **STOP — the verified-but-unfixed list goes to Adnaan,
severity-ordered; nothing proceeds** (no fixes, no e2e, no flip) until
he rules.

**Known duplicates/clusters:** #1 ≡ #3 (the r12 ''-arm stamps over a
REAL zip-class supersede; #2 is the sibling arm-2 hole where the F6
backfill erases the NULL-duration discriminator — all three are
regressions from r12 commit 986368f, found independently by two lanes).
#4 is the F4-doctrine's third instance (FIX_RETRANSLATE's
game_override). #7/#10 both concern tools/fix_actions_from_v2.py.
#8/#9/#11 all concern tools/recal_rebuild_reset.py.

Machine-readable results incl. both refuter verdicts: session
scratchpad `r13-results.json`.

## Confirmed findings (UNFIXED — awaiting Adnaan's ruling)

### #1 [MAJOR] pipeline/reports.py:356 (regressions-r12, refuters 0/2)

**_stamp's CAS-miss '' arm re-seals a slot a REAL zip-class supersede just cleared — corrected re-upload's hours silently stranded**

986368f added arm 4 to _stamp (pipeline/reports.py:355-361): on a CAS miss, `if row is not None and not row["md5_video"]: ledger.update(sid, **{column: now})` — the comment says '' can only mean the stamp-preserving zip heal, adjudicated later by the download-time prev_md5 deferral. But ingest.py:458 performs a REAL supersede with `ledger.supersede(ds.session_id, new_md5=vmd5 or "", ...)` for every zip-origin REJECTED/QUARANTINED slot whose corrected re-upload is signalled by changed bytes/newer ctime (review-r2 #9 flow), and ledger.supersede (ledger.py:274-309) deliberately clears duration_raw_s, uploaded_reported_at, accepted_reported_at and writes md5_video='' with event detail 'superseded: new md5 ' — NO prev_md5 note (only the quarantine heal writes one, ingest.py:295-297), so the download backfill (ingest.py:867-878) finds no prev event and never applies the deferred clear. The resume pre-filter also passes it (run.py:1102: `bool(cur)` is False for ''). Probe at HEAD (scratchpad/probe_stamp_arm4.py): seeded counted root md5 '00c5', `ledger.supersede(sid, new_md5="")` fired mid-send → output: 'after send: md5= "" duration_raw_s= None uploaded_reported_at= 2026-08-18T22:46:05+00:00 ... loud skip printed: False ... BUG CONFIRMED: True; next day's sheet contains the corrected upload's row: False'. Same probe against the pre-fix tree at 11af5a0: 'uploaded_reported_at= None, loud skip printed: True, next day's sheet contains the corrected upload's row: True' with the LATE ARR…

**Finder's proposed fix (NOT vetted by an executor — vet before adopting):** On a CAS miss with a falsy row md5, distinguish heal from supersede before stamping: read the sid's latest ''-writing event — the quarantine heal's detail carries 'prev_md5=' (deferral will adjudicate → stamp is safe), while 'superseded: new md5 ' means a clearing tool DID run → keep the loud SKIP (return False, new hours stay countable). Alternatively have the zip-class supersede write its own prev_md5-style marker so the download-time deferral covers it too.

### #2 [MAJOR] pipeline/reports.py:344 (regressions-r12, refuters 0/2)

**_stamp's recorded-'' NULL-duration discriminator is erased by the F6 validate-time backfill — adjudicated-NEW-bytes roots stamp silently on resume**

986368f's arm 2 (pipeline/reports.py:337-352) lets a sid the sheet recorded as '' skip only when `row["md5_video"] and row["duration_raw_s"] is None` — the transient signature of the download-time deferred clear (ingest.py:874-877 sets duration_raw_s=None then md5_video=real). But the r11-F6 backfill in BOTH drivers (continuous.py:837-839, run.py:341-343: `if pd and led.get(sid)["duration_raw_s"] is None: led.update(sid, duration_raw_s=float(pd))`) refills the NULL duration the moment the new bytes validate, destroying the only evidence the commit's own contract depends on ('stamps UNLESS the download-time deferral has since adjudicated NEW bytes'). The resume pre-filter passes the sid through (run.py:1102: `bool(rec_md5)` False for ''). Probe at HEAD (scratchpad/probe_stamp_arm2.py): heal writes '' pre-count (record md5 map {sid: ''}), crash before stamps, then in the gap the deferred clear (new bytes) + F6 backfill (duration 7200) run; resume output: 'uploaded_reported_at: 2026-08-18T22:42:53+00:00, accepted_reported_at: ...' with EMPTY resume stderr → 'BUG CONFIRMED (silent stamp on adjudicated-NEW bytes): True; next day's sheet contains the root's player row: False'. Same probe at 11af5a0: both marks stay None and the loud 'was re-uploaded with new bytes after the record ... NOT re-stamping ... the new hours stay countable' line prints for both marks — the pre-fix rule protected this path.

**Finder's proposed fix (NOT vetted by an executor — vet before adopting):** Make the deferral's adjudication durable instead of inferring it from the transient NULL duration: at ingest.py:873-877, when the deferred clear fires, write an event (e.g. 'zip-backfill: bytes CHANGED, prev_md5=<old>') and have _stamp's arm 2 (and the resume pre-filter) skip loudly for a recorded-'' sid whenever such an adjudication event postdates the count — regardless of whether duration_raw_s has since been backfilled.

### #3 [MAJOR] pipeline/reports.py:360 (payment-split, refuters 0/2)

**r12 #1/#2 regression: zip-branch supersede inside the stamp window is re-stamped as if it were the stamp-preserving heal — corrected re-upload's hours stranded off every sheet, silently**

_stamp's r12 CAS-miss arm (reports.py:356-361) stamps unconditionally whenever the row now holds md5_video='' ("a zip-class heal rewrote the md5... not byte-change evidence"). But the heal is not the only writer of '': the zip re-upload supersede at ingest.py:458 calls ledger.supersede(sid, new_md5=vmd5 or "") for a REJECTED/QUARANTINED zip slot with changed bytes/ctime, which sets md5_video='', duration_raw_s=NULL and clears BOTH payment stamps (ledger.py:284-291) — a deliberate reset, not a preserve. The arm cannot tell them apart, and the safety net the comment relies on never fires: the download-time deferral (ingest.py:867-877) keys on an events row LIKE '%prev_md5=%', which only the heal writes (ingest.py:296-297); supersede's event detail is 'superseded: new md5 ' with no breadcrumb (ledger.py:302-305). Proven by RUNNING the real code against a scratch ledger (probe1_zip_supersede_stamp.py in the session scratchpad): a zip-origin REJECTED root (md5 backfilled to H1 at download) is counted on sheet 1 (snapshot H1); ledger.supersede(new_md5="") runs mid-window exactly as ingest.py:458 does; mark_uploads_reported/mark_accepted_reported then print NOTHING and return n_up=1, n_acc=1 — 'after stamps: up_rep=2026-08-18T22:39:31+00:00 acc_rep=2026-08-18T22:39:31+00:00' on the freshly-reset slot. The deferral query returns None ('all events: [..., "superseded: new md5 "]'), so after download (md5→H2) and delivery (1850s) the next sheet is EMPTY: 'SHEET2 rows: [] ... VERDICT: NO…

**Finder's proposed fix (NOT vetted by an executor — vet before adopting):** Give the zip-branch supersede the same breadcrumb the heal has, so the existing download-time deferral adjudicates it: in ledger.supersede, when new_md5 is falsy and the old md5_video is real, append '; prev_md5={old_md5}' to the DISCOVERED event detail (ledger.py:302-305). Then a falsely-landed stamp self-heals at download time — changed bytes: the deferral (ingest.py:867-877) clears stamps + duration and the hours re-enter via the late/uncountable guards; identical bytes (ctime-only re-zip): the stamp correctly stands. Do NOT instead make _stamp skip whenever the row holds '' — that re-opens the r12 #1/#2 heal double-pay this commit fixed. Add the missing test: the test_real_vs_real_mismatch_still_skips shape with ledger.supersede(new_md5='') mid-send, asserting the corrected re-upload's…

### #4 [MAJOR] pipeline/fix.py:484 (fix-validate, refuters 0/2)

**FIX_RETRANSLATE always overrides the session's own keybind.json with the built-in — the F4 doctrine's third instance, in the 'universal strong fix' itself**

_dispatch (fix.py:483-484) calls retranslate_from_sidecars(work, game_override=game if game in C.GAMES else None). `game` is the ledger slug passed by both drivers (run.py:622, continuous.py:945), and ingest scoping guarantees it is ALWAYS in C.GAMES=('kamla','outer_wilds') — ingest.parse_listing (ingest.py:126) drops any path whose parts[0] is not in C.GAMES, and split children inherit the parent's game. So game_override is non-None on every production FIX_RETRANSLATE, and retranslate_from_sidecars' override branch (fix.py:913-918) takes `keybind = dict(KEYBINDS.get(slug, {}))` unconditionally — the `else` branch that resolves the session's own raw/keybind.json via resolve_keybind is DEAD CODE in production. The docstring (fix.py:861-863) says game_override is 'the REROUTED game' (review-2 #5 intent: override on reroute only), and keybinds.py's header states the per-session keybind.json 'is authoritative'. Every existing test (test_r_loop7/8/9/10/11.py) calls retranslate_from_sidecars WITHOUT the override, i.e. tests the branch production never runs. Probe (scratchpad/probe_override/probe.py, real ffmpeg video, Kamla bundle with custom keybind {'interact':'q',...}, presses of q and w): PRODUCTION path (override='kamla') → note 're-translated from sidecars (90 frames; ; stripped 6 unbound key presses)', distinct keys ['W'], distinct actions ['move_up'], key frames 3. COUNTERFACTUAL (session keybind honored) → 'stripped 0 unbound key presses', keys ['Q','W'], actions ['interac…

**Finder's proposed fix (NOT vetted by an executor — vet before adopting):** Thread the reroute fact from the plan into the dispatch: apply_fixes knows whether plan['steps'] contains FIX_REROUTE_GAME — pass game_override=game only in that case (preserving review-2 #5's reroute ruling), and None otherwise so retranslate_from_sidecars resolves raw/keybind.json exactly as fix_key_hygiene (F4) and fix_actions_context (r12 #5) now do. Alternatively make the non-reroute branch resolve the session keybind while keeping slug=game for the patches/fallback. Add the fail-first test with a custom-bound bundle through the production dispatch path.

### #5 [MAJOR] pipeline/validate.py:506 (fix-validate, refuters 0/2)

**_map_flags classifies notification/chat edge-vs-mid against session.json's CLAIMED duration — a wrong claim turns a fixable tail-edge flag into an unfixable terminal reject**

validate.py:506 (`if n["t"] <= 3.0 or n["t"] >= dur - 3.0:`) and :531 (`edge = c["t"] <= 3.0 or c["t"] >= dur - 3.0`) test edge-ness against `dur = float(rep.get("duration_s") or 0)` — the player-supplied session.json claim (analyze_sample.py:1343) — while three lines earlier the same function's r12 #7 arms judge shortness on `dur_true` (the probed duration, D2 truth source). The r12 #9 commit documents the claim's observed corruption class ('finite ms-in-seconds corruptions'), and the sweep is clamped to min(claim, probed), so notif/chat timestamps always lie within the REAL timeline while the edge test compares them to the corrupt claim. Probe (scratchpad/probe_override/probe_flags.py) with claim=300000 (ms corruption), probed=300, confirmed notif+chat at t=298 (2s before the real end): emitted CNT_NOTIF_MID blocking=True fixable=False and CNT_CHAT_PII blocking=True fixable=False; control with claim=300 emitted CNT_NOTIF_EDGE fixable=True and CNT_CHAT_PII fixable=True. map_reasons bins any unfixable blocking reason as 3, and both drivers set bin-3 straight to REJECTED (run.py:353-356) — the fixable STR_SJ_INVALID 'video duration ... vs duration_seconds' companion cannot rescue it because no fix pass ever runs. The accepted rulings cover only _map_windows geometry (entry 26) and the new CNT_SHORT arms themselves (entry 50); neither blesses the claim-based edge test, and r12 #7's half-migration (dur_true for shortness, dur for edge-ness inside the same if-statement) shows it …

**Finder's proposed fix (NOT vetted by an executor — vet before adopting):** Classify edge-vs-mid against the probed duration in _map_flags: use dur_true (already computed at validate.py:486) in both tests — `n["t"] >= dur_true - 3.0` at line 506 and `c["t"] >= dur_true - 3.0` at line 531 — keeping `dur` only as the no-probe fallback that dur_true already encodes. This completes the r12 #7 migration and matches the D2 doctrine (probed duration is the truth source for every length-derived decision). Pin with a test: ms-corrupted claim + confirmed tail notification must yield CNT_NOTIF_EDGE, not CNT_NOTIF_MID.

### #6 [MAJOR] translator/v2.py:576 (translator, refuters 0/2)

**r12 #10 OverflowError arm is incomplete: _check_session_json arithmetic + frame-spacing median still crash check_session_v2 on a bigint session field -> wrongful terminal QUARANTINE**

r-loop-12 #10 (commit 75db7fd) added an OverflowError arm to ONLY two consumers of a bigint numeric cell: the frame-sync max() (v2.py:851) and inventory()'s cast. Three other sites in the SAME checker consume the same input class and still raise OverflowError uncaught. (1) _check_session_json's numeric try-block is `except (TypeError, ValueError)` (v2.py:585) — it omits OverflowError. A session.json `duration_ms` that is a large JSON integer makes `s["duration_ms"] / 1000.0` raise at v2.py:576; a large `fps` or `frame_count` makes `s["fps"] * s["duration_seconds"]` raise at v2.py:582. json.loads parses an unbounded-length integer literal to a Python int; int/float or int*float on it raises `OverflowError: int too large to convert to float`, which the tuple does not catch. (2) The frame-spacing check computes `0.2 * med` at v2.py:737 with no guard; when the timestamp_ms column is all bigints the median itself is a bigint and this raises. Real probe output: `_check_session_json` duration_ms=10**400 -> CRASH OverflowError at v2.py:576; fps=10**400 -> CRASH at v2.py:582; frame_count=10**400 -> CRASH at v2.py:582; an all-bigint timestamp column -> CRASH at v2.py:737. The bigint never reaches the guarded v2.py:851, so the r12 #10 fix is shadowed for these triggers. (v2.py:668 normalizes non-(int/float) fields to 0, but a bigint IS an int so it is not normalized, and _check_session_json at v2.py:664 runs BEFORE that normalization anyway.)

**Finder's proposed fix (NOT vetted by an executor — vet before adopting):** Add OverflowError to the numeric except tuple at v2.py:585 (`except (TypeError, ValueError, OverflowError):`) and guard the frame-spacing block at v2.py:734-741 the same way (or normalize the parsed `ts` bigints before the median), mirroring the arms r12 #10 already added at v2.py:851 and in inventory().

### #7 [MINOR] tools/fix_actions_from_v2.py:54 (translator, refuters 1/2)

**F9 traversal sibling missed: fix_actions_from_v2 joins an unsanitized session.json session_id into the output path, writing delivery files outside out_root**

r-loop-11 F9 (accepted item 42) routed every session_id through translate.safe_session_id at the v1 and v2 translate joins and added a resolve() containment assert. tools/fix_actions_from_v2.py — the operator twin of the pipeline's context fix (accepted item 48 names it 'the operator twin tools/fix_actions_from_v2.py') — was not given the same treatment: fix_session reads `sid = s["session_id"]` (line 53) from the delivered session.json and joins it verbatim at line 54 `out_dir = out_root / VENDOR / date / slug / sid`, then `out_dir.mkdir(parents=True, exist_ok=True)` at line 55, with no safe_session_id call and no containment assert. Real probe: with sid='../../../../ESCAPED', out_dir.resolve() = /private/tmp/claude-501/ESCAPED, is_relative_to(out_root) = False — the four delivery files are written outside out_root.

**Finder's proposed fix (NOT vetted by an executor — vet before adopting):** Route sid through translate.safe_session_id(s.get('session_id'), session_dir) before the line-54 join and add the same `assert out_dir.resolve().is_relative_to(out_root.resolve())` containment guard translate.py/v2.py use.

### #8 [MAJOR] tools/recal_rebuild_reset.py:137 (ops-tools, refuters 0/2)

**rebuild-reset silently nulls payment stamps and deletes paid DELIVERED pieces — a rerun after the endgame double-pays the cohort**

recal_rebuild_reset._locked_main resets every non-QUARANTINED/DUPLICATE root with `uploaded_reported_at=NULL, accepted_reported_at=NULL` (lines 135-139) and then `DELETE FROM sessions WHERE parent_id IS NOT NULL` (line 145), with NO stamped-root refusal, no paid-piece recording (ledger.record_paid_piece's only caller is recal_refix_reset.py:367 — verified by grep), and no mention of stamps in the dry-run plan. Proved by running the tool in a scratch sandbox: a DELIVERED root stamped uploaded+accepted came out DISCOVERED with BOTH stamps NULL, an accepted-stamped DELIVERED child was deleted, paid_pieces stayed 0 rows, and neither the dry-run JSON ({before, roots_to_reset, children_to_delete}, line 107) nor the --yes output printed one word about the destroyed stamps. Once dailies resume (FLIP_RUNBOOK 7.3), build_sheet_rows re-counts every un-stamped root: in_window and the late-arrival guard both key on `not root["uploaded_reported_at"]` (pipeline/reports.py:686 and 695-697). The tool's own sibling treats exactly this as a refuse-by-default condition: recal_refix_reset.py:242-255 aborts rc=2 on any uploaded-stamped root without --allow-reported and deliberately PRESERVES the stamp (its comment at lines 335-347: "un-stamping would pay the same footage on two sheets"); vm_setup.sh:186-193 calls a daily send over an unstamped cohort "the one precondition whose breach is not automatically recoverable". The kickoff pairing that made the nulling money-safe in the 08-16 one-shot (REV…

**Finder's proposed fix (NOT vetted by an executor — vet before adopting):** Mirror recal_refix_reset's r-loop-4 doctrine: before acting, count in-scope roots carrying uploaded_reported_at and DELIVERED descendants carrying accepted_reported_at; if any exist, ABORT rc=2 naming them, behind an explicit override flag. Under the override, record paid pieces for accepted-counted DELIVERED nodes before the child DELETE (reuse refix's block at recal_refix_reset.py:212-233/366-372) and print the stamped counts in both the dry-run plan and the --yes summary.

### #9 [MINOR] tools/recal_rebuild_reset.py:67 (ops-tools, refuters 0/2)

**Reset tools' pending-interlock ABORT misdiagnoses regen-pending and wedged days, prescribing a remedy the driver can never perform**

r-loop 11 F10 extended pending_daily_send to also return a day whose .regen-v2-counted.json lacks .regen-v2-done (pipeline/reports.py:477-479), and it has always returned wedged days (record present, .sent absent — the .wedged file is not consulted, reports.py:467-476), but both reset tools print a hardcoded diagnosis written for the original daily case only: why="reports/<day>/.daily-counted.json exists without its settled .sent+doc_sent markers" and how="let the driver finish the resume (or reconcile the day by hand), then re-run" (recal_rebuild_reset.py:66-75, recal_refix_reset.py:143-153). Proved by execution: with ONLY reports/2026-08-15/.regen-v2-counted.json present (no .daily-counted.json anywhere), the tool printed exactly that daily-record text — naming a file that does not exist and a resume the driver will never run. The driver cannot settle either state: a pending regen makes send_daily_report_if_due refuse outright (pipeline/run.py:1175-1181 — only `recal_regen_sheets --send` can settle it), and a wedged day is skipped by the scan by design (run.py:1200-1208). At the flip, when these tools actually run (FLIP_RUNBOOK 6d), CONT_DAILY_REPORTS=False additionally makes the driver print '[daily] suppressed' and do nothing at all (run.py:1160-1163).

**Finder's proposed fix (NOT vetted by an executor — vet before adopting):** Make pending_daily_send return (day, kind) — or have the callers re-test which record kind is pending — and print a kind-specific why/how: daily-pending -> 'let the driver finish the resume'; regen-pending -> 'finish recal_regen_sheets --send (the driver refuses dailies while this record exists)'; wedged -> 'reconcile by hand, then rm reports/<day>/.wedged'; UNKNOWN sentinel -> 'reports dir unreadable — fix and re-run'. Same text in both tools.

### #10 [MAJOR] tools/fix_actions_from_v2.py:87 (tests-coverage, refuters 0/2)

**Operator-twin half of r12 #5/#8 has no test — full revert survives the arming gate at 696/696**

The r12 #5/#8 fix (02bdeac) changed two sites: pipeline/fix.py fix_actions_context (pinned by test_actions_context_honors_the_sessions_own_keybind + control) and tools/fix_actions_from_v2.py:87-94 (kbp = session_dir/'raw'/'keybind.json'; resolve_keybind(...)). Grep over pipeline/tests, translator/tests and tools/review finds ZERO references to fix_actions_from_v2 anywhere. Mutation proof in a scratch copy outside the repo: `git show 02bdeac -- tools/fix_actions_from_v2.py | patch -R -p1`, then `PYTHONPATH=. uv run --with pytest --with numpy==2.4.6 --with opencv-python-headless==5.0.0.93 --with rerun-sdk==0.36.0 pytest pipeline/tests translator/tests -q` → "696 passed in 124.59s" — identical to the un-mutated baseline (696 passed), floor 692, rc 0. The arming gate cannot see this fix being deleted.

**Finder's proposed fix (NOT vetted by an executor — vet before adopting):** Add a test that loads the tool via the existing _load pattern (pipeline/tests/test_payment_split_r6._load) or importlib, builds a v2 session dir with raw/keybind.json binding a custom literal (e.g. {"general_flashlight": "g"}), stubs translator.context.classify_video/available exactly as _context_work does, drives fix_session, and asserts the custom bind's action survives in the output frames.csv; add the built-in-fallback control (no sidecar).

### #11 [MAJOR] tools/recal_rebuild_reset.py:47 (tests-coverage, refuters 0/2)

**r12 #3/#12 lock fix: refuse-when-live and stale-reclaim both unpinned — a live-lock STEAL mutant passes 696/696**

test_r_loop12.py::test_rebuild_reset_holds_the_run_lock pins only (a) lock dir exists while pending_daily_send runs inside _locked_main and (b) lock released after. It never exercises acquire_lock returning False. Mutation proof in a scratch copy: replaced lines 47-50 (`if not acquire_lock(cfg): print("ABORT: run lock held..."); return 2`) with `if not acquire_lock(cfg): shutil.rmtree(cfg.lock_dir, ignore_errors=True); acquire_lock(cfg)` — i.e. the tool STEALS a live driver's lock and proceeds with the teardown. Full suite: "696 passed in 124.83s", floor 692, rc 0. The #12 half (stale-lock reclaim closing the false-refusal case) is equally untested: no test presents a stale or live lock dir to this tool (verified by the suite-wide green run; the r6/r8/r9/r11 rebuild-reset tests all run with no lock present).

**Finder's proposed fix (NOT vetted by an executor — vet before adopting):** Two tests beside test_rebuild_reset_holds_the_run_lock: (1) create cfg.lock_dir with a pid file whose pid reads as live (monkeypatch pipeline.run._pid_is_pipeline to True), assert reset.main() == 2, ledger untouched, and the lock dir still present; (2) create a stale lock (pid file for a dead/non-pipeline pid, old mtime), assert the tool reclaims it, proceeds (rc 0), and releases — pinning the #12 false-refusal fix.

### #12 [MINOR] pipeline/validate.py:497 (tests-coverage, refuters 0/2)

**r12 #7's probed-duration clause is vacuously tested — both tests set probed == claimed**

The r12 #7 tests (test_edge_notification_on_a_barely_long_clip_is_short_at_map_time, test_edge_chat_tail_...) pass {"duration_s": 71.0} with {"probed_duration_s": 71.0}, so the fix's core clause — the edge remainder is 'judged on the probed duration' (accepted-behaviours entry 50, D2 doctrine) — is never distinguished from the claimed duration. Mutation proof in a scratch copy: changed line 497 to `remain = (dur - (t + 1.0)) if edge == "head" else (t - 1.0)` (claimed instead of dur_true; the tail arm uses no duration at all) → full suite "696 passed in 124.56s". A broad mutation of the dur_true definition at line 486 is killed (r9 CNT_SHORT tests) — the surviving gap is specifically the head-edge formula, i.e. the entire notif/chat head-cut arm the commit added.

**Finder's proposed fix (NOT vetted by an executor — vet before adopting):** In one of the two edge tests, split the values: {"duration_s": 500.0} with {"probed_duration_s": 71.0} must still yield CNT_SHORT with post_cut_s == 67.5; add the reverse control ({"duration_s": 71.0, "probed_duration_s": 200.0} → fixable CNT_NOTIF_EDGE).
