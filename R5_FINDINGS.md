# r-loop 5 — 26 confirmed findings (full detail)

Source: workflow wf_440a01a1-41b. 4 verify agents died on API errors; the
findings marked INCOMPLETE-VERIFY survived on ONE vote, so the 2-vote refute
discipline was not applied to them — re-verify before fixing or discarding.

######## 1 [blocker] pipeline/continuous.py:347 (lane new-driver) votes=2
TITLE: Media cap and every reclaim sweep are blind to DISCOVERED work dirs
CLAIM: `_local_count` sums only `LOCAL_STATES` (continuous.py:46-54), which deliberately excludes DISCOVERED, and `runmod._sweep_terminal_work` (run.py:1441-1473) has branches only for QUARANTINED/DELIVERED/SPLIT/DUPLICATE. But `_download_one` routes every transient and every `zip_incomplete` failure back to DISCOVERED (continuous.py:420-422, 430-432, 436-439) while `ingest.download` leaves everything rclone already transferred in `work/<sid>` (ingest.py:746-758 creates the dir and never cleans it on the rc!=0 path; `_unzip_payload` unlinks the zip parts only on success, ingest.py:713-715). So a session that keeps failing to download holds multi-GB of local media that the ~40-session cap does not count and that no sweep ever reclaims. The stuck list cannot surface it either: `_stuck_lines` excludes DISCOVERED by design (continuous.py:1029). This is the exact class ruled a blocker in r-loop 3 for QUARANTINED, left unfixed for DISCOVERED.
SCENARIO: A player uploads a multi-part zip and abandons it (parts 002/003 never arrive). D downloads part 001 (~3 GB) into work/<sid>; `_unzip_payload` raises BadZipFile -> DownloadError(kind='zip_incomplete') -> row set back to DISCOVERED + 5-min cooldown, forever (F8 only records it in the `incomplete` table, and the folder-issues report is suppressed at the flip because CONT_DAILY_REPORTS ships False). The 3 GB stays on disk. Verified by probe (/private/tmp/.../scratchpad/probe1.py): a DISCOVERED row with a populated work dir gives `_local_count()==0`, and `_sweep_terminal_work` + `_finalize_orphan_rejects` both leave the dir in place. On the flip VM (pd-balanced 250 GB boot disk, DISK_LOW_WATER_GB=100 -> ~130 GB of usable headroom) ~40 such abandoned/failing uploads consume the entire headroom invisibly; `_pick_download` then returns None on the F7 disk check on every poll while `_local_count` still reports far below 40, so the cap-pressure alert never fires and the operator is told nothing about where the disk went.
FIX: Either (a) wipe `cfg.work/<sid>` (and `-analysis`) whenever `_download_one` returns a row to DISCOVERED — rclone re-downloads idempotently anyway, which is the stated reason DOWNLOADING resume is safe — or (b) count DISCOVERED rows whose work dir exists in `_local_count` (same dir-existence test r-loop 4 adopted for QUARANTINED) and add a DISCOVERED-with-media reclaim arm to `_sweep_terminal_work` keyed on age, plus stop excluding such rows from `_stuck_lines`.


######## 2 [blocker] tools/retrim_v2_session.py:73 (lane core-flow) votes=2
TITLE: FIX_RETRIM_HEAD anchors created_at on the ms-rounded CSV timestamp — r-loop-4 blocker #3 fixed in cutter.py only
CLAIM: r-loop 4 blocker #3 fixed the split-child created_at anchor in pipeline/cutter.py:192 (`seg_created = created + timedelta(microseconds=src_pts[i0])`) because a millisecond-rounded anchor shifts qa-v2's raw re-bin by up to +-500us and flips every event near a frame boundary. The IDENTICAL math is still live in the retrim tool that pipeline/fix.py:373-375 dispatches for FIX_RETRIM_HEAD: `base_ms = int(kept[0][1])` (line 66) then `created += timedelta(milliseconds=base_ms)` (line 73). The true head offset is the exact source PTS of the first retained frame; the stamped one is that value rounded to whole ms. translator/v2.py:846 recovers `head_us = (created - started)*1e6` from exactly this field and re-bins every raw mouse event against it, so the rounding error is a systematic band, not isolated jitter. Verified by repro (scratchpad, outside the repo): 30 fps clip, head_s=7.0 snapped to keyframe frame 220, src_pts=7333333us vs base_ms=7333 -> 333us error -> `FAIL: raw recomputation: dx/dy differs from CSV in 653 frames (max run 2, 66.63% of rows)`; substituting the cutter's exact-PTS anchor on the same bytes yields `WARN: isolated dx/dy attribution flips: 1 frame(s) ... within tolerance`. Error is zero only when the retained frame's PTS is a whole millisecond (1 frame in 3 on a nominal 30 fps grid, rarer on the real dropped-frame grid).
SCENARIO: Kamla session with raw sidecars, reasons = CNT_EDGE_NONGAMEPLAY(head, 7.0s) + INP_FROZEN_ACTIONS(300-303s). Attempt 1 plan = [FIX_GATE_WINDOW, FIX_RETRIM_HEAD, FIX_SESSIONJSON_RECOMPUTE] (the r-loop-4 ordering). retrim stamps created_at 333us early. Revalidation: validate.py:721 registers raw/ -> check_session_v2(raw_bundle=raw) -> _verify_against_raw FAILs 'raw recomputation ...' -> validate.py:87 maps it to SYN_TS_NOT_PTS (blocking, fixable) -> bin 2 -> FIX_QUEUED. Attempt 2 (the last, C.FIX_RETRIES=2): plan_fixes sees SYN_TS_NOT_PTS with has_raw -> FIX_RETRANSLATE, which rebuilds every row from inputs.jsonl and therefore repopulates input_keys/input_actions inside the window attempt 1 blanked (the exact re-derivation r-loop 4 cited when reverting .gated_windows.json). Revalidation re-raises INP_FROZEN_ACTIONS; fix_attempts == 2 -> continuous.py:730 `REJECTED, 'fix retries exhausted (R2)'`. Deliverable footage permanently rejected, player unpaid, two paid Gemini sweeps spent, and ops sees only the bare fix-failed marker.
FIX: Mirror cutter.py:192. In retrim(), read `src_pts = V.frame_pts(src)` and use `i0 = len(rows) - new_info.frame_count`; stamp `created += timedelta(microseconds=src_pts[i0])` instead of `timedelta(milliseconds=base_ms)`. Keep base_ms for the CSV rebase only (its <=1 ms error is far inside the 100 ms FRAME_SYNC_MS bar). Add a test that head-retrims a raw-carrying synthetic session at a keyframe whose PTS is not a whole millisecond and asserts _verify_against_raw does not FAIL.


######## 3 [blocker] tools/vm_setup.sh:108 (lane delta) votes=2
TITLE: vm_setup --enable-continuous aborts (falsely) unless cwd is the repo root — after it has already disarmed the batch driver
CLAIM: The two inline Python probes added in r-loop 4 are cwd-dependent: `lock_free()` (tools/vm_setup.sh:105-116) runs `uv run python -` with `sys.path.insert(0, ".")` and `from pipeline import config, run`, and the CONT_DAILY_REPORTS interlock (tools/vm_setup.sh:142-143) does the same. The script never `cd`s — every other path in it is absolute (`UNITS="$HOME/hl-gamedata/pipeline/systemd"`, line 59) — and its own usage line (tools/vm_setup.sh:3) tells the operator to invoke it by absolute path: `bash ~/hl-gamedata/tools/vm_setup.sh [--enable-timers]`, i.e. from `$HOME`, which is where an ssh session lands. Verified: `cd /tmp && python3 -c "import sys; sys.path.insert(0,'.'); from pipeline import config, run"` -> `ModuleNotFoundError: No module named 'pipeline'`; the same line from the repo root imports fine. A ModuleNotFoundError makes `lock_free` return non-zero forever, which is indistinguishable in this code from "a live pid holds the lock".
SCENARIO: Operator ssh's to the VM and runs FLIP step 6e as the script's own header documents it: `bash ~/hl-gamedata/tools/vm_setup.sh --enable-continuous` (cwd = $HOME). Line 82 `systemctl disable --now hl-pipeline.timer` and line 92 `systemctl stop hl-pipeline.service` both execute. Then the 60x `lock_free` loop (lines 117-120) fails every iteration on ModuleNotFoundError, and line 121 `if ! lock_free` fires: the script prints "FATAL: run.lock is held by a LIVE pipeline process after 60s ... A batch run or a driver is genuinely still running" and exits 1 before ever reaching the enable. End state: hl-pipeline.timer disabled, hl-pipeline.service stopped, hl-continuous NOT enabled — NO driver armed at all, production down mid-flip, with a diagnosis that is false and sends the operator hunting a nonexistent live process. This is verbatim the end state the r-loop-4 comment at lines 96-102 says this block exists to prevent.
FIX: Make the script cwd-independent like the rest of it: add `cd "$HOME/hl-gamedata"` immediately after `set -euo pipefail`, or replace `sys.path.insert(0, ".")` with `sys.path.insert(0, os.path.expanduser("~/hl-gamedata"))` in both snippets. Additionally distinguish "probe could not run" from "lock is held": have `lock_free` print/exit a distinct code on ImportError so the FATAL text cannot blame a live pid for a broken probe.


######## 4 [major] pipeline/continuous.py:1105 (lane new-driver) votes=2
TITLE: Digest reports bad-path folders the operator FIXED as new quarantines
CLAIM: `quar_n` counts `COUNT(DISTINCT session_id) FROM events WHERE to_state='QUARANTINED' AND ts IN [lo,hi)`. But `ingest.scan`'s bad-path chase writes a **same-state** transition for every INT_PATH row whose folder has vanished from the Drive listing — `ledger.set_state(sid, 'QUARANTINED', 'bad-path folder no longer on Drive — dropped from the chase list')` (ingest.py:616-618) — and `Ledger.set_state` unconditionally appends an events row (ledger.py:130-137). The chase fires precisely when an operator has CORRECTED a badly-named folder, so the digest's window line inverts the meaning of the number: resolutions are printed as fresh quarantines.
SCENARIO: Three operators fix their misnamed folders overnight (a routine outcome of the folder-issues chase list). The next 5-min scan clears their INT_PATH reasons and stamps three QUARANTINED->QUARANTINED events. The 3-h digest — the ONLY ops surface at the flip, since CONT_DAILY_REPORTS ships False — prints `window: 0 delivered (+0.0 h) · 0 rejected · 3 quarantined`. Verified end-to-end by probe (/private/tmp/.../scratchpad/probe3.py): three bad-path rows created before the window, resolved inside it, yield `digest quarantined_n = 3` with zero actual new quarantines. During the canary this reads as the new driver quarantining sessions and is exactly the signal that would trigger a rollback.
FIX: Count only genuine entries into the state: add `AND from_state <> 'QUARANTINED'` to the digest query (and, independently, have the bad-path chase clear reasons via `set_reasons` + a `ledger.update` audit note instead of a same-state `set_state`, so no counter keyed on QUARANTINED events can ever see it).


######## 5 [major] pipeline/continuous.py:738 (lane core-flow) votes=2
TITLE: fix plan is built with the PRE-reroute game, so misfiled sessions get a wrong-game plan (and can be wrongly rejected)
CLAIM: _fix_one calls `plan = fix.plan_fixes(reasons, game=row['game'], has_raw=has_raw)` at line 738, where row['game'] is the DRIVE-FOLDER game — the very value STR_GAME_MISMATCH says is wrong. The reroute is only applied to the ledger at lines 750-755, after the plan is built AND after the `if plan['unfixable']` reject at line 740. plan_fixes branches on `game` in two places: INP_FANOUT (fix.py:120-126, fixable only for outer_wilds or when has_raw) and the hygiene->context coupling (fix.py:245, `hygiene_planned and game == 'outer_wilds'`). Verified with a probe on the real plan_fixes: reasons=[STR_GAME_MISMATCH(actual=outer_wilds), INP_FANOUT], has_raw=False -> game='kamla' gives steps=['FIX_REROUTE_GAME','FIX_SESSIONJSON_RECOMPUTE'] with unfixable=['INP_FANOUT']; game='outer_wilds' gives steps=['FIX_REROUTE_GAME','FIX_ACTIONS_CONTEXT','FIX_SESSIONJSON_RECOMPUTE'] with unfixable=[]. Same call pattern in the rollback path at run.py:513. Both reasons are produced in one pass (validate.py:494 _map_qa_issues and :497 _map_game_identity).
SCENARIO: An Outer Wilds session uploaded into the kamla/ tree (ledger game='kamla', session.json game_title='Outer Wilds'), no raw sidecars (an out/-only upload, which ingest accepts and merely records as incomplete). Validation emits STR_GAME_MISMATCH(actual=outer_wilds) + INP_FANOUT (qa-v2's same-literal fan-out, computed against the session's own OW keybind). _fix_one charges attempt 1, then plan_fixes(game='kamla') classifies INP_FANOUT as unfixable and line 741 writes REJECTED 'unfixable: [INP_FANOUT]' + finalize_rejected — the reroute never happens and the session is terminal, although FIX_REROUTE_GAME + FIX_ACTIONS_CONTEXT is exactly the designed fix. The milder variant (reasons INP_OSKEYS + STR_GAME_MISMATCH) plans FIX_KEY_HYGIENE with no FIX_ACTIONS_CONTEXT after it, so hygiene re-fans-out the multi-bound OW keys and INP_FANOUT is re-raised on revalidation, burning the second attempt and a second paid sweep.
FIX: Resolve the reroute target from the reasons BEFORE planning: scan `reasons` for STR_GAME_MISMATCH and, when params['actual'] is in C.GAMES, pass that as `game=` to plan_fixes (and commit `led.update(sid, game=...)` at the same point). Apply the identical change at run.py:513 so the dormant batch driver stays behaviour-identical.


######## 6 [major] pipeline/continuous.py:1459 (lane tests-ops) votes=1  INCOMPLETE-VERIFY
TITLE: unclean-drain os._exit(0) ends the pytest process with status 0 mid-suite
CLAIM: run_continuous()'s finally calls os._exit(0) whenever the drain grace expires with a lane thread alive or a gate slot held. run_continuous is called IN-PROCESS by ~25 tests (the `_run` helper, test_r_loop3, test_continuous), so that branch terminates the pytest interpreter itself with exit status 0 — no summary, no remaining tests, and a shell/CI reading the exit code sees success. The r-loop-4 note at :1440-1453 reasons only about the systemd service; the `elif any(t.is_alive()...)` guard at :1436 was written to keep tests returning normally, but it only covers the case where everything actually stopped.
SCENARIO: Proven in a scratch copy OUTSIDE the repo (mutrepo/pipeline/tests/test_zz_probe.py, since deleted): test A monkeypatches C.CONT_DRAIN_GRACE_S=1 and ingest.scan to sleep 30 s, then calls cont.run_continuous(cfg, max_wall_s=2, install_signals=False); test B asserts False. `pytest -v` printed only 'test_a_unclean_drain' and exited 0 — test B never ran and no failure was reported. Any lane step that outlives the grace does this: 10 s inside test_continuous.py (_fast_knobs), the real 600 s elsewhere. The flip's arming gate is literally 'full suite green' (FLIP_RUNBOOK), and this loop's evidence base is 'Suite 404 green Mac AND VM' — a partially-run suite is indistinguishable from a green one.
FIX: Do not exit the interpreter from a library call: gate the os._exit on process ownership (e.g. only when install_signals is True, or a new exit_process=False parameter that main() sets True), and return normally otherwise. Add a test that asserts run_continuous RETURNS on an unclean drain when it does not own the process.


######## 7 [major] pipeline/continuous.py:1064 (lane tests-ops) votes=2
TITLE: r-loop-4's stuck-list delivery aging can be deleted with the suite green
CLAIM: HEAD's own major fix — aging READY/PACKAGED/UPLOADED from the events audit because every delivery retry bumps updated_at via the rrd_sampled write — has no regression test. Mutation: delete the `for r in undelivered:` loop (:1063-1065) -> 362 passed, 1 skipped. test_r_loop3.test_stuck_list_sorted_so_oldest_hold_is_visible only exercises FIX_QUEUED and HOLD_VLM rows; the `rows` comprehension at :1060-1062 explicitly filters READY/PACKAGED/UPLOADED out, so with the loop gone those states are structurally unreachable in the stuck list again.
SCENARIO: A session whose upload fails forever (rclone 403 on one folder): _deliver_one leaves it READY on a 10-min cooldown and retries forever, and deliver_session's ledger.update(rrd_sampled=...) refreshes updated_at on every attempt, so it never matches the updated_at<cut query. With the branch reverted it never appears in the digest's stuck list. At the flip CONT_DAILY_REPORTS ships False, so the 3-h digest is the only ops surface — the session is invisible until someone reads the ledger by hand.
FIX: Seed a READY row with a >CONT_STUCK_H-old READY event and a fresh updated_at, then assert _stuck_lines names it and ages it from the event, not from updated_at.


######## 8 [major] pipeline/fix.py:274 (lane split-cascade) votes=2
TITLE: r-loop-4's gate-before-retrim ordering does not save the second fix attempt: FIX_RETRIM_HEAD re-phases the VLM sample grid, so INP_FROZEN_ACTIONS is re-raised anyway
CLAIM: r-loop 4 stopped dropping the gate when a head trim is planned and instead emits FIX_GATE_WINDOW then FIX_RETRIM_HEAD in one pass (fix.py:204-221, 272-275; verified by running plan_fixes: ['FIX_KEY_HYGIENE','FIX_GATE_WINDOW','FIX_RETRIM_HEAD','FIX_SESSIONJSON_RECOMPUTE']). The stated goal was to stop spending attempt 2 on the frozen window. It does not achieve that, because the detector's coordinate frame — not the CSV — is what the retrim invalidates. VLM sample times are anchored at the CLIP START (tools/analyze_sample.py:566-572: base=[0.3]; t=2.0; while t < duration_s-0.6: append(t); t+=interval), and a gating window's bounds are midpoints between those samples (analyze_sample.py:609-610: t0=(prev.t+run[0].t)/2, t1=(run[-1].t+next.t)/2), so the bounds already extend up to interval/2 = 2 s into live gameplay on each side. FIX_RETRIM_HEAD removes head_cut seconds and rebases every timestamp (tools/retrim_v2_session.py:64-70), so on revalidation the whole 4 s sample grid lands on different content (head_cut is a keyframe time, essentially never a multiple of 4 s) and every window bound moves by up to 2 s. GATE_PAD_FRAMES = 2 rows (config.py:87, ~66 ms at 30 fps) was calibrated for the scanner re-drawing a boundary by +-1 frame and cannot absorb that. This is a mechanism distinct from the open r-loop-3 #6 (model/label nondeterminism): it is a deterministic re-phasing introduced by the r-loop-4 edge itself, and it fires on exactly the trim+gate collision that fix.py:219-220 says R3 made common.
SCENARIO: Kamla session with a 12.5 s menu at the head and a 5.0 s mid-clip freeze at 59.5-64.5 s carrying inputs. Pass 1: reasons = CNT_EDGE_NONGAMEPLAY(head, 12.5) + INP_FROZEN_ACTIONS(59.5, 64.5). Plan runs hygiene, gates rows 59.5-64.5 (+-2 frames), then retrims 12.5 s. fix_attempts = 1. Pass 2 re-validation on the trimmed clip: the freeze now sits at 47.0-52.0, the sample grid is 0.3, 2, 6, ... 46, 50, 54 against content that used to be sampled at 58, 62, 66, so the run's flanking samples differ and the window comes back as e.g. 46.0-51.5 (= 58.5-64.0 pre-trim). Rows 58.5-59.5 were never blanked and hold real gameplay input -> action_frames > 0 -> INP_FROZEN_ACTIONS re-fires -> FIX_QUEUED, fix_attempts = 2 spent re-gating. Any third reason arriving on that pass now has no budget: _fix_one rejects with 'fix retries exhausted (R2)' (continuous.py:729-731) and reports.session_reject_labels shows only the bare 'fix-failed' marker (reports.py:57, 80-82) — precisely the outcome r-loop 4 said it had fixed.
FIX: Two options, both cheap. (a) Move the gate after the retrim and rebase its windows by the head_cut the tool actually applied: retrim() already returns {'head_cut_s': ...}, so apply_fixes can subtract it from the pending gate windows before dispatching FIX_GATE_WINDOW — the gate then runs in the same coordinate frame the next detection will use. (b) Size the pad to the detector's geometry instead of the scanner's: gate [cut0 - interval/2, cut1 + interval/2] (2 s at the default 4 s sweep) for VLM-sourced windows, keeping the 2-frame row pad for scanner-sourced ones. (b) also closes the open #6 for the non-retrim case; do not reintroduce a coordinates sidecar.


######## 9 [major] pipeline/fix.py:192 (lane core-flow) votes=1  INCOMPLETE-VERIFY
TITLE: plan_fixes silently drops every gate window whenever the plan contains a cut
CLAIM: r-loop 4 fixed the head-trim path so FIX_GATE_WINDOW is 'DEFERRED, not dropped' (fix.py:201-221), but the three cut-bearing exits still discard gate_windows outright: line 192 `if cut_windows or (head_cut and tail_cut):` returns at line 200, and line 222 `if tail_cut:` returns at line 225 — both before the `if gate_windows: steps.append(('FIX_GATE_WINDOW', ...))` at line 272. Verified with a probe against the real plan_fixes: head+gate -> ['FIX_GATE_WINDOW','FIX_RETRIM_HEAD','FIX_SESSIONJSON_RECOMPUTE']; TAIL+gate, mid-cut+gate, AFK-cut+gate and head+tail+gate all -> ['FIX_CUT_SEGMENTS'] with GATE=False. Nothing carries the discarded window forward: gate.py's sidecar was deliberately reverted in r-loop 4, so the parent's confirmed detection is lost and the children inherit the frozen window's action frames verbatim (cutter.py:173 slices the parent's rows). R3 makes this collision common exactly as the head_cut comment says — a 2-5 s mid-clip window is now a GATE, which cannot merge into FIX_CUT_SEGMENTS the way a cut did.
SCENARIO: OW session, reasons = CNT_AFK(cut 500-540s) + INP_FROZEN_ACTIONS(300-303s, 41 action frames). Attempt 1 plan = ['FIX_CUT_SEGMENTS'] only; apply_fixes breaks after the cut (fix.py:300), parent -> SPLIT, child -p1 carries rows 300-303 s with input_actions intact. The child re-enters Phase II and must pay a full second Gemini sweep and spend one of its own two fix attempts re-deriving and gating a window the parent had already confirmed. If the child's sweep does not re-raise it — the VLM samples every 4 s so a 3 s freeze can fall between samples, _map_windows requires tier=='high', and _build_aux drops scanner statics with `a > 1.0 and b < duration - 1.0` — the segment ships with semantic actions recorded during a confirmed freeze, which is the client complaint the gate exists to prevent.
FIX: Emit FIX_GATE_WINDOW immediately BEFORE FIX_CUT_SEGMENTS in all three cut exits (after _pre_cut_csv_fixes()). The gate only blanks input_keys/input_actions in place on the parent's own timeline — correct at the moment it runs — and cutter copies rows through unchanged, so children inherit the blanking and never re-raise the reason. Add plan-ordering tests for tail+gate, mid-cut+gate and AFK+gate mirroring the existing head+gate test at test_fix_cut_gate.py:321.


######## 10 [major] pipeline/run.py:1518 (lane tests-ops) votes=2
TITLE: the batch driver never consults PIPELINE_CONTINUOUS, so a re-armed hl-pipeline.timer can down the continuous driver for good
CLAIM: PIPELINE_CONTINUOUS is read in exactly one place (continuous.run_continuous:1350) and asserted by exactly one test. run() takes the lock and proceeds unconditionally, so the flag is a one-way interlock: it stops the continuous unit when False, but nothing stops the batch driver when True. The run lock only guarantees the two never run at the SAME instant — it does not stop the batch driver from taking over during a continuous restart window. FLIP_RUNBOOK's rollback paragraph enables hl-pipeline.timer and the file ends there (154 lines): there is no roll-forward procedure and no mention of `systemctl reset-failed` anywhere.
SCENARIO: After a rollback, an operator rolls forward the obvious way — set PIPELINE_CONTINUOUS=True, deploy, `systemctl start hl-continuous` — without re-running vm_setup --enable-continuous (whose disable-the-timer block is the only thing that closes this). Both are armed. hl-continuous is OOM-killed or restarts (RestartSec=10); the next *:0/30 tick wins run.lock and hl-pipeline.service is Type=oneshot with TimeoutStartSec=infinity, so a backlog run holds it for hours. run_continuous returns 1 on each restart; 5 attempts in ~50 s burn StartLimitBurst, the unit enters 'failed', and the OnFailure alert says 'CRASH-LOOPING' (wrong cause). Production silently runs on the batch driver, creating batch rows on the continuous ledger, and the continuous driver stays down after the batch run ends until a manual reset-failed.
FIX: Mirror the continuous side: in run(), if C.PIPELINE_CONTINUOUS is True, print the reason and return 0 before acquire_lock; add a test alongside test_flag_interlock. Add a roll-forward section to FLIP_RUNBOOK that routes through vm_setup --enable-continuous and names reset-failed.


######## 11 [major] pipeline/validate.py:511 (lane split-cascade) votes=2
TITLE: Gating can manufacture CNT_ACTIONS_FEW / INP_KEYS_MISSING — an unfixable reject that blames the player for rows the pipeline blanked
CLAIM: FIX_GATE_WINDOW blanks input_keys AND input_actions (pipeline/gate.py:51-57) and the session then goes through a FULL re-validation (continuous.py:796 -> _validate_one -> validate_session), where the inventory is recomputed from the gated frames.csv (tools/analyze_sample.py:245 distinct_actions = len(acts)). map_reasons then re-tests that recomputed number at pipeline/validate.py:511 and emits CNT_ACTIONS_FEW with blocking=True, fixable=False -> bin 3 -> REJECTED (continuous.py:689). Nothing anywhere subtracts the frames the pipeline itself emptied. R1+R3 multiply the exposure by roughly an order of magnitude: each kept window is now up to 5 s instead of 2 s, and the scanner path that previously CUT almost every window on any clip shorter than ~1000 s (old bar was min(2.0, 0.002*dur), i.e. 1.2 s on a 600 s clip and 0.4 s on a 200 s clip, both under SCANNER_STATIC_MIN_S=0.8) now GATES every window <=5 s (validate.py:404-409), with up to 40 of them per session. The same failure shape was already ruled a major in r-loop 4 ("the keybind inversion ... emptying the whole keyboard column and rejecting the session as CNT_ACTIONS_FEW (unfixable) with the player coached to 'play actively' for our parser bug") — this is the second, unfixed instance of it. INP_KEYS_MISSING (validate.py:531, also unfixable, 're-record (never fabricate)') is reachable the same way for a mouse-heavy session whose only key presses fall inside frozen contexts.
SCENARIO: Outer Wilds session, 210 s, three distinct actions {look, thrust, interact}. `interact` appears only in the ~40 frames where the player is at the Observatory / ship terminal — an unmodelled frozen context (see project memory 'OW satellite-camera context gap'). Pass 1: scanner finds a 3.1 s static window there, classify_stills labels it other_non_gameplay/high, action_frames>0 -> INP_FROZEN_ACTIONS (validate.py:406). plan_fixes -> FIX_GATE_WINDOW (fix.py:273) blanks those rows +-2 frames. Pass 2 re-validation: inventory distinct_actions = 2 -> CNT_ACTIONS_FEW, blocking + UNFIXABLE -> bin 3 -> REJECTED. deliver.finalize_rejected writes coaching.md with _COACHING['CNT_ACTIONS_FEW'] = 'Play actively — sessions need several different in-game actions.' (deliver.py:234-236, 274-280). A deliverable session is lost, the player is unpaid, and the operator-facing record blames the player for actions this pipeline deleted.
FIX: Make the content-inventory gates blind to pipeline-blanked rows. Cheapest correct option: have gate.gate_windows return the pre-gate action/key inventory it destroyed (it already counts `gated` rows and knows the exact row set) and record it in the fixlog; in map_reasons, when a CNT_ACTIONS_FEW / INP_KEYS_MISSING deficit is fully attributable to gated rows, emit an operator advisory (or a distinct non-player-blaming code) rather than an unfixable reject. Alternative, no cross-pass state: in _map_windows, refuse to route a window to the gate when gating it would take the session under MIN_DISTINCT_ACTIONS and route it to CNT_MID_NONGAMEPLAY (cut) instead — the pre-R3 behaviour for exactly this population.


######## 12 [major] pipeline/validate.py:928 (lane validation-stack) votes=1  INCOMPLETE-VERIFY
TITLE: A holder whose lock the staleness breaker rescinded still rmdirs that path, disarming the mutex for the next holder
CLAIM: _locked_report_update's release is `if held: os.rmdir(lock)` (validate.py:927-932) — it removes whatever directory currently sits at the lock path, with no check that it is still OUR lock. The breaker at validate.py:892-906 renames a lock older than C.REPORT_LOCK_STALE_S aside and lets the next waiter mkdir a fresh one, but the original holder's `held` flag is still True and its finally deletes the SUCCESSOR's lock. r-loop 3 lowered REPORT_LOCK_STALE_S from 120 s to 20 s (config.py:213) to satisfy the WAIT>STALE invariant, which shrank the window in which a live-but-slow holder is judged dead by 6x. r-loop 4 recognised precisely this multi-racer-one-pid problem for the grave NAME (validate.py:894-903) but did not extend it to the release.
SCENARIO: On the VM, thread A (a V session runner finalizing a reject via deliver._drop_shift_entry) holds the lock and is descheduled/blocked for >20 s — 44 concurrent validation subprocesses plus ffmpeg/cv2 saturating disk and the documented ballooning memory pressure (FLIP_RUNBOOK §6b measured 2.06 TiB cumulative balloon over 27 h). Thread B's breaker fires, renames A's lock to a grave, rmtrees it, mkdirs its own, and enters the section with held=True. A resumes, does its own read-modify-write (lost update #1 — B's just-written entry or A's is discarded), then rmdirs B's lock while B is still inside. Thread C now mkdirs successfully and writes concurrently with B (lost update #2). The lost record is a session's {"shift_us": ...} entry, so translator.v2._applied_shift_us returns 0 for it, qa-v2 re-bins the raw mouse events at head offset 0, _verify_against_raw reports a systematic dx/dy mismatch, and the session takes a spurious SYN_TS_NOT_PTS / raw-recomputation FAIL — one of only two fix attempts and one paid Gemini sweep burned on a session with nothing wrong with it (the exact r1 #8 class the lock exists to close).
FIX: Make the lock owner-stamped: after os.mkdir(lock), write a token file (pid-tid-uuid) inside it; in the finally, read that file and rmdir only if the token still matches (else log and leave it alone — someone else owns it now). Equivalently, release by renaming the lock to our own unique grave and rmtree'ing that, so a rescinded holder's rename fails harmlessly instead of removing a live lock.


######## 13 [major] pipeline/validate.py:115 (lane fix-regressions) votes=1  INCOMPLETE-VERIFY
TITLE: de-mapping "frame_id column unparseable" cancels a repairable row-count delta
CLAIM: r-loop 4 deleted `("frame_id column unparseable", "STR_ROWS_MISMATCH", True)` from `_QA_STR_MAP` on the grounds that FIX_ROWS_SURGERY can never rewrite a frame_id cell. But that FAIL co-occurs with the qa FAIL `row count N != session.json frame_count M`, which FIX_ROWS_SURGERY clears exactly by TRUNCATING the offending tail rows. Unmapped, the issue becomes QA_FAIL_UNMAPPED whose `fixable` is `has_raw`; with no sidecars it is unfixable and a single unfixable blocking reason forces bin 3, so map_reasons rejects the session outright instead of running the surgery that would have repaired both FAILs.
SCENARIO: A no-sidecar v2 upload (a zip carrying only out/, or any session whose raw/ was not delivered) has one extra well-formed 37-column tail row whose frame_id cell is non-integer (duplicated header line, blank cell, spreadsheet round-trip). check_session_v2 emits `FAIL: row count 1801 != session.json frame_count 1800` then `FAIL: frame_id column unparseable (...)`. VERIFIED by running the real code: `_map_qa_issues(..., has_raw=False)` now yields `[('STR_ROWS_MISMATCH', True), ('QA_FAIL_UNMAPPED', False)]` and `map_reasons` returns bin=3, so `_validate_one` writes REJECTED + finalize_rejected immediately — while `plan_fixes` on the same reasons still produces `FIX_ROWS_SURGERY` (which truncates delta=+1 and clears both FAILs). Pre-r-loop-4 both strings collapsed onto STR_ROWS_MISMATCH, giving bin=2 and a delivered session. Result: a fully repairable session is rejected with zero fix attempts and the player is unpaid.
FIX: Either restore the mapping and let the surgery run (its no-op case costs one attempt, not a wrongful reject), or keep it unmapped but make QA_FAIL_UNMAPPED non-blocking/advisory when another blocking-and-fixable reason from the SAME early-returned qa pass is already present — i.e. never let an unmapped FAIL upgrade a bin-2 verdict to bin 3.


######## 14 [major] pipeline/validate.py:1146 (lane tests-ops) votes=2
TITLE: R1's ruled-KEPT VLM label+confidence filter (and the whole scanner-statics classification arm) is executed by zero tests
CLAIM: Ruling R1 keeps 'the VLM label+confidence filter'. Replacing the whole condition at validate.py:1143-1146 with `if True:` leaves 395 passed / 2 skipped. Stronger: inserting `raise AssertionError` before `eng = load_engine()` (validate.py:1122) fails exactly ONE test (test_notif_confirm_runs_without_scanner, the notification arm) — so the statics arm, the AFK label carve-out (:1189-1191), the r-loop-3 inclusive-end action_frames count (:1170-1173) and the F5 'no VLM verdict' advisory (:1139) are never executed by the suite at all. test_split_cascade_r1r3 only exercises _map_windows with hand-built dicts, which is downstream of every one of these.
SCENARIO: Drop or widen the filter (a plausible edit while tuning R1 thresholds) and every measured-still window becomes an acted-on extra_window regardless of what the VLM said: a >5 s window the model labels 'gameplay' now raises CNT_MID_NONGAMEPLAY and cuts a child out of real play — re-arming exactly the split cascade R1 exists to stop — while a low-confidence <=5 s window raises INP_FROZEN_ACTIONS and blanks real inputs. Nothing in the suite fails. Same for the AFK arm: dropping the dialogue/map exclusion cuts OW Nomai-reading footage, which the locked AFK rule calls gameplay.
FIX: Unit-test _build_aux's statics arm with the existing module seams — monkeypatch vlmmod.classify_stills and validate.load_engine/FrameGrabber — asserting (a) a 'gameplay' label and a 'low' conf produce no extra_window, (b) a high-conf 'loading' label does, (c) action_frames includes the end frame.


######## 15 [major] pipeline/vlm.py:161 (lane validation-stack) votes=2
TITLE: 429-backpressure channel is blind to the outage shape that actually stalls the pool
CLAIM: _pressure() is called ONLY from the HTTPError 429/5xx branch (vlm.py:150-151). The transport-failure branch (vlm.py:161-177 — URLError, TimeoutError, OSError/SSL/ConnectionReset, HTTPException, JSONDecodeError) sleeps the same exponential backoff and writes NOTHING to vlm-pressure.jsonl. On top of that, the 5xx lines that ARE written are dropped by the consumer: continuous.py:937 counts only `status == 429` into `_pressure_recent`. So two of the three failure classes the retry ladder distinguishes produce p429_per_min == 0, and autoscale rule 1 (the CONT_STEP_DOWN=4 step-down) can never fire for them. Meanwhile autoscale rule 3 (continuous.py:216-219: `cpu_pct < CONT_CPU_HIGH and queue_depth > active` -> target += 2) is *satisfied* by exactly the state a backoff storm creates, because every validation subprocess is asleep in time.sleep(), not burning CPU. This directly contradicts the design's own stated rationale for the file channel ("gives real-time backpressure even while every worker is asleep mid-backoff; worker-result-only signaling would be blind exactly when it matters", PIPELINE_CONTINUOUS_DESIGN.md §4).
SCENARIO: Gemini starts dropping TLS connections / timing out (endpoint saturation or a partial network fault) rather than returning HTTP 429. Each generate() call burns VLM_MAX_TRIES=5 attempts with 2/4/8/16 s sleeps per endpoint, per rung — a 10-minute session's sweep (~19 chunks of 8 frames) spends tens of minutes asleep. Host CPU falls well under CONT_CPU_HIGH=85 while V_DEPTH_STATES backlog grows, so every 60 s _autoscale_tick steps the target UP by CONT_STEP_UP=2 until it hits CONT_POOL_MAX (44 on c2d-highcpu-56). Forty-four runners now each pay a full scanner decode + engine analyze + a doomed sweep, all end VLMError -> HOLD_VLM, HOLD_VLM counts in LOCAL_STATES so _pick_download stops intake at CONT_MEDIA_CAP_SESSIONS=40, and each held session re-pays the whole battery every CONT_HOLD_RETRY_MIN=30 min forever. The one control that was designed to prevent this (step down to CONT_POOL_MIN) is provably unreachable because no pressure line was ever written and no rung ever climbed (nothing succeeded at any rung, so absorb_rung is never called either).
FIX: Call _pressure(0, tag) (or a distinct status like -1 / 599) from the network-error branch at vlm.py:161-177 before the backoff sleep, and widen the consumer at continuous.py:937 from `== 429` to "any pressure event" (or count 429 + 5xx + transport separately and feed the union into p429_per_min). Keeping the field name p429 while the writer emits 5xx and the reader silently discards them is the second half of the bug.


######## 16 [major] tools/analyze_sample.py:1276 (lane translator) votes=2
TITLE: The engine re-reads session.json and frames.csv unguarded right after check_session_v2 FAILed them — the r-loop-1/3 'FAIL, never crash' hardening never reaches the pipeline
CLAIM: analyze() calls check_session_v2 at line 1256 and then, with no early return on FAIL, re-reads the same two untrusted files itself: `json.loads((sdir/'session.json').read_text())` at 1247 with bare `float()/int()` casts at 1250-1252, and `(sdir/'frames.csv').open(newline='')` at 1276 with no encoding or exception guard. Both re-derive exactly the values the checker deliberately normalizes/guards (translator/v2.py:567-571 normalizes malformed numerics after emitting the type FAIL; v2.py:580-587 catches UnicodeDecodeError/OSError/csv.Error on frames.csv). Confirmed by running the shipped checker: session.json with fps=[30] yields `FAIL: session.json numeric fields malformed (non-numeric type)` (mapped at validate.py:122 to STR_SJ_INVALID, fixable → FIX_SESSIONJSON_REWRITE repairs it), while `float(s.get('fps') or 0)` on the same value raises TypeError.
SCENARIO: An uploaded v2 session.json carries fps as a list (or frames.csv carries one cp1252 byte in a key token — the exact input the r-loop-3 guard cites). detect_format returns 'v2' (session.json parses, game_title present, frames.csv exists), check_session_v2 produces the actionable FAIL, then line 1250 (TypeError) or line 1276 (UnicodeDecodeError) raises out of analyze() → out of validate_session → run.py:166 wraps it as {'error': …} → continuous.py:671-676 writes QUARANTINED 'validation crashed' and alerts. The session lands in the manual queue with its media held for CONT_QUARANTINE_RECLAIM_H=48 h instead of taking the one-attempt FIX_SESSIONJSON_REWRITE / typed reject the checker's verdict describes — i.e. the accepted r-loop-1/r-loop-3 fixes buy nothing in production.
FIX: Reuse the checker's already-parsed data or guard these reads the same way: wrap 1247 and 1276 in try/except (OSError, UnicodeDecodeError, json.JSONDecodeError, csv.Error) setting a.error to a mapped structural message, and coerce the numerics with the same `isinstance(..., (int,float)) and not bool` normalization v2.py:567 uses.


######## 17 [major] tools/recal_regen_sheets.py:192 (lane tools) votes=2
TITLE: stray-stamp gate false-aborts on QUARANTINED/DUPLICATE roots that recal_rebuild_reset deliberately left stamped
CLAIM: The stray-stamp pre-check scans EVERY root with `uploaded_reported_at IS NOT NULL` (line 192-194) and aborts rc=2 for any whose top-root ctime < HI16, on the stated assumption that the only way a cohort root can carry a stamp is a post-flip daily send breaching the CONT_DAILY_REPORTS interlock. That assumption is false. `recal_rebuild_reset.py` nulls `uploaded_reported_at` only for roots NOT in `KEEP_STATES = ("QUARANTINED","DUPLICATE")` (recal_rebuild_reset.py:27, 57-59, 92-95), so any cohort root that carried a PRE-rebuild stamp and is now QUARANTINED or DUPLICATE keeps it. Such stamps are reachable: `build_sheet_rows` stamps every in-window root that is merely `r_countable` — duration_raw_s probed — regardless of state (reports.py:462-467), and it only skips DUPLICATE/QUARANTINED roots as evaluated AT GENERATION TIME (reports.py:406-408). A root probed at ingest, counted+stamped by the original 08-15 or 08-16 send while still INGESTED/VALIDATING, and quarantined afterwards by a validation crash, is exactly this shape.
SCENARIO: Root R (player_email set, duration_raw_s probed at ingest on 08-14) is still VALIDATING when the original 08-15 daily send runs; build_sheet_rows counts it and mark_uploads_reported stamps it. On 08-15 its validation subprocess crashes -> QUARANTINED. recal_rebuild_reset skips it (KEEP_STATES) so the stamp survives. At FLIP_RUNBOOK step 7.2 the operator runs recal_regen_sheets: `recorded` is empty (no .regen-v2-counted.json exists yet), R has parent_id NULL, uploaded_reported_at NOT NULL, and ct < hi16 -> the tool prints 'ABORT: 1 COHORT root(s) already stamped outside our resume records — a daily send counted rebuild-cohort hours into the wrong sheet (CONT_DAILY_REPORTS interlock breached?)' and returns 2. The payment endgame is hard-blocked with no override flag, and the diagnosis names the wrong subsystem (the interlock was never breached), sending the operator to audit CONT_DAILY_REPORTS and the driver instead of a pre-rebuild residue. No regenerated sheet could ever have counted R anyway, since build_sheet_rows skips QUARANTINED roots.
FIX: Exclude the states recal_rebuild_reset deliberately preserved from the stray scan — add `AND state NOT IN ('QUARANTINED','DUPLICATE')` to the query at line 192-194 (they are structurally uncountable by build_sheet_rows, so their stamps cannot empty a superseding sheet), and print them as a separate 'pre-rebuild residual stamp (ignored)' note rather than an abort.


######## 18 [major] tools/recal_verify_tree.py:133 (lane tools) votes=2
TITLE: in-flight carve-out only covers rows with a prior UPLOADED event, so a first-time PACKAGED session's remote dir is still branded STALE
CLAIM: The r-loop-4 fix builds `inflight` by looking up, for each PACKAGED/UPLOADED row, the latest `to_state='UPLOADED'` event with a 'verified at <path>' detail (lines 133-139). But `deliver_session` sets PACKAGED (deliver.py:196) and only writes the UPLOADED event AFTER `upload_and_verify` returns (deliver.py:200-203), so a session interrupted during its FIRST upload has no UPLOADED event at all. `inflight` therefore covers only rows already in state UPLOADED (and re-deliveries of previously-uploaded rows), and misses precisely the long-running case the fix's own comment describes: 'deliver_session sets PACKAGED and then runs upload_and_verify, which can take hours, so deleting one discards a verified partial upload the driver would otherwise resume into.' The tool still contradicts itself about the same path.
SCENARIO: At FLIP_RUNBOOK step 8 the operator runs `systemctl stop hl-continuous` while a 3 GB kamla session X is 60% through its first upload. The ledger row is PACKAGED, no UPLOADED event exists, and Drive II holds a partial `humynlabs/08-19-2026/kamla/X/`. recal_verify_tree emits both 'stuck mid-delivery: X (PACKAGED)' AND 'STALE remote dir (no session row): humynlabs/08-19-2026/kamla/X' — the second label's documented meaning is 'listed for cleanup', immediately before the runbook's last destructive act. An operator clearing the listed defects deletes a live delivery path; if done after the driver restarts, the in-flight `upload_and_verify` listing check fails and the session is bounced to failed_upload + 10-min cooldown instead of resuming.
FIX: For stuck rows with no UPLOADED event, reconstruct the dir from the audit trail instead of giving up: the PACKAGED transition detail is `staged <date> rrd_sampled=<b>` (deliver.py:196-197), so `SELECT detail FROM events WHERE session_id=? AND to_state='PACKAGED' ORDER BY ts DESC LIMIT 1` yields the date, and `humynlabs/<date>/<game>/<sid>` is the exact remote_dir deliver.py:198 builds. Add that to `inflight` (falling back to `deliver.staged_date(cfg, sid, game)` when the event is missing).


######## 19 [major] tools/retrim_v2_session.py:90 (lane core-flow) votes=2
TITLE: FIX_RETRIM_HEAD renders a full video-sized session.rrd into the work dir that nothing ever reads
CLAIM: retrim() ends with `rrd.generate(out_dir, timeout_s=1800)`, and in the fix path out_dir IS the work dir (fix.py:374 `tool.retrim(work, params['head_s'], work)`). The generated script embeds the whole clip via `rr.AssetVideo(path=str(video_mp4))` (translator/rrd.py:41) and logs 5 entries per frame, so session.rrd comes out roughly video-sized — the repo's own evidence is deliver.py:126 `timeout_s=21600  # rrd-sampled uploads are big`. Nothing consumes it: translator/v2.py:546-549 only checks that session.rrd EXISTS (which is why ingest.py:806-807 touches a 0-byte stub), deliver.stage_session copies SPEC_FILES only and regenerates the rrd inside the stage dir for sampled sessions (deliver.py:85-93), and the work copy is deleted at the terminal wipe. So every head-trim fix pays a full rerun render (hundreds of thousands of log calls plus the video blob) while holding a ResizableGate slot, and roughly doubles that session's bytes on disk.
SCENARIO: A 30-minute OW session (~1.5 GB at the repo's own ~3.13 GB/footage-hour estimate) gets CNT_EDGE_NONGAMEPLAY(head). FIX_RETRIM_HEAD trims it and then writes a ~1.5 GB session.rrd into work/<sid>/, taking minutes inside the runner's gate slot. The media cap (CONT_MEDIA_CAP_SESSIONS=40) is a SESSION count used as the bytes bound (design §7), so with a meaningful share of the 40 local sessions head-trimmed the cap now admits up to ~2x the bytes it was calibrated for on a 250 GB disk (tools/provision_vm.sh:21) whose F7 low-water is 100 GB — and F7 pauses intake, which is the failure r-loop 3 described as 'a stop with no path back'. Pure waste: not one byte of that rrd is read or shipped.
FIX: Give retrim a `make_rrd: bool = True` parameter and pass False from fix.py's FIX_RETRIM_HEAD dispatch, touching a 0-byte session.rrd instead (the same stub ingest.download writes for the presence gate). Standalone CLI use keeps the default True.


######## 20 [major] translator/translate.py:83 (lane translator) votes=2
TITLE: r-loop 4's keybind-direction fix stops an inverted KAMLA keybind.json from being flipped — every key is then 'unbound' and the whole keyboard column ships empty
CLAIM: _looks_semantic returns False at line 83-84 for any string without an underscore, BEFORE the authoritative vocabulary test at line 85. Two of Kamla's seven semantic action names have no underscore — 'interact' and 'look' — so they can never reach `x in _SEMANTIC_VOCAB`. _as_semantic_to_literal's `looks_inverted` (line 106) therefore evaluates False for a genuinely inverted Kamla file and it is used as if it were semantic→literal. Verified by running the shipped code: `invert_keybind(KEYBINDS['kamla'])` = {"a":["move_left"],…,"e":["interact"],"mouse":["look"]} is returned unchanged, and bound_literals of the result is ['interact','look','move_down','move_left','move_right','move_up','pause_menu'] — the action names, not the keys. Outer Wilds is unaffected (every OW action name has ≥1 underscore). This is a regression from the r-loop-4 fix itself: the previous predicate (`all values are lists`) flipped this file correctly.
SCENARIO: A contributor uploads a Kamla bundle whose keybind.json is in literal→[semantic] form (byte-identical to the shape our own v1 key_binding.json deliverable has). translate_bundle_v2 (v2.py:268) or, worse, FIX_RETRANSLATE — the universal strong fix, fix.py:528 — calls resolve_keybind, gets the un-flipped dict, and builds rules keyed on 'interact'/'look'/'move_up'. No real key token is in `bound`, so _v2_rows (v2.py:196-198) strips every key on every row and resolve_actions fires nothing: input_keys and input_actions are empty in all N rows. qa-v2 only warns ("only 0 distinct actions"), but validate.py:511 raises CNT_ACTIONS_FEW and validate.py:533 raises INP_KEYS_MISSING — both blocking and UNFIXABLE — so the session is REJECTED with no fix attempt and the player is coached to re-record/'play actively' for a parser bug. A session that arrived clean and merely needed a retranslate is destroyed the same way.
FIX: Test the vocabulary first: `if x in _SEMANTIC_VOCAB: return True` before the `"_" not in x` rejection, keeping the `x.count("_") >= 2` fallback for out-of-vocabulary names. That still refuses to flip the r-loop-4 case ({"movement_move_x_axis": ["a","d"]}), because literals like 'a'/'d' are neither in the vocabulary nor underscored.


######## 21 [minor] PIPELINE_ARCHITECTURE.md:156 (lane split-cascade) votes=2
TITLE: Architecture doc still documents the gate-dropping behaviour r-loop 4 deleted, and the pre-r-loop-4 step order
CLAIM: The design doc of record contradicts the shipped code on the edge r-loop 4 changed. Line 156 states: '`FIX_RETRIM_HEAD` | Head-only trim ... Gate windows are dropped with it — pre-trim timestamps would blank the wrong frames.' That behaviour was deleted in 9886596 (fix.py no longer sets `gate_windows = []`; the retrim is deferred to fix.py:274-275). Lines 132-138 likewise place `RETRIM_HEAD / CUT_SEGMENTS` ABOVE `KEY_HYGIENE` and `GATE_WINDOW`, while the code emits RETRIM_HEAD LAST of the three — verified by running plan_fixes on a head-trim + gate + hygiene reason set: ['FIX_KEY_HYGIENE','FIX_GATE_WINDOW','FIX_RETRIM_HEAD','FIX_SESSIONJSON_RECOMPUTE']. r-loop 4 listed 'architecture doc documented the pre-r-loop-3 fix order as the design' among its own fixes and updated the block for r-loop 3's reordering, but not for its own; the table row was missed entirely. The block ends with the standing instruction 'Do not "restore" the older order', which now points at the wrong order.
SCENARIO: A maintainer (or the flip/canary operator following FLIP_HANDOVER) reads PIPELINE_ARCHITECTURE.md §2, sees that gate windows are supposed to be dropped when a head trim is planned, and 'restores' `gate_windows = []` in the head_cut branch of plan_fixes — reinstating the r-loop-4 major (a whole fix attempt spent re-gating, deliverable sessions rejected as 'fix retries exhausted'). The two new tests in test_fix_cut_gate.py would catch the code change, but the doc is what a human reads first and it currently reads as the authority.
FIX: Update line 156 to 'Head-only trim (notification/menu at clip start). Emitted AFTER the gate: the gate's windows are pre-trim coordinates, correct at the moment it runs, and the retrim only slices head rows and rebases — it never re-derives actions.' and reorder lines 132-138 to structural -> KEY_HYGIENE -> ACTIONS_CONTEXT -> other CSV writers -> GATE_WINDOW -> RETRIM_HEAD / CUT_SEGMENTS -> SESSIONJSON_RECOMPUTE.


######## 22 [minor] PIPELINE_ARCHITECTURE.md:135 (lane delta) votes=2
TITLE: New architecture doc records the pre-r-loop-4 fix order it was committed alongside the reversal of
CLAIM: PIPELINE_ARCHITECTURE.md was added in commit 9886596 — the same commit that reordered `plan_fixes` — yet it documents the superseded order. Line 135 puts `RETRIM_HEAD / CUT_SEGMENTS` before `KEY_HYGIENE ... GATE_WINDOW`, and line 156 states "`FIX_RETRIM_HEAD` | Head-only trim ... Gate windows are dropped with it — pre-trim timestamps would blank the wrong frames." The code does the opposite: pipeline/fix.py:204-221 defers the retrim into `head_step` instead of dropping the gate, and fix.py:272-275 appends `FIX_GATE_WINDOW` first and `head_step` after it, giving structural -> hygiene/context/CSV -> GATE_WINDOW -> RETRIM_HEAD -> SESSIONJSON_RECOMPUTE. The document contradicts itself internally: its own lines 141-149 explain why GATE_WINDOW is last and close with "Do not 'restore' the older order" — printed directly beneath the block that prints the older order.
SCENARIO: The flip session (FLIP_HANDOVER.md directs it to read the architecture reference) or any later maintainer verifies the fix chain against PIPELINE_ARCHITECTURE.md:135/156, concludes the code is wrong because RETRIM_HEAD is emitted last, and 'restores' the documented order. That reinstates exactly the r-loop-4 defect: a session carrying both CNT_EDGE_NONGAMEPLAY(head) and INP_FROZEN_ACTIONS has its gate dropped, the frozen-actions reason survives untouched into revalidation, attempt 2 is spent gating it, and any third reason then rejects a deliverable session as "fix retries exhausted (R2)" — an unpaid player surfaced to ops as a bare fix-failed marker.
FIX: Rewrite the order block at PIPELINE_ARCHITECTURE.md:132-139 to `... -> CUT_SEGMENTS -> KEY_HYGIENE -> ACTIONS_CONTEXT -> other CSV writers -> GATE_WINDOW -> RETRIM_HEAD -> SESSIONJSON_RECOMPUTE`, and replace line 156's "Gate windows are dropped with it" with the current rule: gate windows carry pre-trim timestamps and are therefore applied BEFORE the retrim, which only slices head rows and rebases survivors (tools/retrim_v2_session.py:66-73) and never re-derives input columns.


######## 23 [minor] pipeline/continuous.py:1239 (lane new-driver) votes=2
TITLE: A failed daily send regenerates the payment sheet every 20 s instead of once per batch
CLAIM: H calls `runmod.send_daily_report_if_due` on every ~20 s tick (the `self.stop.wait(20)` at continuous.py:1263 bounds the body). That function builds the sheet BEFORE it sends (`reports.write_payment_sheet`, run.py:830-832) and returns False without writing the marker or the anchor when Telegram fails (run.py:841-845). In the batch driver the same function was reached at most once per batch drain plus once at end-of-run — tens of minutes apart. Under the continuous driver a Telegram outage after 14:00 IST turns it into ~180 full sheet generations per hour, each doing a whole-table `player_rollup` + `build_folder_issues` scan and each rewriting `reports/<day>/payment-<day>.csv` non-atomically via `csv_path.open('w')` (reports.py:544-548).
SCENARIO: Telegram is unreachable from 14:00 to 16:00 IST (the payment-endgame window, when an operator is being told to fetch the sheet off the VM). For two hours the H thread regenerates and truncates+rewrites `~/hl-pipeline/reports/2026-08-24/payment-2026-08-24.csv` roughly every 20 seconds, so an `scp`/`cat` of that path can capture a truncated file, and H's own cadence stretches (each tick also pays up to three 60 s urlopen timeouts in `telegram.send_message`), delaying the 60 s autoscale ticks that are the driver's only 429 backpressure response. Payment correctness is unaffected — stamps and the anchor are still post-send — but the ops surface and the control loop both degrade.
FIX: Give the daily/folder-issues duties their own cadence stamp in H (like `_next_sweep`), e.g. retry at most every 10 minutes after a failed send, and write the sheet with tmp+os.replace as `_send_digest` already does for its anchor.


######## 24 [minor] pipeline/ingest.py:262 (lane core-flow) votes=2
TITLE: quarantine-path heal resets the slot like supersede but skips supersede's dossier archive, so the new upload's evidence is mixed with the old
CLAIM: The QUARANTINED-path heal (ingest.py:245-297) states it is 'a FRESH-upload event: reset the slot like supersede does' and duplicates supersede's ledger reset (fix_attempts, durations, rrd_sampled, delivered_at, uploaded_reported_at), the work-dir wipe and the _locked_report_remove. It does NOT do the one thing Ledger.supersede does first: archive dossiers/<sid>/* into history/superseded-<stamp>/ (ledger.py:173-182). The dossier is the evidence of record behind payment (design §13, and r-loop 4 made fixlog.json atomic + corrupt-preserving for exactly that reason), and fix._append_fixlog APPENDS to the existing dossiers/<sid>/fixlog.json (fix.py:316-345) while validate._write_verdict overwrites verdict.json and deliver.finalize_rejected overwrites coaching.md.
SCENARIO: A session is uploaded at a malformed path, quarantined AFTER download by a validation crash (so dossiers/<sid>/verdict.json and fixlog.json exist), and the operator then fixes the folder tree. The next scan heals the row to DISCOVERED and it is re-downloaded and re-fixed. The new pass overwrites verdict.json — the prior verdict is gone — and appends its fix entries to the SAME fixlog.json, so the audit trail for the new bytes silently contains fixes applied to different bytes with no separator. A payment dispute is then adjudicated against a merged, partly-overwritten record; the identical situation reached through supersede keeps both generations under history/.
FIX: Extract supersede's dossier-archive block into a helper (e.g. Ledger._archive_dossier(sid, dossier_root)) and call it from the heal branch immediately before the ledger.update at ingest.py:262, so both fresh-upload paths preserve the prior generation identically.


######## 25 [minor] tools/recal_regen_sheets.py:91 (lane tools) votes=2
TITLE: regenerated reject detail is windowed strictly on [lo,hi) while the CSV columns also count pre-window late arrivals
CLAIM: `cohort_reject_detail` drops every REJECTED row whose top-root upload time falls outside `[lo_dt, hi_dt)` (line 91), and `rewrite_reject_section` REPLACES the sheet's whole '## Reject detail' section with that list. But the CSV/MD table it accompanies is built by `build_sheet_rows`, whose LATE-ARRIVAL guard deliberately counts unstamped countable roots with `up < lo_dt` into the current window (reports.py:432-467) — their uploaded hours and their `*_rejection_reasons` labels both land in the sheet. The comment at lines 82-84 asserts the cohort view is 'the cohort view the regenerated sheet's columns already use', which is false for exactly those rows. The 08-15 sheet is the exposed one: it is the first sheet of record (its lo=2026-08-14T06:45:22 is the no-anchor 24 h fallback), so every root uploaded before that instant enters through the late guard, and recal_rebuild_reset nulled all their stamps.
SCENARIO: Root R uploaded 2026-08-13T20:00Z, terminal and unstamped after the refix re-run. Regenerating the 08-15 sheet: R is not in_window (up < lo) but is countable and unstamped, so build_sheet_rows counts its hours and appends its reject labels to the player's `kamla_rejection_reasons` cell. `cohort_reject_detail` skips R at line 91 because 2026-08-13T20:00Z < lo_dt. The published, VOID-superseding payment document therefore names a reject class in the player's row with no matching evidence line and no dossier path in the '## Reject detail' section it was rewritten to be argued from — and if R is the only pre-window reject for that day the section reads '- none in this upload cohort' while the table above it shows unpaid hours.
FIX: Window the detail on the same population the sheet counted, not on a fresh [lo,hi) test: have `write_payment_sheet` return (or reuse) `counted_out`, then list rejects whose top root is in `counted` (plus in-window rows), so the CSV columns and the MD detail describe the same set. If a strict cohort view is still wanted, label the omission explicitly and print a second 'late arrivals credited to this window' block rather than dropping them silently.


######## 26 [minor] translator/translate.py:63 (lane translator) votes=2
TITLE: r-loop 4 hardened inputs.jsonl against non-UTF-8 but left its two siblings (keybind.json, metadata.json) strict in the same fix paths
CLAIM: load_events (translate.py:45) now opens with errors='replace' plus a dict guard, on the stated grounds that a single cp1252 byte from a non-US layout used to raise out of the translator. The two files that arrive in the same upload and are read in the same functions are still strict UTF-8 with no guard: resolve_keybind at translate.py:63 (`json.loads(Path(keybind_path).read_text())`, and `_as_semantic_to_literal` then calls `raw.values()` with no type check) and metadata.json at v2.py:243 and pipeline/fix.py:501. keybind.json is the file that consists entirely of key names, and metadata.json carries player-typed free text (session.role, session.objective_task in the real sample under latest_requirements/), so both are strictly more exposed to the cited failure than inputs.jsonl. translator/v2.py:843 already guards its own metadata.json read (its except tuple includes ValueError, which covers UnicodeDecodeError) — these callers do not.
SCENARIO: A player on a cp1252 Windows locale types an accented objective into HumynCapture (or has an accented key name in keybind.json). The bundle uploads fine (ingest md5-verifies only video.mp4 and never parses these files). The session needs FIX_RETRANSLATE: fix.py:501 raises UnicodeDecodeError, apply_fixes (fix.py:308-312) records the step as failed and aborts the chain, the reason survives revalidation untouched, attempt 2 fails identically, and continuous.py:730 writes REJECTED 'fix retries exhausted (R2)' — an unpaid player and, per the reject-reason rules, a bare fix-failed marker for ops with no actionable reason. The same crash reaches FIX_TRANSLATE_RAW via v2.py:243.
FIX: Read both with errors='replace' (and json.JSONDecodeError/OSError guarded) exactly as load_events does; in resolve_keybind also `if not isinstance(raw, dict): return built-in fallback` before _as_semantic_to_literal touches .values().
